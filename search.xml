<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>2023年北航夏令营经历</title>
    <url>/2023/07/02/2023%E5%B9%B4%E5%8C%97%E8%88%AA%E5%A4%8F%E4%BB%A4%E8%90%A5%E7%BB%8F%E5%8E%86/</url>
    <content><![CDATA[<center>北航计算机夏令营经历（优营）</center>

<span id="more"></span>
<p>2023年6月30日至2023年7月2日，北航计算机学院开展夏令营活动，这里记录一下活动的经历。</p>
<h3 id="个人背景"><a href="#个人背景" class="headerlink" title="个人背景"></a>个人背景</h3><p>在网站的<a href="https://xwblog.xyz/about/">关于页面</a>中有我的cv，大概如下。</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>key</th>
<th>value</th>
</tr>
</thead>
<tbody>
<tr>
<td>title</td>
<td>南开大学</td>
</tr>
<tr>
<td>rank</td>
<td>10/161(6.2%)</td>
</tr>
<tr>
<td>科研</td>
<td>0</td>
</tr>
<tr>
<td>竞赛</td>
<td>0</td>
</tr>
<tr>
<td>CSP</td>
<td>345(1.5%)</td>
</tr>
</tbody>
</table>
</div>
<h3 id="流程大致介绍"><a href="#流程大致介绍" class="headerlink" title="流程大致介绍"></a>流程大致介绍</h3><p>首先就是夏令营报名，北航的bar相对于华五之类的是要低一点的。夏令营第一天上午报到加宣讲，下午机试，我机试用CSP抵了(今年机试好像不是很难，第一题比较简单，第二题是个编译的大模拟？)。机试当天晚上就能出结果，我们这一届学硕入营380个左右，机试之后剩下250个左右。机试当天晚上会给后两天的面试分组名单。后两天就根据分组进行面试。面试也是第二天面完，晚上就出优营和候补名单了，我们100个优营，20个候补。</p>
<h3 id="面试过程"><a href="#面试过程" class="headerlink" title="面试过程"></a>面试过程</h3><p>由于我没有参加机试，这里也不好对机试题目进行评价，具体说一下面试流程吧。下面，按顺序列出我面试时被问到的东西和大致流程（顺序可能记错QAQ）。</p>
<ol>
<li>将报名的纸质材料交给老师。</li>
<li>坐下，读个承诺书，保证材料真实，否则后果自负。好像没有要求不能透露面试问题（也可能我没有注意到）。</li>
<li>抽一道政治题（就是物理上的抽，题目都写到纸条上，随机抽一道即可），我抽到了是个啥高科技自立自主啥的，我照着卡脖子技术给他答得。答得时候老师们基本没有很认真的挺，大概都在看我的材料。</li>
<li>抽到英语小小短文翻译（这个不是自己抽，老师直接递给我了一个），我的是翻译一个深度学习应用于图像降噪的短文，感觉翻译的时候老师也没有很认真的听。</li>
<li>开始问数学问题，一共问了我俩，顺序记不清了。一个是<strong>讲讲正定矩阵和半正定矩阵</strong>，一个是<strong>讲讲大数定理和中心极限定理</strong>，这俩问题比较简单，说了就行（还好没问高数，高数我只知道可导可微和连续啥的）。</li>
<li>开始问专业问题，老师先问我物理内存怎么管理，我一开始没理解对，答了虚拟内存物理内存映射之类的。之后老师说他的意思是想让我答怎么管理物理内存，就是字面上的管理。我一开始有点懵，操作系统学过什么first fit、best fit啥的，但是我一开始没想起来，就记得ucore实验的时候自己实现过伙伴系统，就答了伙伴系统，然后老师就开始往深了问，但是伙伴系统我其实细节也忘得差不多了，最后答得也算中规中矩吧。不过老师说我是目前（目前还是当天来着）唯一一个答出来伙伴系统的，感觉我答得还算深入吧。</li>
<li>问完内存管理之后，问os的老师大概是满足了，问其他老师有没有啥想问的，有个老师问我学过数据库没。当然学过。然后他又问我知不知道关系代数，emm，这个蒸滴忘了，我说我知道关系是啥，关系代数不记得了。然后老师说关系代数就是表嘛。……6，然后他说，没事儿会用就行。</li>
<li>接下来问我os的老师又有问题了，他问我知不知道TLB，这个很简单，给他答了。结果他又问，知不知道目前的TLB有几项，这个当然不会，对不起了。然后他又问我知不知道大页，我说知道，数据库好像会用到大页，其他的不太懂，然后这个就过了。</li>
<li>问os的老师问完os后，又心血来潮问我数据库三范式是啥，这里他的意思是让我只说第三范式。我一听三范式，就开始从第一范式说，然后就被打断了QAQ（这感觉不能怪我，三范式确实有点歧义）。然后我答没有属性对非主属性的依赖，他说不对。这里他想让我答没有传递依赖……不过最后我还是答出来了。</li>
<li>这中间，还有一个老师问了我未来研究方向，我跟他说比较想搞系统方向，比如体系结构、os之类的，目前还没有明确的方向。</li>
<li>最后老师看20分钟够了就让我走了。</li>
</ol>
<p>面试流程大概就这么多，写的有点臭长了TAT，还是要多锻炼写作。希望能给未来的保研er一点帮助。</p>
<p>最后，还有一点也是唯一一点想吐槽的就是，北航夏令营不报销食宿，我光住就花了1000出头，好歹免费让鼠鼠尝尝北航的饭呀TAT。</p>
<p>对了，最近想起来好像还问了我啥是欧拉图。我还答错了。</p>
]]></content>
      <tags>
        <tag>夏令营</tag>
      </tags>
  </entry>
  <entry>
    <title>ARM加法指令中的立即数</title>
    <url>/2023/01/08/ARM%E5%8A%A0%E6%B3%95%E7%AB%8B%E5%8D%B3%E6%95%B0/</url>
    <content><![CDATA[<center>灵活的第二操作数</center>

<span id="more"></span>
<h1 id="ARM加法指令中的立即数"><a href="#ARM加法指令中的立即数" class="headerlink" title="ARM加法指令中的立即数"></a>ARM加法指令中的立即数</h1><p>最近在写编译器的时候，生成形如add sp, sp, #imm的arm汇编指令的时候经常报出立即数错误，然后查阅资料学习了一下。分享一下学习心得。</p>
<p>首先，arm中有这样一种指令：ADD Rd, Rn, #imm12，这种指令里面，可以使用12位的立即数，立即数范围为0-4095，但是，这种指令在arm状态下是不可用的，在Thumb状态下才可用。</p>
<p>在arm状态下，如果想加减一个立即数，使用的是这样的指令：ADD{S} Rd, Rn, Operand2。这里我们就需要关注一下arm中的第二操作数作为立即数时候能表示的范围。</p>
<h2 id="灵活的第二操作数"><a href="#灵活的第二操作数" class="headerlink" title="灵活的第二操作数"></a>灵活的第二操作数</h2><p>上述说到的第二操作数在arm中的用法比较灵活，它可以作为：</p>
<ul>
<li>立即数</li>
<li>寄存器</li>
<li>寄存器移位</li>
</ul>
<p>这里我们重点说明第二操作数做立即数时候的表示机制和表示范围。</p>
<p>首先，第二操作数有12位，因此我们不难想到按照传统的表示方法，它最多也就表示0-4095的立即数。而为了进一步扩大第二操作数能表示的立即数范围，arm对其中的立即数的存储格式进行了改动。这12位的格式如下图所示。由4位的位移和8位的立即数组成。这里我们定义”&gt;&gt;”符号为<strong>循环右移</strong>，那么这12位实际表示的立即数就是<script type="math/tex">immediate>>(2*rotate)</script></p>
<p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202301081158973.png" alt=""></p>
<p>上述表达式比较书面的描述就是立即数是由一个8位的常数循环右移偶数位得到的。这里我们考虑这样设计的原因，rotate域为4位，那么它表示的范围为0-15，乘2之后可以表示0-30中的偶数，这也就是为什么是循环右移偶数位。同时，8位立即数最多可以循环右移30位，这就使得立即数能够表示的范围大大增加了(这里的范围不是指能表示的立即数的个数，而是大小)，不再只局限于0-4095了。但是我们进行思考，其实这种方式能够表示的立即数的个数是$2^8*2^4=2^{12}$，还是没变，只是能够表示更大的立即数了。</p>
<p>虽然可以表示更大的立即数了，但是显然，有些立即数是无法使用这个格式来表示的，那么如何判断一个立即数可以使用这种格式来合法的表示就成了我们写编译器的人的一个要解决的问题。我看网上有比较奇怪的方法来判断一个立即数可不可以被合法的表示。这里我们用比较简单的方法，下面我提出两种比较简单的方法来判断一个立即数是否可以用上述这种12位的方式来表示。</p>
<h3 id="判断方法一"><a href="#判断方法一" class="headerlink" title="判断方法一"></a>判断方法一</h3><p>既然立即数是由8位立即数循环右移偶数位得到的，那么我们倒过来，对于一个立即数，我们将他循环左移偶数位，如果结果能够用8位来表示，那么表示这个立即数可以被合法表示。</p>
<p>代码如下：<br><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">isLegalImm</span><span class="params">(<span class="type">int</span> imm)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 这里为啥要强制类型转换嘞，因为下边要用到右移，而对于有符号数右移，如果是负数，算数右移会在前边补1。</span></span><br><span class="line">    <span class="comment">// 无符号数右移就是逻辑右移</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> num = (<span class="type">unsigned</span> <span class="type">int</span>)imm;</span><br><span class="line">    <span class="comment">// 思路就是每次循环左移两位，看看结果是否可以用8位来表示了。</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">16</span>; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (num &lt;= <span class="number">0xff</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        num = ((num &lt;&lt; <span class="number">2</span>) | (num &gt;&gt; <span class="number">30</span>));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>(代码不保证对哦，但是思路应该没问题)</p>
<h3 id="判断方法二"><a href="#判断方法二" class="headerlink" title="判断方法二"></a>判断方法二</h3><p>上边已经提到了，就算用arm里这种比较创新的表示方法，最多也就是表示$2^{12}$个立即数，一个立即数4个字节，那么存储所有合法立即数需要$2^{14}=16384$个字节，大约16KB的内存，如果我们内存资源不是很紧张的话，这里我们完全可以提前把所有能够合法表示的立即数给计算出来，存储到哈希表里，查的时候就会比较快。再次体现了空间和时间上的tradeoff。</p>
<p>这种方法就不写代码了，比较简单。可以用C++里的map很方便的实现。</p>
]]></content>
      <tags>
        <tag>编译</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2022/12/13/Hello%20World/</url>
    <content><![CDATA[<center>Hello World!</center>

<span id="more"></span>
<p>Welcome to Bin’s blog.</p>
]]></content>
  </entry>
  <entry>
    <title>ARM浮点数翻译心得</title>
    <url>/2023/01/13/ARM%E6%B5%AE%E7%82%B9%E6%95%B0%E7%BF%BB%E8%AF%91%E5%BF%83%E5%BE%97/</url>
    <content><![CDATA[<center>ARM浮点数</center>

<span id="more"></span>
<h1 id="ARM浮点数翻译心得"><a href="#ARM浮点数翻译心得" class="headerlink" title="ARM浮点数翻译心得"></a>ARM浮点数翻译心得</h1><p>编译原理实现的Sysy编译器支持使用float浮点数。这里讲一下arm汇编代码生成浮点数<br>部分遇到的一些问题和心得。</p>
<p>把重要的东西放到前边来说，下边的文章可以一点不看。<a href="https://godbolt.org/">godbolt</a>这个网站真的强烈推荐用于写编译器时做个参考。长下图这样，在左边写代码，右边几乎可以实时出LLVM IR代码或者汇编代码。还有很多种类和版本的编译器可以选择，在不知道该怎么翻译时这个网站可以作为一个不错的参考。主要就是方便，其实我们也完全可以在本机编译，但是比较麻烦，这个网站好处就是快，方便，编译器种类多，<strong>好看</strong>。<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202301131741547.jpg" alt=""></p>
<h2 id="浮点数常量的表示"><a href="#浮点数常量的表示" class="headerlink" title="浮点数常量的表示"></a>浮点数常量的表示</h2><p>首先就是浮点字面值常量的表示了。以下边这一段代码为例：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> pi = <span class="number">3.14</span>;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">float</span> r = <span class="number">10.0</span>;</span><br><span class="line">    <span class="built_in">putfloat</span>(pi * r * r);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以看到，出现了两个浮点数字面值常量，分别是3.14和10.0。其中一个赋值给全局变量，一个赋值给局部变量，但是它俩的表示方式其实是一样的。在arm汇编中我们应该怎么表示它们呢？比较直接的想法就是下边这样，直接把3.14给糊上去不就行啦？但是其实这样是不对的，汇编的时候会报错。</p>
<figure class="highlight arm"><table><tr><td class="code"><pre><span class="line"><span class="symbol">.section</span> <span class="meta">.data</span></span><br><span class="line"><span class="symbol">.global</span> pi</span><br><span class="line"><span class="symbol">.size</span> pi, <span class="number">4</span></span><br><span class="line"><span class="symbol">pi:</span></span><br><span class="line">	<span class="meta">.word</span> <span class="number">3</span>.<span class="number">14</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>AFAIK just printing a decimal float works. If you really want a hexadecimal encoding, just reinterpret the floating-point number as an integer and print in hexadecimal; an “LLVM float” is just an IEEE float printed in hexadecimal.</p>
</blockquote>
<p>这里我们借鉴的是LLVM IR代码中浮点数表示的思路，上边这段话的最后一句，LLVM浮点数其实就是一个浮点数以16进制的形式打印出来(当然，这只是一种表现形式，除了16进制之外，还可以用科学计数法之类的表示LLVM浮点数)。在arm汇编中，我们直接把所有的float字面值常量都转译成对应的32位无符号整数就行了。重点就是保持无符号整数的位模式和它对应的float值的位模式一样(遵从IEEE格式)。代码也很简单，如下所示，value存储了对应的float常量值，保持位模式不变，把它翻译成无符号整数就行，然后打印的时候就打印v的值即可。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> value = (<span class="type">float</span>)<span class="built_in">dynamic_cast</span>&lt;ConstantSymbolEntry *&gt;(se)-&gt;<span class="built_in">getValue</span>();</span><br><span class="line"><span class="type">uint32_t</span> v = <span class="built_in">reinterpret_cast</span>&lt;<span class="type">uint32_t</span> &amp;&gt;(value);</span><br><span class="line"><span class="comment">// 上边这一句也可以这样写</span></span><br><span class="line"><span class="comment">// uint32_t v = *((uint32_t*)&amp;value);</span></span><br></pre></td></tr></table></figure>
<p>根据我们的处理方式，开头代码中的float pi = 3.14;可以如下翻译：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">.section .data</span><br><span class="line">.global pi</span><br><span class="line">.size pi, <span class="number">4</span></span><br><span class="line">pi:</span><br><span class="line">    .word <span class="number">1078523331</span></span><br></pre></td></tr></table></figure>
<p>1078523331的16进制就是4048F5C3。</p>
<p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202301131716419.jpg" alt=""></p>
<p>上图的网站为<a href="https://tooltt.com/floatconverter/">https://tooltt.com/floatconverter/</a>。</p>
<p>想把浮点数常量放到某个寄存器里也是类似的，把对应的无符号整数想办法放到寄存器里就行。</p>
<h2 id="软浮点和硬浮点"><a href="#软浮点和硬浮点" class="headerlink" title="软浮点和硬浮点"></a>软浮点和硬浮点</h2><p>解决了浮点常量的表示之后我们就要考虑怎么翻译具体的浮点运算之类的指令了。这里有两种方案，一种是软浮点，就是没有显式的浮点运算指令，浮点运算纯靠调用ABI；还有一种方案是硬浮点，就是使用专用寄存器s那一套还有浮点运算指令。由于一开始arm芯片没有浮点运算单元FPU(就是个协处理器)，arm早期使用普通指令来模拟浮点运算，这种方法比较慢，但是用起来简单。慢慢地随着发展就逐渐的向使用专用的浮点运算指令过渡，这种方法比较快。但是，这里就出现了一个问题，对于没有FPU的芯片，硬浮点编译的程序显然跑不起来，这就是向前不兼容？那我有FPU，你总不能不让我用把，于是就出现了一个中间态，还是用的软浮点的一些规定和接口，但是软浮点里面ABI之类的可以用专门的浮点指令来实现。</p>
<p>扯得有点远了，想了解更多上网搜，一搜一大堆，比如<a href="https://blog.csdn.net/sdsszk/article/details/108680822">这个</a>。这里我们只要明确，翻译浮点汇编代码时，有两种不同的方式，一种是软浮点，它的感觉就是要浮点运算就调用对应的函数，不需要其他的专用寄存器啥的；另一种是硬浮点，它的感觉就是为浮点运算再量身打造一套寄存器和指令，浮点运算就使用专门的寄存器和指令。下边会举几个例子来进行大致的说明。</p>
<p>对了，还有一点就是用arm-linux-gnueabihf-gcc默认是硬浮点架构，arm-linux-gnueabi-gcc默认是软浮点架构，这里在编译libsysy.a这个库的时候要注意，不然链接的时候会出错。hf=hard float?</p>
<h3 id="软浮点实现"><a href="#软浮点实现" class="headerlink" title="软浮点实现"></a>软浮点实现</h3><p>下边来举几个例子切身感受一下软浮点的风格。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> a, b, c; <span class="comment">// 这里就不赋初值了</span></span><br><span class="line">a = b + c;</span><br><span class="line">a = b - c;</span><br><span class="line">a = b * c;</span><br><span class="line">a = b / c;</span><br></pre></td></tr></table></figure>
<p>对于上边这段代码，软浮点咋翻译嘞？如下所示：(手翻的，意思对，但不一定一点问题都没)</p>
<figure class="highlight arm"><table><tr><td class="code"><pre><span class="line"><span class="keyword">sub</span> <span class="built_in">sp</span>, <span class="built_in">sp</span>, <span class="number">#12</span> <span class="comment">; 开栈空间</span></span><br><span class="line"></span><br><span class="line"><span class="comment">; a = b + c;</span></span><br><span class="line"><span class="keyword">ldr</span> <span class="built_in">r0</span>, [<span class="built_in">fp</span>, #-<span class="number">8</span>]   <span class="comment">;</span></span><br><span class="line"><span class="keyword">ldr</span> <span class="built_in">r1</span>, [<span class="built_in">fp</span>, #-<span class="number">12</span>]  <span class="comment">; 取出b和c</span></span><br><span class="line"><span class="keyword">bl</span> __aeabi_fadd     <span class="comment">; 这个就是ABI，就是调库，就跟调用普通函数一样</span></span><br><span class="line"><span class="keyword">str</span> <span class="built_in">r0</span>, [<span class="built_in">fp</span>, #-<span class="number">4</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment">; a = b - c;</span></span><br><span class="line"><span class="keyword">ldr</span> <span class="built_in">r0</span>, [<span class="built_in">fp</span>, #-<span class="number">8</span>]   <span class="comment">;</span></span><br><span class="line"><span class="keyword">ldr</span> <span class="built_in">r1</span>, [<span class="built_in">fp</span>, #-<span class="number">12</span>]  <span class="comment">; </span></span><br><span class="line"><span class="keyword">bl</span> __aeabi_fsub     <span class="comment">; </span></span><br><span class="line"><span class="keyword">str</span> <span class="built_in">r0</span>, [<span class="built_in">fp</span>, #-<span class="number">4</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment">; a = b * c;</span></span><br><span class="line"><span class="keyword">ldr</span> <span class="built_in">r0</span>, [<span class="built_in">fp</span>, #-<span class="number">8</span>]   <span class="comment">;</span></span><br><span class="line"><span class="keyword">ldr</span> <span class="built_in">r1</span>, [<span class="built_in">fp</span>, #-<span class="number">12</span>]  <span class="comment">; </span></span><br><span class="line"><span class="keyword">bl</span> __aeabi_fmul     <span class="comment">; </span></span><br><span class="line"><span class="keyword">str</span> <span class="built_in">r0</span>, [<span class="built_in">fp</span>, #-<span class="number">4</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment">; a = b / c;</span></span><br><span class="line"><span class="keyword">ldr</span> <span class="built_in">r0</span>, [<span class="built_in">fp</span>, #-<span class="number">8</span>]   <span class="comment">;</span></span><br><span class="line"><span class="keyword">ldr</span> <span class="built_in">r1</span>, [<span class="built_in">fp</span>, #-<span class="number">12</span>]  <span class="comment">; </span></span><br><span class="line"><span class="keyword">bl</span> __aeabi_fdiv     <span class="comment">; </span></span><br><span class="line"><span class="keyword">str</span> <span class="built_in">r0</span>, [<span class="built_in">fp</span>, #-<span class="number">4</span>]</span><br></pre></td></tr></table></figure>
<p>通过上述样例，应该很好找到软浮点的感觉，其实就是调函数。下边就没啥好说的了，这里再列出几个翻译时候会用到的函数吧。</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>function</th>
<th style="text-align:left">note</th>
</tr>
</thead>
<tbody>
<tr>
<td>__aeabi_fadd</td>
<td style="text-align:left">浮点加</td>
</tr>
<tr>
<td>__aeabi_fsub</td>
<td style="text-align:left">浮点减</td>
</tr>
<tr>
<td>__aeabi_fmul</td>
<td style="text-align:left">浮点乘</td>
</tr>
<tr>
<td>__aeabi_fdiv</td>
<td style="text-align:left">浮点除</td>
</tr>
<tr>
<td>__aeabi_fcmpeq</td>
<td style="text-align:left">浮点比较，==</td>
</tr>
<tr>
<td>__aeabi_fcmpge</td>
<td style="text-align:left">浮点比较，&gt;=</td>
</tr>
<tr>
<td>__aeabi_fcmplt</td>
<td style="text-align:left">浮点比较，&lt;</td>
</tr>
<tr>
<td>__aeabi_fcmple</td>
<td style="text-align:left">浮点比较，&lt;=</td>
</tr>
<tr>
<td>__aeabi_fcmpgt</td>
<td style="text-align:left">浮点比较，&gt;</td>
</tr>
<tr>
<td>__aeabi_f2iz</td>
<td style="text-align:left">浮点转整型</td>
</tr>
<tr>
<td>__aeabi_i2f</td>
<td style="text-align:left">整型转浮点</td>
</tr>
</tbody>
</table>
</div>
<h3 id="硬浮点实现"><a href="#硬浮点实现" class="headerlink" title="硬浮点实现"></a>硬浮点实现</h3><p>至于硬浮点，就要用一套新的寄存器了s0-s31。</p>
<p>还是翻译上边的代码，具体如下：</p>
<figure class="highlight arm"><table><tr><td class="code"><pre><span class="line"><span class="keyword">sub</span> <span class="built_in">sp</span>, <span class="built_in">sp</span>, <span class="number">#12</span> <span class="comment">; 开栈空间</span></span><br><span class="line"></span><br><span class="line"><span class="comment">; a = b + c</span></span><br><span class="line"><span class="symbol">vldr.32</span> <span class="built_in">s16</span>, [<span class="built_in">sp</span>, #-<span class="number">8</span>]</span><br><span class="line"><span class="symbol">vldr.32</span> <span class="built_in">s17</span>, [<span class="built_in">sp</span>, #-<span class="number">12</span>]</span><br><span class="line"><span class="symbol">vadd.f32</span> <span class="built_in">s18</span>, <span class="built_in">s16</span>, <span class="built_in">s17</span></span><br><span class="line"><span class="symbol">vstr.32</span> <span class="built_in">s18</span>, [<span class="built_in">sp</span>, #-<span class="number">4</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment">; a = b - c</span></span><br><span class="line"><span class="symbol">vldr.32</span> <span class="built_in">s16</span>, [<span class="built_in">sp</span>, #-<span class="number">8</span>]</span><br><span class="line"><span class="symbol">vldr.32</span> <span class="built_in">s17</span>, [<span class="built_in">sp</span>, #-<span class="number">12</span>]</span><br><span class="line"><span class="symbol">vsub.f32</span> <span class="built_in">s18</span>, <span class="built_in">s16</span>, <span class="built_in">s17</span></span><br><span class="line"><span class="symbol">vstr.32</span> <span class="built_in">s18</span>, [<span class="built_in">sp</span>, #-<span class="number">4</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment">; a = b * c</span></span><br><span class="line"><span class="symbol">vldr.32</span> <span class="built_in">s16</span>, [<span class="built_in">sp</span>, #-<span class="number">8</span>]</span><br><span class="line"><span class="symbol">vldr.32</span> <span class="built_in">s17</span>, [<span class="built_in">sp</span>, #-<span class="number">12</span>]</span><br><span class="line"><span class="symbol">vmul.f32</span> <span class="built_in">s18</span>, <span class="built_in">s16</span>, <span class="built_in">s17</span></span><br><span class="line"><span class="symbol">vstr.32</span> <span class="built_in">s18</span>, [<span class="built_in">sp</span>, #-<span class="number">4</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment">; a = b / c</span></span><br><span class="line"><span class="symbol">vldr.32</span> <span class="built_in">s16</span>, [<span class="built_in">sp</span>, #-<span class="number">8</span>]</span><br><span class="line"><span class="symbol">vldr.32</span> <span class="built_in">s17</span>, [<span class="built_in">sp</span>, #-<span class="number">12</span>]</span><br><span class="line"><span class="symbol">vdiv.f32</span> <span class="built_in">s18</span>, <span class="built_in">s16</span>, <span class="built_in">s17</span></span><br><span class="line"><span class="symbol">vstr.32</span> <span class="built_in">s18</span>, [<span class="built_in">sp</span>, #-<span class="number">4</span>]</span><br></pre></td></tr></table></figure>
<p>可以看到，不再是调用函数了，浮点运算都有了自己的指令。想了解更多硬浮点的指令，可以去arm官网上看<a href="https://developer.arm.com/documentation/den0018/a/NEON-and-VFP-Instruction-Summary/List-of-all-NEON-and-VFP-instructions?lang=en">https://developer.arm.com/documentation/den0018/a/NEON-and-VFP-Instruction-Summary/List-of-all-NEON-and-VFP-instructions?lang=en</a>。</p>
<p>实现硬浮点，要改的代码更多。</p>
<h2 id="ARM中的栈指针对齐"><a href="#ARM中的栈指针对齐" class="headerlink" title="ARM中的栈指针对齐"></a>ARM中的栈指针对齐</h2><p>按照约定，ARM中的堆栈指针要八字节对齐，否则在处理浮点数的时候可能会出现比较奇怪的错误。<a href="https://blog.51cto.com/u_12956289/2917318">一个参考网站。</a>这里说明我们栈指针对齐的方法。先把栈指针逻辑右移三位，再把它逻辑左移三位，这样就8字节对齐了。</p>
<figure class="highlight arm"><table><tr><td class="code"><pre><span class="line"><span class="keyword">lsr</span> <span class="built_in">sp</span>, <span class="built_in">sp</span>, <span class="number">#3</span></span><br><span class="line"><span class="keyword">lsl</span> <span class="built_in">sp</span>, <span class="built_in">sp</span>, <span class="number">#3</span></span><br></pre></td></tr></table></figure>
<h2 id="ARM中函数调用中浮点数和整数混合传参"><a href="#ARM中函数调用中浮点数和整数混合传参" class="headerlink" title="ARM中函数调用中浮点数和整数混合传参"></a>ARM中函数调用中浮点数和整数混合传参</h2><p>如果函数参数中既有整数又有浮点数，那么它们互不相干。也就是说，整数用r0-r3寄存器传递，浮点数用s0-s15寄存器传递。有可能有整数寄存器不够用了需要用栈传参，而此时浮点数s寄存器还够用的情况。可以看<a href="https://developer.arm.com/documentation/den0018/a/Compiling-NEON-Instructions/NEON-assembler-and-ABI-restrictions/Passing-arguments-in-NEON-and-floating-point-registers?lang=en">官网</a>。</p>
]]></content>
      <tags>
        <tag>编译</tag>
      </tags>
  </entry>
  <entry>
    <title>VSCODE比较代码差异</title>
    <url>/2022/12/15/VSCODE%E6%AF%94%E8%BE%83%E4%BB%A3%E7%A0%81%E5%B7%AE%E5%BC%82/</url>
    <content><![CDATA[<center>VSCODE比较代码差异</center>

<span id="more"></span>
<p>这学期的实验老是要把代码从上一个实验移到下一个实验，由于使用win+vmware，我直接在windows的vscode里进行代码编写，利用vscode连接linux虚拟机。当要合并代码时就很烦。之前使用linux的meld，但是要进入到vmware里。最后我发现，使用vscode也可以进行代码的对比和合并，虽然功能大概没有meld强大（<strong>而且目前好像只能比较两个文件</strong>），但是目前够用了。下面简单说一下大致用法。</p>
<ul>
<li>选中两个文件<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212081131962.jpg" alt=""></li>
<li>右键，选择“将已选项进行比较”<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212081132639.jpg" alt=""></li>
</ul>
<p>然后我们就可以对比文件差异了，如下图所示，我们也可以点击箭头进行代码移动。</p>
<p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212081133074.jpg" alt=""></p>
]]></content>
      <tags>
        <tag>小科技</tag>
      </tags>
  </entry>
  <entry>
    <title>acm dp学习</title>
    <url>/2023/11/01/acm-dp%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<center>动态规划</center>

<span id="more"></span>
<h1 id="树形动态规划"><a href="#树形动态规划" class="headerlink" title="树形动态规划"></a>树形动态规划</h1><h2 id="树上背包"><a href="#树上背包" class="headerlink" title="树上背包"></a>树上背包</h2><h3 id="P1"><a href="#P1" class="headerlink" title="P1"></a>P1</h3><p>给定一个n个点的有根树，每个点有一个权值。要求对于每个点，回答在它的子树中选择一个大小恰好为m的包含这个点的连通块的最大权值和。</p>
<p>考虑答案的结构，以u为根的子树中选取大小为m的最优的连通块方案中，u的孩子的选取方案也一定是最优的。我们要解决的便是把m-1个点分配个u的所有孩子$c_1,c_2…c_n$，每个孩子对应的子树选择$m_1,m_2…m_n$个点，求最大可能的权值和。这显然是一个背包问题。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// O(nm) 在某个子树内包含子树根的m个点的连通块的最大权值和</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="type">const</span> <span class="type">int</span> MAXN = <span class="number">2005</span>;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> MAXM = MAXN;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> inf = <span class="number">1</span> &lt;&lt; <span class="number">29</span>;</span><br><span class="line">vector&lt;<span class="type">int</span>&gt; tree[MAXN];</span><br><span class="line"><span class="type">int</span> dp[MAXN][MAXM];</span><br><span class="line"><span class="type">int</span> sz[MAXN];</span><br><span class="line"><span class="type">int</span> n, q, M = <span class="number">100</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> u)</span> </span>&#123;</span><br><span class="line">    sz[u] = <span class="number">1</span>;</span><br><span class="line">    <span class="type">static</span> <span class="type">int</span> tmp[MAXN];</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> son : tree[u]) &#123;</span><br><span class="line">        <span class="built_in">dfs</span>(son);</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= <span class="built_in">min</span>(M, sz[u] + sz[son]); i++)</span><br><span class="line">            tmp[i] = -inf;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= <span class="built_in">min</span>(M, sz[u]); i++) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt;= <span class="built_in">min</span>(M - i, sz[son]); j++) &#123;</span><br><span class="line">                tmp[i + j] = <span class="built_in">max</span>(tmp[i + j], dp[u][i] + dp[son][j]);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= <span class="built_in">min</span>(M, sz[u] + sz[son]); i++)</span><br><span class="line">            dp[u][i] = tmp[i];</span><br><span class="line">        sz[u] += sz[son];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; q;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">2</span>; i &lt;= n; i++) &#123;</span><br><span class="line">        <span class="type">int</span> f;</span><br><span class="line">        cin &gt;&gt; f;</span><br><span class="line">        tree[f].<span class="built_in">push_back</span>(i);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">        cin &gt;&gt; dp[i][<span class="number">1</span>];</span><br><span class="line">    <span class="built_in">dfs</span>(<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">while</span> (q--) &#123;</span><br><span class="line">        <span class="type">int</span> u, m;</span><br><span class="line">        cin &gt;&gt; u &gt;&gt; m;</span><br><span class="line">        cout &lt;&lt; dp[u][m] &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="P2"><a href="#P2" class="headerlink" title="P2"></a>P2</h3><p>给定一个n个点的有根树，每个点有一个重量$w_i$和权值$v_i$。要求回答选择一个重量和<strong>恰好</strong>为m的<strong>包含根的连通块</strong>最大的权值和。</p>
<p>对于该问题，我们可以使用和上一题类似的解法来解，令$dp[i][j]$表示i这棵子树选择重量和恰好为j的连通块的最大权值和。但是，对于每个节点，我们都需要$O(m^2)$的时间复杂度去更新dp数组，最终的时间复杂度为$O(nm^2)$，比较耗时。</p>
<p>这里我们可以使用一个技巧，即在<strong>dfs序</strong>上做dp，将问题转化为一个特殊的线性问题。如下图所示的树，对应的dfs序为：$3\ 1\ 4\ 8\ 6\ 2\ 5\ 7$。这里我们定义r函数，$r(u)$表示dfs序中在$u$这棵子树后被遍历的第一个节点在dfs序中的下标。比如下图中1对应的子树遍历完后首先将会遍历节点2，则$r(1)=6$，即2在dfs序中的下标(这里下标从1开始计数)。<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202311011652728.jpg" alt="tree"></p>
<p>根据题意我们可以得知，如果一个节点不选，那么该节点对应的子树中其余的节点也一定不会被选(因为已经和根节点不连通了)，也就是直接跳过这棵子树。这里我们定义$dp[i][j]$表示在dfs序下标从i到n的节点中选取重量和恰为j的若干节点的最大权值，且满足若某一个节点被选择，其在dfs序中位于i到n的祖先也一定被选择。则我们可以得到状态转移方程：$dp[i][j]=max(dp[r[dfs[i]]][j],dp[i+1][j-w_i]+v_i)$，其中$dfs[i]$表示dfs序中第i个节点的编号。状态转移含义为，如果下标为i的节点不选，那么它对应的子树也不选，直接忽略整颗子树，从$r[dfs[i]]$继续考虑即可；如果选择$dfs[i]$，那么可以继续考虑其孩子是否选择，即继续考虑下标为i+1的节点。</p>
<p>最终时间复杂度为$O(nm)$。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// O(nm) dfs序</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="type">const</span> <span class="type">int</span> N = <span class="number">2005</span>;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> inf = <span class="number">1</span> &lt;&lt; <span class="number">29</span>;</span><br><span class="line">vector&lt;<span class="type">int</span>&gt; tree[N];</span><br><span class="line"><span class="type">int</span> dp[N][N];</span><br><span class="line"><span class="type">int</span> w[N], a[N], l[N], id[N], r[N];</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> n, m, tot;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> u)</span> </span>&#123;</span><br><span class="line">    l[u] = ++tot;</span><br><span class="line">    id[tot] = u;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> v : tree[u])</span><br><span class="line">        <span class="built_in">dfs</span>(v);</span><br><span class="line">    r[u] = tot;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; m;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">2</span>; i &lt;= n; i++) &#123;</span><br><span class="line">        <span class="type">int</span> f;</span><br><span class="line">        cin &gt;&gt; f;</span><br><span class="line">        tree[f].<span class="built_in">push_back</span>(i);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">        cin &gt;&gt; a[i];</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">        cin &gt;&gt; w[i];</span><br><span class="line">    <span class="built_in">dfs</span>(<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">1</span>; j &lt;= m; j++)</span><br><span class="line">        dp[n + <span class="number">1</span>][j] = -inf;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = n; i &gt;= <span class="number">1</span>; i--) &#123;</span><br><span class="line">        <span class="type">int</span> u = id[i];</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt;= m; j++) &#123;</span><br><span class="line">            dp[i][j] = dp[r[u] + <span class="number">1</span>][j]; <span class="comment">// 不选u, u对应的子树都不能选</span></span><br><span class="line">            <span class="keyword">if</span> (j &gt;= w[u])</span><br><span class="line">                dp[i][j] = <span class="built_in">max</span>(dp[i][j], dp[i + <span class="number">1</span>][j - w[u]] + a[u]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt;= m; i++)</span><br><span class="line">        <span class="keyword">if</span> (dp[<span class="number">1</span>][i] &gt;= <span class="number">0</span>)</span><br><span class="line">            cout &lt;&lt; dp[<span class="number">1</span>][i] &lt;&lt; endl;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            cout &lt;&lt; <span class="number">0</span> &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="树上路径"><a href="#树上路径" class="headerlink" title="树上路径"></a>树上路径</h2><h3 id="P1-1"><a href="#P1-1" class="headerlink" title="P1"></a>P1</h3><p>给定一个n个节点的树和m条树上的简单路径，每个路径有一个权值，求不相交路径的最大权值和。</p>
<p>令$dp[u]$表示u这棵子树中不超出u点的不相交路径的最大权值和。这里的不超出u点指的是选择的路径不能从u点继续向上延申，也就是说路径两个端点的lca最高能到达u。比如下图中，$dp[1]$中不能包含选择路径$3148$的情况。<img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202311011747340.jpg" alt="tree"></p>
<p>接下来我们考虑状态转移，对于u这棵子树，如果不选择任何通过u节点的路径，那么$dp[u]= \sum_{i \in childs[u]} dp[i]$。如果选择了一条通过u节点的路径，$dp[u]$为从子树u中删除该路径后得到的森林中所有树的根节点的dp值的和与该路径的权值的和。以上图为例，如果我们选择了$3148$这条路径，那么$dp[3]=max(dp[3], dp[6]+dp[2]+v[3148])$。但是上述过程编程比较麻烦，这里进一步观察我们可以发现，如果不选择通过节点3的路径，$dp[3]_1=dp[1]+dp[2]$。我们先不考虑路径权值(最后加上就行)，如果选择路径$31$，$dp[3]_2=dp[4]+dp[6]+dp[2]=dp[3]_1+(\sum_{i \in childs[1]} dp[i]-dp[1])$。更进一步，如果选择路径$314$，$dp[3]_3=dp[8]+dp[6]+dp[2]=dp[3]_2+(\sum_{i \in childs[4]} dp[i]-dp[4])$。我们可以发现，我们每删除一个节点x，dp的增量为$\delta _x=\sum_{i \in childs[x]} dp[i]-dp[x]$。而$dp[u]$的初始值为$\sum_{i \in childs[u]} dp[i]$，这样我们只需要遍历一遍路径上所有的点便可以计算出选择该通过u的路径的情况下$dp[u]$的值。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// O(nm) 每个路径一个权值，问不相交的路径的最大权值和</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="type">long</span> <span class="type">long</span> ll;</span><br><span class="line"></span><br><span class="line"><span class="type">const</span> <span class="type">int</span> N = <span class="number">2010</span>;</span><br><span class="line"><span class="type">const</span> ll inf = <span class="number">1ll</span> &lt;&lt; <span class="number">60</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> n, m;</span><br><span class="line">vector&lt;<span class="type">int</span>&gt; son[N];</span><br><span class="line">vector&lt;array&lt;<span class="type">int</span>, 3&gt;&gt; path[N];</span><br><span class="line"><span class="type">int</span> father[N];</span><br><span class="line"><span class="type">int</span> dep[N];</span><br><span class="line"></span><br><span class="line">ll dp[N], sdp[N];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> u)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> v : son[u]) &#123;</span><br><span class="line">        <span class="built_in">dfs</span>(v);</span><br><span class="line">        sdp[u] += dp[v];</span><br><span class="line">    &#125;</span><br><span class="line">    dp[u] = sdp[u];</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> p : path[u]) &#123;</span><br><span class="line">        ll tmp = sdp[u];</span><br><span class="line">        <span class="type">int</span> x = p[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">while</span> (x != u) &#123;</span><br><span class="line">            tmp += sdp[x] - dp[x];</span><br><span class="line">            x = father[x];</span><br><span class="line">        &#125;</span><br><span class="line">        x = p[<span class="number">1</span>];</span><br><span class="line">        <span class="keyword">while</span> (x != u) &#123;</span><br><span class="line">            tmp += sdp[x] - dp[x];</span><br><span class="line">            x = father[x];</span><br><span class="line">        &#125;</span><br><span class="line">        tmp += p[<span class="number">2</span>];</span><br><span class="line">        dp[u] = <span class="built_in">max</span>(dp[u], tmp);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; m;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">2</span>; i &lt;= n; i++) &#123;</span><br><span class="line">        cin &gt;&gt; father[i];</span><br><span class="line">        son[father[i]].<span class="built_in">push_back</span>(i);</span><br><span class="line">        dep[i] = dep[father[i]] + <span class="number">1</span>; <span class="comment">// 因为输入是按照顺序的，这里这样也行</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= m; i++) &#123;</span><br><span class="line">        <span class="type">int</span> u, v, a;</span><br><span class="line">        cin &gt;&gt; u &gt;&gt; v &gt;&gt; a;</span><br><span class="line">        <span class="type">int</span> x = u, y = v;</span><br><span class="line">        <span class="keyword">while</span> (x != y) &#123;</span><br><span class="line">            <span class="keyword">if</span> (dep[x] &gt; dep[y]) x = father[x];</span><br><span class="line">            <span class="keyword">else</span> y = father[y];</span><br><span class="line">        &#125;</span><br><span class="line">        path[x].<span class="built_in">push_back</span>(&#123;u, v, a&#125;);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">dfs</span>(<span class="number">1</span>);</span><br><span class="line">    cout &lt;&lt; dp[<span class="number">1</span>];</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="P2-1"><a href="#P2-1" class="headerlink" title="P2"></a>P2</h3><p>给定一个n个点的有根树。给出m条树上的简单路径，每个路径有一个权值。保证每个路径都是从一个点到它的祖先。要求选择一些路径，使得每个点至少在一条路径上，并且路径的权值和最小。</p>
<p>令$dp[i][j]$表示i这棵子树内所有的点都在某条路径上，且通过i点的路径最高向上延申到深度为j的地方。$dp[i][min(j_1, j_2, …, j_n)] = dp[c_1][j_1] + dp[c_2][j_2] + … + dp[c_n][j_n]$。</p>
]]></content>
      <tags>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>zram代码阅读</title>
    <url>/2023/03/26/zram%E4%BB%A3%E7%A0%81%E9%98%85%E8%AF%BB/</url>
    <content><![CDATA[<center>zram</center>

<span id="more"></span>
<h1 id="斌斌陪你读之——zram"><a href="#斌斌陪你读之——zram" class="headerlink" title="斌斌陪你读之——zram"></a>斌斌陪你读之——zram</h1><h2 id="zram是啥"><a href="#zram是啥" class="headerlink" title="zram是啥"></a>zram是啥</h2><p>首先，zram是啥，zram是linux使用内存抽象出来的一个块设备。zram常用作交换设备使用，类似于一个ssd。zram会对写入其中的页进行压缩处理，从而起到节省内存同时提高程序启动速率的效果。对于没有交换设备或者磁盘读写次数有限的情况，zram可以起到比较好的加速效果。<a href="https://en.wikipedia.org/wiki/Zram">(zram wiki)</a>。</p>
<h2 id="zram大体框架"><a href="#zram大体框架" class="headerlink" title="zram大体框架"></a>zram大体框架</h2><p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202303261654044.jpg" alt=""></p>
<p>如上图所示(不太会画图，权当没有图)，zram向外部呈现一个block device的抽象，向外界提供块设备的接口；内部，由于zram使用的是物理内存，需要一个内存分配器，这里zram使用zsmalloc内存分配器；而zram则负责对用户向其写入的数据进行压缩处理，将压缩后的数据存储到zsmalloc分配的内存中。其实压缩这一部分也不是zram做的，压缩主要通过zcomp部分来进行，zram只是调用了zcomp部分的一些函数。感觉zram主要就是写了一个驱动，然后其他的功能大部分是用的linux中现成的代码做的。</p>
<p>在CSDN上找到了一篇关于zram的写的比较好的博客，可以先看看有一个较为宏观的了解：<br><a href="https://blog.csdn.net/qq_38135221/article/details/127042461?ops_request_misc=%257B%2522request%255Fid%2522%253A%2522168014709216800182793439%2522%252C%2522scm%2522%253A%252220140713.130102334..%2522%257D&amp;request_id=168014709216800182793439&amp;biz_id=0&amp;utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-2-127042461-null-null.142^v77^control,201^v4^add_ask,239^v2^insert_chatgpt&amp;utm_term=disksize_store&amp;spm=1018.2226.3001.4187">zRAM内存压缩技术分析及优化方向</a></p>
<h2 id="代码阅读"><a href="#代码阅读" class="headerlink" title="代码阅读"></a>代码阅读</h2><p>下面，对linux中zram部分的源码进行大致地阅读。内核版本为<strong>linux6.1</strong>。</p>
<h3 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h3><h4 id="zram结构体-zram类"><a href="#zram结构体-zram类" class="headerlink" title="zram结构体(zram类)"></a>zram结构体(zram类)</h4><p>下方代码为zram类的实现，对其中的成员变量的含义进行解释。</p>
<ul>
<li>table: 是一个zram_table_entry数组，数组的每一项对应了zram中的一页，存储了该页的元数据信息。</li>
<li>mem_pool: 前文提到zram使用zsmalloc进行内存的分配和管理。这个mem_pool就相当于zsmalloc给zram分配的一个内存池，zram可以向其读写数据。</li>
<li>comp: 这个是zram的压缩模块，zram对数据压缩和解压时通过comp的一些函数进行。</li>
<li>disk: linux内核中，用gendisk结构体表示一个磁盘设备或分区。其中存储了一些关于zram的信息，如设备号啥的，同时disk还指向了zram处理I/O的一些API，这个就相当于zram对外部提供的一个块设备抽象。<a href="https://blog.csdn.net/wlli1985/article/details/83065898">一个参考blog</a></li>
<li>init_lock: 一个读写锁，就是可以有很多人同时读，但只能有一个人写的那种锁。</li>
<li>limit_pages: 这个就是zram这个块设备最多有几页，也是table数组的最大长度。</li>
<li>stats: statistics的缩写？这个变量存储了zram执行过程中的一些统计信息。</li>
<li>disksize: 整个zram磁盘有多大，字节为单位。</li>
<li>comperssor: 压缩算法的名字。</li>
<li><font color="red">之后的后边再写，别忘了</font>

</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">zram</span> &#123;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">zram_table_entry</span> *<span class="title">table</span>;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">zs_pool</span> *<span class="title">mem_pool</span>;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">zcomp</span> *<span class="title">comp</span>;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">gendisk</span> *<span class="title">disk</span>;</span></span><br><span class="line">	<span class="comment">/* Prevent concurrent execution of device init */</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">rw_semaphore</span> <span class="title">init_lock</span>;</span></span><br><span class="line">	<span class="comment">/*</span></span><br><span class="line"><span class="comment">	 * the number of pages zram can consume for storing compressed data</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="type">unsigned</span> <span class="type">long</span> limit_pages;</span><br><span class="line"></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">zram_stats</span> <span class="title">stats</span>;</span></span><br><span class="line">	<span class="comment">/*</span></span><br><span class="line"><span class="comment">	 * This is the limit on amount of *uncompressed* worth of data</span></span><br><span class="line"><span class="comment">	 * we can store in a disk.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	u64 disksize;	<span class="comment">/* bytes */</span></span><br><span class="line">	<span class="type">char</span> compressor[CRYPTO_MAX_ALG_NAME];</span><br><span class="line">	<span class="comment">/*</span></span><br><span class="line"><span class="comment">	 * zram is claimed so open request will be failed</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="type">bool</span> claim; <span class="comment">/* Protected by disk-&gt;open_mutex */</span></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_ZRAM_WRITEBACK</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">file</span> *<span class="title">backing_dev</span>;</span></span><br><span class="line">	<span class="type">spinlock_t</span> wb_limit_lock;</span><br><span class="line">	<span class="type">bool</span> wb_limit_enable;</span><br><span class="line">	u64 bd_wb_limit;</span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">block_device</span> *<span class="title">bdev</span>;</span></span><br><span class="line">	<span class="type">unsigned</span> <span class="type">long</span> *bitmap;</span><br><span class="line">	<span class="type">unsigned</span> <span class="type">long</span> nr_pages;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_ZRAM_MEMORY_TRACKING</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">dentry</span> *<span class="title">debugfs_dir</span>;</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h4 id="zram-table-entry结构体"><a href="#zram-table-entry结构体" class="headerlink" title="zram_table_entry结构体"></a>zram_table_entry结构体</h4><p>描述了zram中一个页的元数据。</p>
<ul>
<li>handle: 当前页在mem_pool中的句柄。注意，这个handle不指向具体的地址，而是在需要的时候调用一个函数，将handle对应的内容映射到一个地址处，数据访问完之后再撤销这个映射。</li>
<li>element: 如果当前页被相同的元素填充，element存储这个元素的值。此外，如果zram中的一个页被写回到了磁盘中，element会存储该页在磁盘中的句柄。</li>
<li>flags: zram为了节省内存空间，这个变量的低位存储当前页中存储的对象的大小，高位存储当前页的状态。</li>
<li>ac_time: 不知道干嘛的，看注释像是track计时用的。</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">zram_table_entry</span> &#123;</span></span><br><span class="line">	<span class="class"><span class="keyword">union</span> &#123;</span></span><br><span class="line">		<span class="type">unsigned</span> <span class="type">long</span> handle;</span><br><span class="line">		<span class="type">unsigned</span> <span class="type">long</span> element;</span><br><span class="line">	&#125;;</span><br><span class="line">	<span class="type">unsigned</span> <span class="type">long</span> flags;</span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_ZRAM_MEMORY_TRACKING</span></span><br><span class="line">	<span class="type">ktime_t</span> ac_time;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h4 id="zram-pageflags枚举"><a href="#zram-pageflags枚举" class="headerlink" title="zram_pageflags枚举"></a>zram_pageflags枚举</h4><p>这个枚举就是上述zram_table_entry结构体中flags变量高位的含义，注释给的比较清楚了，这里不做过多说明。其中ZRAM_LOCK这一位比较有意思，这一位充当了一个自旋锁的作用，对该页加锁时将该位置1，解锁时将该位清零。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> ZRAM_FLAG_SHIFT (PAGE_SHIFT + 1)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* Flags for zram pages (table[page_no].flags) */</span></span><br><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">zram_pageflags</span> &#123;</span></span><br><span class="line">	<span class="comment">/* zram slot is locked */</span></span><br><span class="line">	ZRAM_LOCK = ZRAM_FLAG_SHIFT,</span><br><span class="line">	ZRAM_SAME,	<span class="comment">/* Page consists the same element */</span></span><br><span class="line">	ZRAM_WB,	<span class="comment">/* page is stored on backing_device */</span></span><br><span class="line">	ZRAM_UNDER_WB,	<span class="comment">/* page is under writeback */</span></span><br><span class="line">	ZRAM_HUGE,	<span class="comment">/* Incompressible page */</span></span><br><span class="line">	ZRAM_IDLE,	<span class="comment">/* not accessed page since last idle marking */</span></span><br><span class="line"></span><br><span class="line">	__NR_ZRAM_PAGEFLAGS,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h4 id="zram-stats结构体"><a href="#zram-stats结构体" class="headerlink" title="zram_stats结构体"></a>zram_stats结构体</h4><p>这个结构体存储了zram运行过程中的一些统计信息，注释给的也比较清楚。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">zram_stats</span> &#123;</span></span><br><span class="line">	<span class="type">atomic64_t</span> compr_data_size;	<span class="comment">/* compressed size of pages stored */</span></span><br><span class="line">	<span class="type">atomic64_t</span> num_reads;	<span class="comment">/* failed + successful */</span></span><br><span class="line">	<span class="type">atomic64_t</span> num_writes;	<span class="comment">/* --do-- */</span></span><br><span class="line">	<span class="type">atomic64_t</span> failed_reads;	<span class="comment">/* can happen when memory is too low */</span></span><br><span class="line">	<span class="type">atomic64_t</span> failed_writes;	<span class="comment">/* can happen when memory is too low */</span></span><br><span class="line">	<span class="type">atomic64_t</span> invalid_io;	<span class="comment">/* non-page-aligned I/O requests */</span></span><br><span class="line">	<span class="type">atomic64_t</span> notify_free;	<span class="comment">/* no. of swap slot free notifications */</span></span><br><span class="line">	<span class="type">atomic64_t</span> same_pages;		<span class="comment">/* no. of same element filled pages */</span></span><br><span class="line">	<span class="type">atomic64_t</span> huge_pages;		<span class="comment">/* no. of huge pages */</span></span><br><span class="line">	<span class="type">atomic64_t</span> pages_stored;	<span class="comment">/* no. of pages currently stored */</span></span><br><span class="line">	<span class="type">atomic_long_t</span> max_used_pages;	<span class="comment">/* no. of maximum pages stored */</span></span><br><span class="line">	<span class="type">atomic64_t</span> writestall;		<span class="comment">/* no. of write slow paths */</span></span><br><span class="line">	<span class="type">atomic64_t</span> miss_free;		<span class="comment">/* no. of missed free */</span></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span>	CONFIG_ZRAM_WRITEBACK</span></span><br><span class="line">	<span class="type">atomic64_t</span> bd_count;		<span class="comment">/* no. of pages in backing device */</span></span><br><span class="line">	<span class="type">atomic64_t</span> bd_reads;		<span class="comment">/* no. of reads from backing device */</span></span><br><span class="line">	<span class="type">atomic64_t</span> bd_writes;		<span class="comment">/* no. of writes from backing device */</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h4 id="一些宏"><a href="#一些宏" class="headerlink" title="一些宏"></a>一些宏</h4><ul>
<li>PAGE_SHIFT: 一个页几位，12位。</li>
<li>SECTOR_SHIFT: 一个扇区几位，linux中好像都是9，即一个扇区512字节。</li>
<li>SECTORS_PER_PAGE_SHIFT: 一页中的扇区号占几位，12-9=3，即一个页中有8个扇区。</li>
<li>SECTORS_PER_PAGE: 一个页中的扇区数。</li>
<li>ZRAM_LOGICAL_BLOCK_SHIFT: zram逻辑块占几位，这里是12，一页。</li>
<li>ZRAM_LOGICAL_BLOCK_SIZE: zram逻辑块大小，4K。</li>
<li>ZRAM_SECTOR_PER_LOGICAL_BLOCK: zram中每个逻辑块中有几个扇区，8个。</li>
</ul>
<p>这里涉及到了扇区和块的一些定义，网上的解释挺多的。<a href="https://blog.csdn.net/aspirinvagrant/article/details/11365209?spm=1001.2101.3001.6661.1&amp;utm_medium=distribute.pc_relevant_t0.none-task-blog-2%7Edefault%7ECTRLIST%7ERate-1-11365209-blog-79369698.235%5Ev27%5Epc_relevant_landingrelevant&amp;depth_1-utm_source=distribute.pc_relevant_t0.none-task-blog-2%7Edefault%7ECTRLIST%7ERate-1-11365209-blog-79369698.235%5Ev27%5Epc_relevant_landingrelevant&amp;utm_relevant_index=1">比如这个</a>，请自行查阅。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> SECTORS_PER_PAGE_SHIFT	(PAGE_SHIFT - SECTOR_SHIFT)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SECTORS_PER_PAGE	(1 &lt;&lt; SECTORS_PER_PAGE_SHIFT)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> ZRAM_LOGICAL_BLOCK_SHIFT 12</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> ZRAM_LOGICAL_BLOCK_SIZE	(1 &lt;&lt; ZRAM_LOGICAL_BLOCK_SHIFT)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> ZRAM_SECTOR_PER_LOGICAL_BLOCK	\</span></span><br><span class="line"><span class="meta">	(1 &lt;&lt; (ZRAM_LOGICAL_BLOCK_SHIFT - SECTOR_SHIFT))</span></span><br></pre></td></tr></table></figure>
<h3 id="具体代码"><a href="#具体代码" class="headerlink" title="具体代码"></a>具体代码</h3><p>看具体代码之前可以先看看linux驱动程序怎么编写的，以及一些关于设备I/O方面的知识。这里有一些参考网站:</p>
<ul>
<li><a href="https://blog.csdn.net/liangzc1124/article/details/129329309?spm=1001.2101.3001.6650.1&amp;utm_medium=distribute.pc_relevant.none-task-blog-2%7Edefault%7EAD_ESQUERY%7Eyljh-1-129329309-blog-122869046.235%5Ev27%5Epc_relevant_landingrelevant&amp;depth_1-utm_source=distribute.pc_relevant.none-task-blog-2%7Edefault%7EAD_ESQUERY%7Eyljh-1-129329309-blog-122869046.235%5Ev27%5Epc_relevant_landingrelevant&amp;utm_relevant_index=2">超长文解析Linux块设备驱动编写方法</a></li>
<li><a href="https://blog.csdn.net/a154299/category_10386557.html">如何编写一个简单的Linux驱动</a></li>
<li><a href="https://aliez22.github.io/posts/11537/">通用块层 struct bio 详解</a></li>
<li><a href="http://m.blog.chinaunix.net/uid-28449808-id-5820139.html">块设备剖析之关键数据结构分析</a></li>
<li><a href="https://www.bilibili.com/read/cv17063262?from=search">Linux内核之块设备驱动</a></li>
<li><a href="https://gamedebug.github.io/CPU_hotplug_in_the_Kernel/">内核中的CPU热插拔</a></li>
<li><a href="https://blog.51cto.com/u_13267193/5371148">Linux中的IDR机制</a></li>
</ul>
<h4 id="设备初始化"><a href="#设备初始化" class="headerlink" title="设备初始化"></a>设备初始化</h4><h5 id="zram-init"><a href="#zram-init" class="headerlink" title="zram_init"></a>zram_init</h5><p>初始化函数为zram_init函数。函数首先调用cpuhp_setup_state_multi函数设置了一下cpu热插拔的回调函数(cpu热插拔我还没怎么看)。然后调用class_register注册一个zram_control_class类(这个类好像是用于文件系统相关的，还没怎么看)。然后调用register_blkdev注册了一个块设备，叫做”zram”。虽然注册好了，但是实际上管理zram的数据结构还都没咋建立起来，然后继续调用zram_add来分配和初始化实际的zram设备。如果上述过程中哪一步出错了，就会转去执行destory_devices函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">int</span> __init <span class="title function_">zram_init</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">	<span class="type">int</span> ret;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 这个是防止zram中的flag的高几位溢出</span></span><br><span class="line">	BUILD_BUG_ON(__NR_ZRAM_PAGEFLAGS &gt; BITS_PER_LONG);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 设置cpu热插拔回调函数</span></span><br><span class="line">	ret = cpuhp_setup_state_multi(CPUHP_ZCOMP_PREPARE, <span class="string">&quot;block/zram:prepare&quot;</span>,</span><br><span class="line">				      zcomp_cpu_up_prepare, zcomp_cpu_dead);</span><br><span class="line">	<span class="keyword">if</span> (ret &lt; <span class="number">0</span>)</span><br><span class="line">		<span class="keyword">return</span> ret;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 注册一个类供上层文件系统和用户使用</span></span><br><span class="line">	ret = class_register(&amp;zram_control_class);</span><br><span class="line">	<span class="keyword">if</span> (ret) &#123;</span><br><span class="line">		pr_err(<span class="string">&quot;Unable to register zram-control class\n&quot;</span>);</span><br><span class="line">		cpuhp_remove_multi_state(CPUHP_ZCOMP_PREPARE);</span><br><span class="line">		<span class="keyword">return</span> ret;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 应该是调试相关的函数，估计作用不是很关键</span></span><br><span class="line">	zram_debugfs_create();</span><br><span class="line">	<span class="comment">// 在内核中注册设备</span></span><br><span class="line">	zram_major = register_blkdev(<span class="number">0</span>, <span class="string">&quot;zram&quot;</span>);</span><br><span class="line">	<span class="keyword">if</span> (zram_major &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">		pr_err(<span class="string">&quot;Unable to get major number\n&quot;</span>);</span><br><span class="line">		class_unregister(&amp;zram_control_class);</span><br><span class="line">		cpuhp_remove_multi_state(CPUHP_ZCOMP_PREPARE);</span><br><span class="line">		<span class="keyword">return</span> -EBUSY;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 这里num_devices是1</span></span><br><span class="line">	<span class="keyword">while</span> (num_devices != <span class="number">0</span>) &#123;</span><br><span class="line">		mutex_lock(&amp;zram_index_mutex);</span><br><span class="line">		<span class="comment">// 分配和初始化zram设备</span></span><br><span class="line">		ret = zram_add();</span><br><span class="line">		mutex_unlock(&amp;zram_index_mutex);</span><br><span class="line">		<span class="keyword">if</span> (ret &lt; <span class="number">0</span>)</span><br><span class="line">			<span class="keyword">goto</span> out_error;</span><br><span class="line">		num_devices--;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">out_error:</span><br><span class="line">	<span class="comment">// 初始化出错了，以及模块结束的时候都会调用这个函数</span></span><br><span class="line">	destroy_devices();</span><br><span class="line">	<span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="zcomp-cpu"><a href="#zcomp-cpu" class="headerlink" title="zcomp_cpu_*"></a>zcomp_cpu_*</h5><p>接下来我们看看上述代码中设置的cpu热插拔的两个回调函数zcomp_cpu_up_prepare和zcomp_cpu_dead。我目前理解的是前一个函数是cpu上线时执行的函数，后一个函数是cpu下线时执行的函数。这俩逻辑都比较简单，直接给注释了。注意其中的压缩流，压缩流需要一个压缩缓冲区和一个执行压缩的实体，创建压缩流和释放压缩流其实就是操作这两个东西。这个压缩流就是用来压缩和解压页面的东西。这里有个问题，就是为啥要从一个链表中取comp嘞，这个链表节点啥时候加进去的呢？可以看看zcomp_init函数，比较简单，这里略过。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">zcomp_cpu_up_prepare</span><span class="params">(<span class="type">unsigned</span> <span class="type">int</span> cpu, <span class="keyword">struct</span> hlist_node *node)</span></span><br><span class="line">&#123;</span><br><span class="line">	<span class="comment">// 获得zcomp结构体</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">zcomp</span> *<span class="title">comp</span> =</span> hlist_entry(node, <span class="keyword">struct</span> zcomp, node);</span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">zcomp_strm</span> *<span class="title">zstrm</span>;</span></span><br><span class="line">	<span class="type">int</span> ret;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// ?这里不需要判断zstrm是否为NULL吗</span></span><br><span class="line">	zstrm = per_cpu_ptr(comp-&gt;stream, cpu);</span><br><span class="line">	<span class="comment">// 初始化一个锁，这个锁用于保护zstream中的数据结构</span></span><br><span class="line">	local_lock_init(&amp;zstrm-&gt;lock);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 初始化压缩流，就是分配一个压缩算法和两个页大小的压缩缓冲区(因为可能压缩之后内容反而超过一页，这里分配两页大小)</span></span><br><span class="line">	ret = zcomp_strm_init(zstrm, comp);</span><br><span class="line">	<span class="keyword">if</span> (ret)</span><br><span class="line">		pr_err(<span class="string">&quot;Can&#x27;t allocate a compression stream\n&quot;</span>);</span><br><span class="line">	<span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">zcomp_cpu_dead</span><span class="params">(<span class="type">unsigned</span> <span class="type">int</span> cpu, <span class="keyword">struct</span> hlist_node *node)</span></span><br><span class="line">&#123;</span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">zcomp</span> *<span class="title">comp</span> =</span> hlist_entry(node, <span class="keyword">struct</span> zcomp, node);</span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">zcomp_strm</span> *<span class="title">zstrm</span>;</span></span><br><span class="line"></span><br><span class="line">	zstrm = per_cpu_ptr(comp-&gt;stream, cpu);</span><br><span class="line">	<span class="comment">// 把压缩流的数据结构都释放了，但是这里没有把comp-&gt;stream设置成NULL可能就是上边不用判断zstrm是否为NULL的原因，因为一直都不是NULL，我们只是对zstrm里面的数据结构进行创建和释放</span></span><br><span class="line">	zcomp_strm_free(zstrm);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="zram-add"><a href="#zram-add" class="headerlink" title="zram_add"></a>zram_add</h5><p>接下来看添加zram设备的zram_add函数，这个函数主要做的就是创建zram、gendisk结构体，然后对这些结构体中的参数进行初始化。代码首先创建了一个zram结构体，然后调用idr_alloc函数将zram结构体和一个整数关联起来，这个整数就作为该zram设备的设备号。之后初始化了一下zram中的锁。然后创建gendisk结构体，这个结构体就相当于一个磁盘类的对象，里面存储了磁盘的信息以及磁盘的操作。gendisk的major为主设备号，first_minor为第一个次设备号，minors为分区个数，可以看到这里minors为1，同时将flag设为GENHD_FL_NO_PART表示不支持分区，所以一个zram应该只有一个分区。然后比较重要的是disk-&gt;fops变量，这个fops里含有这个zram磁盘对外的操作接口。它是一个block_device_operations类型的指针，其中zram_devops是文件中定义好的一个block_device_operations结构体，里面是一些zram处理IO的函数。接下来我们还需要设置disk的请求队列的一些参数，对应的就是那一堆blk_queue*函数，每个函数具体含义网上也比较多。初始化完数据结构之后，最后调用device_add_disk函数把zram-&gt;disk注册到通用块层。然后差不多就结束了。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">int</span> <span class="title function_">zram_add</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">zram</span> *<span class="title">zram</span>;</span></span><br><span class="line">	<span class="type">int</span> ret, device_id;</span><br><span class="line"></span><br><span class="line">	zram = kzalloc(<span class="keyword">sizeof</span>(<span class="keyword">struct</span> zram), GFP_KERNEL);</span><br><span class="line">	<span class="keyword">if</span> (!zram)</span><br><span class="line">		<span class="keyword">return</span> -ENOMEM;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 将zram和一个整数device_id关联</span></span><br><span class="line">	ret = idr_alloc(&amp;zram_index_idr, zram, <span class="number">0</span>, <span class="number">0</span>, GFP_KERNEL);</span><br><span class="line">	<span class="keyword">if</span> (ret &lt; <span class="number">0</span>)</span><br><span class="line">		<span class="keyword">goto</span> out_free_dev;</span><br><span class="line">	device_id = ret;</span><br><span class="line"></span><br><span class="line">	init_rwsem(&amp;zram-&gt;init_lock);</span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_ZRAM_WRITEBACK</span></span><br><span class="line">	spin_lock_init(&amp;zram-&gt;wb_limit_lock);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">	<span class="comment">/* gendisk structure */</span></span><br><span class="line">	zram-&gt;disk = blk_alloc_disk(NUMA_NO_NODE);</span><br><span class="line">	<span class="keyword">if</span> (!zram-&gt;disk) &#123;</span><br><span class="line">		pr_err(<span class="string">&quot;Error allocating disk structure for device %d\n&quot;</span>,</span><br><span class="line">			device_id);</span><br><span class="line">		ret = -ENOMEM;</span><br><span class="line">		<span class="keyword">goto</span> out_free_idr;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	zram-&gt;disk-&gt;major = zram_major;</span><br><span class="line">	zram-&gt;disk-&gt;first_minor = device_id;</span><br><span class="line">	zram-&gt;disk-&gt;minors = <span class="number">1</span>;</span><br><span class="line">	zram-&gt;disk-&gt;flags |= GENHD_FL_NO_PART;</span><br><span class="line">	zram-&gt;disk-&gt;fops = &amp;zram_devops;</span><br><span class="line">	zram-&gt;disk-&gt;private_data = zram;</span><br><span class="line">	<span class="built_in">snprintf</span>(zram-&gt;disk-&gt;disk_name, <span class="number">16</span>, <span class="string">&quot;zram%d&quot;</span>, device_id);</span><br><span class="line"></span><br><span class="line">	<span class="comment">/* Actual capacity set using syfs (/sys/block/zram&lt;id&gt;/disksize */</span></span><br><span class="line">	set_capacity(zram-&gt;disk, <span class="number">0</span>);</span><br><span class="line">	<span class="comment">/* zram devices sort of resembles non-rotational disks */</span></span><br><span class="line">	<span class="comment">// 这两句表示zram像是一个ssd</span></span><br><span class="line">	blk_queue_flag_set(QUEUE_FLAG_NONROT, zram-&gt;disk-&gt;<span class="built_in">queue</span>);</span><br><span class="line">	blk_queue_flag_clear(QUEUE_FLAG_ADD_RANDOM, zram-&gt;disk-&gt;<span class="built_in">queue</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">/*</span></span><br><span class="line"><span class="comment">	 * To ensure that we always get PAGE_SIZE aligned</span></span><br><span class="line"><span class="comment">	 * and n*PAGE_SIZED sized I/O requests.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	blk_queue_physical_block_size(zram-&gt;disk-&gt;<span class="built_in">queue</span>, PAGE_SIZE);</span><br><span class="line">	blk_queue_logical_block_size(zram-&gt;disk-&gt;<span class="built_in">queue</span>,</span><br><span class="line">					ZRAM_LOGICAL_BLOCK_SIZE);</span><br><span class="line">	blk_queue_io_min(zram-&gt;disk-&gt;<span class="built_in">queue</span>, PAGE_SIZE);</span><br><span class="line">	blk_queue_io_opt(zram-&gt;disk-&gt;<span class="built_in">queue</span>, PAGE_SIZE);</span><br><span class="line">	zram-&gt;disk-&gt;<span class="built_in">queue</span>-&gt;limits.discard_granularity = PAGE_SIZE;</span><br><span class="line">	blk_queue_max_discard_sectors(zram-&gt;disk-&gt;<span class="built_in">queue</span>, UINT_MAX);</span><br><span class="line"></span><br><span class="line">	<span class="comment">/*</span></span><br><span class="line"><span class="comment">	 * zram_bio_discard() will clear all logical blocks if logical block</span></span><br><span class="line"><span class="comment">	 * size is identical with physical block size(PAGE_SIZE). But if it is</span></span><br><span class="line"><span class="comment">	 * different, we will skip discarding some parts of logical blocks in</span></span><br><span class="line"><span class="comment">	 * the part of the request range which isn&#x27;t aligned to physical block</span></span><br><span class="line"><span class="comment">	 * size.  So we can&#x27;t ensure that all discarded logical blocks are</span></span><br><span class="line"><span class="comment">	 * zeroed.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">if</span> (ZRAM_LOGICAL_BLOCK_SIZE == PAGE_SIZE)</span><br><span class="line">		blk_queue_max_write_zeroes_sectors(zram-&gt;disk-&gt;<span class="built_in">queue</span>, UINT_MAX);</span><br><span class="line"></span><br><span class="line">	blk_queue_flag_set(QUEUE_FLAG_STABLE_WRITES, zram-&gt;disk-&gt;<span class="built_in">queue</span>);</span><br><span class="line">	ret = device_add_disk(<span class="literal">NULL</span>, zram-&gt;disk, zram_disk_groups);</span><br><span class="line">	<span class="keyword">if</span> (ret)</span><br><span class="line">		<span class="keyword">goto</span> out_cleanup_disk;</span><br><span class="line"></span><br><span class="line">	strscpy(zram-&gt;compressor, default_compressor, <span class="keyword">sizeof</span>(zram-&gt;compressor));</span><br><span class="line"></span><br><span class="line">	zram_debugfs_register(zram);</span><br><span class="line">	pr_info(<span class="string">&quot;Added device: %s\n&quot;</span>, zram-&gt;disk-&gt;disk_name);</span><br><span class="line">	<span class="keyword">return</span> device_id;</span><br><span class="line"></span><br><span class="line">out_cleanup_disk:</span><br><span class="line">	put_disk(zram-&gt;disk);</span><br><span class="line">out_free_idr:</span><br><span class="line">	idr_remove(&amp;zram_index_idr, device_id);</span><br><span class="line">out_free_dev:</span><br><span class="line">	kfree(zram);</span><br><span class="line">	<span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="disksize-store"><a href="#disksize-store" class="headerlink" title="disksize_store"></a>disksize_store</h5><p>前几部分相当于在内核中添加了一个zram设备驱动，最终通过zram_add函数完成了zram设备的添加，但是，在阅读上述代码的时候，我们发现，zram结构体中的一些变量如table、mem_pool、comp好像都还没有被初始化。创建zram驱动后，还需要对zram设备的大小进行配置，对应disksize_store函数。这个函数的逻辑也比较简单，首先调用memparse函数对buf进行解析，得到zram的实际大小。之后调用zram_meta_alloc函数创建zram中的table和mem_pool。然后调用zcomp_create函数创建一个压缩对象用于zram执行压缩操作。最后再给对应的变量赋一下值就行了。其中调用的zram_meta_alloc和zcomp_create都比较简单，这里先略过了，如有必要，之后会补充。这里有个问题，就是为啥要从dev里找zram呢？建议看看这个：<a href="http://m.blog.chinaunix.net/uid-28449808-id-5820139.html">块设备剖析之关键数据结构分析</a>。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">ssize_t</span> <span class="title function_">disksize_store</span><span class="params">(<span class="keyword">struct</span> device *dev,</span></span><br><span class="line"><span class="params">		<span class="keyword">struct</span> device_attribute *attr, <span class="type">const</span> <span class="type">char</span> *buf, <span class="type">size_t</span> len)</span></span><br><span class="line">&#123;</span><br><span class="line">	u64 disksize;</span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">zcomp</span> *<span class="title">comp</span>;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">zram</span> *<span class="title">zram</span> =</span> dev_to_zram(dev);</span><br><span class="line">	<span class="type">int</span> err;</span><br><span class="line"></span><br><span class="line">	disksize = memparse(buf, <span class="literal">NULL</span>);</span><br><span class="line">	<span class="keyword">if</span> (!disksize)</span><br><span class="line">		<span class="keyword">return</span> -EINVAL;</span><br><span class="line"></span><br><span class="line">	down_write(&amp;zram-&gt;init_lock);</span><br><span class="line">	<span class="keyword">if</span> (init_done(zram)) &#123;</span><br><span class="line">		pr_info(<span class="string">&quot;Cannot change disksize for initialized device\n&quot;</span>);</span><br><span class="line">		err = -EBUSY;</span><br><span class="line">		<span class="keyword">goto</span> out_unlock;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	disksize = PAGE_ALIGN(disksize);</span><br><span class="line">	<span class="keyword">if</span> (!zram_meta_alloc(zram, disksize)) &#123;</span><br><span class="line">		err = -ENOMEM;</span><br><span class="line">		<span class="keyword">goto</span> out_unlock;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	comp = zcomp_create(zram-&gt;compressor);</span><br><span class="line">	<span class="keyword">if</span> (IS_ERR(comp)) &#123;</span><br><span class="line">		pr_err(<span class="string">&quot;Cannot initialise %s compressing backend\n&quot;</span>,</span><br><span class="line">				zram-&gt;compressor);</span><br><span class="line">		err = PTR_ERR(comp);</span><br><span class="line">		<span class="keyword">goto</span> out_free_meta;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	zram-&gt;comp = comp;</span><br><span class="line">	zram-&gt;disksize = disksize;</span><br><span class="line">	set_capacity_and_notify(zram-&gt;disk, zram-&gt;disksize &gt;&gt; SECTOR_SHIFT);</span><br><span class="line">	up_write(&amp;zram-&gt;init_lock);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> len;</span><br><span class="line"></span><br><span class="line">out_free_meta:</span><br><span class="line">	zram_meta_free(zram, disksize);</span><br><span class="line">out_unlock:</span><br><span class="line">	up_write(&amp;zram-&gt;init_lock);</span><br><span class="line">	<span class="keyword">return</span> err;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="zram-I-O处理"><a href="#zram-I-O处理" class="headerlink" title="zram I/O处理"></a>zram I/O处理</h4><p>看这一部分的时候建议先大概搞清楚bio这个结构体，因为内核和zram设备之间主要依靠这个bio结构体来进行IO操作。比较推荐的是这个blog：<a href="https://aliez22.github.io/posts/11537/">通用块层 struct bio 详解</a>。也建议看看啥通用块层和request_queue之类的，虽然我搞得也不太懂。大概就是通用块层是对底层设备的一个抽象层，应该就是用gendisk结构体来抽象代表具体的块设备？然后request_queue就是一个I/O请求队列，每个gendisk都有一个这个队列，os可以向这个队列里push I/O请求，队列里的I/O请求会经过合并(比如把对相邻扇区的读写操作放在一起之类的)，然后交给设备驱动处理。</p>
<h5 id="zram-submit-bio"><a href="#zram-submit-bio" class="headerlink" title="zram_submit_bio"></a>zram_submit_bio</h5><p>zram_submit_bio这个函数用于处理所有的zram的I/O请求，好像比较早的linux版本这个函数叫zram_make_request。如下所示，函数首先调用valid_io_request函数来判断当前的I/O请求是否是一个合法的I/O请求，主要就是判断一下扇区号有没有超过zram的界限之类的。如果合法的话，就会调用__zram_make_request函数来正式处理当前的这个I/O请求。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">zram_submit_bio</span><span class="params">(<span class="keyword">struct</span> bio *bio)</span></span><br><span class="line">&#123;</span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">zram</span> *<span class="title">zram</span> =</span> bio-&gt;bi_bdev-&gt;bd_disk-&gt;private_data;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!valid_io_request(zram, bio-&gt;bi_iter.bi_sector,</span><br><span class="line">					bio-&gt;bi_iter.bi_size)) &#123;</span><br><span class="line">		atomic64_inc(&amp;zram-&gt;stats.invalid_io);</span><br><span class="line">		bio_io_error(bio);</span><br><span class="line">		<span class="keyword">return</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	__zram_make_request(zram, bio);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="valid-io-request"><a href="#valid-io-request" class="headerlink" title="valid_io_request"></a>valid_io_request</h5><p>我们先来看一下valid_io_request函数吧。首先判断起始扇区是否对齐到每个逻辑块中的扇区数以及I/O传输的字节数是否对齐到逻辑块大小，注意这里的if判断中的unlikely，unlikely主要是用于提示编译器这个if判断结果很有可能是假，从而让编译器调整代码的位置以使代码运行的更高效，除此之外unlikely对表达式的具体结果没有影响，我们可以将unlikely忽略。然后判断一下I/O操作是否越界即可。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 判断io是否有效 */</span></span><br><span class="line"><span class="type">static</span> <span class="keyword">inline</span> <span class="type">bool</span> <span class="title function_">valid_io_request</span><span class="params">(<span class="keyword">struct</span> zram *zram,</span></span><br><span class="line"><span class="params">		<span class="type">sector_t</span> start, <span class="type">unsigned</span> <span class="type">int</span> size)</span></span><br><span class="line">&#123;</span><br><span class="line">	u64 end, bound;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/* unaligned request */</span></span><br><span class="line">    <span class="comment">// start应该就是I/O操作的起始扇区号，然后这里扇区号要对齐到每个块中的扇区个数</span></span><br><span class="line">	<span class="keyword">if</span> (unlikely(start &amp; (ZRAM_SECTOR_PER_LOGICAL_BLOCK - <span class="number">1</span>)))</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="comment">// size是I/O字节数，size要按照块大小对齐</span></span><br><span class="line">	<span class="keyword">if</span> (unlikely(size &amp; (ZRAM_LOGICAL_BLOCK_SIZE - <span class="number">1</span>)))</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// I/O操作终止扇区号</span></span><br><span class="line">	end = start + (size &gt;&gt; SECTOR_SHIFT);</span><br><span class="line">	bound = zram-&gt;disksize &gt;&gt; SECTOR_SHIFT;</span><br><span class="line">	<span class="comment">// 不能越界，即超过zram存储的范围</span></span><br><span class="line">	<span class="keyword">if</span> (unlikely(start &gt;= bound || end &gt; bound || start &gt; end))</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/* I/O request is valid */</span></span><br><span class="line">	<span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="zram-make-request"><a href="#zram-make-request" class="headerlink" title="__zram_make_request"></a>__zram_make_request</h5><p>如果valid_io_request判断I/O合法，驱动就会执行__zram_make_request函数来对I/O进行具体的处理。</p>
<p>接下来我们来具体看一下__zram_make_request的具体实现。首先index和offset用于定位zram磁盘中的位置，之前也讲到zram用一个table来描述每一页的元数据，这里index就是用于索引页的页号，而offset就是页内偏移。</p>
<p>考虑对I/O操作的处理方式，这里一共有三种可能的I/O操作，分别是丢弃、读和写。函数首先判断是否为丢弃discard I/O操作，如果是则调用zram_bio_discard函数进行处理。关于discard的概念可以看看这一篇blog：<a href="https://developer.aliyun.com/article/770779">REQ_OP_DISCARD</a>。这里注意，对于discard类型的bio，没有也不需要bio_vec结构体。</p>
<p>如果I/O操作不是discard操作，则代码会遍历bio中的每一个bio_vec，根据bio_vec中描述的内存中的位置和长度以及index和offset索引的zram中的位置来不断进行I/O的读或写操作，对应zram_bvec_rw函数的调用，然后更新index和offset。关于bio_vec中具体字段的值，还请看我一开始提到的博客：<a href="https://aliez22.github.io/posts/11537/">通用块层 struct bio 详解</a>。下面给出代码注释：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span> __zram_make_request(<span class="keyword">struct</span> zram *zram, <span class="keyword">struct</span> bio *bio)</span><br><span class="line">&#123;</span><br><span class="line">	<span class="comment">// 索引zram的页</span></span><br><span class="line">	<span class="type">int</span> offset;</span><br><span class="line">	<span class="comment">// zram页内偏移</span></span><br><span class="line">	u32 index;</span><br><span class="line">	<span class="comment">// 这俩用于遍历bio中的所有的bio_vec结构体</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">bio_vec</span> <span class="title">bvec</span>;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">bvec_iter</span> <span class="title">iter</span>;</span></span><br><span class="line">	<span class="comment">// 这个应该是用来统计I/O信息的，不管他</span></span><br><span class="line">	<span class="type">unsigned</span> <span class="type">long</span> start_time;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 得到zram中的页号和页内偏移</span></span><br><span class="line">	index = bio-&gt;bi_iter.bi_sector &gt;&gt; SECTORS_PER_PAGE_SHIFT;</span><br><span class="line">	offset = (bio-&gt;bi_iter.bi_sector &amp;</span><br><span class="line">		  (SECTORS_PER_PAGE - <span class="number">1</span>)) &lt;&lt; SECTOR_SHIFT;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 若bio为discard操作，调用zram_bio_discard函数进行页的clear操作</span></span><br><span class="line">	<span class="keyword">switch</span> (bio_op(bio)) &#123;</span><br><span class="line">	<span class="keyword">case</span> REQ_OP_DISCARD:</span><br><span class="line">	<span class="keyword">case</span> REQ_OP_WRITE_ZEROES:</span><br><span class="line">		zram_bio_discard(zram, index, offset, bio);</span><br><span class="line">		bio_endio(bio);</span><br><span class="line">		<span class="keyword">return</span>;</span><br><span class="line">	<span class="keyword">default</span>:</span><br><span class="line">		<span class="keyword">break</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 开始计时？不管他</span></span><br><span class="line">	start_time = bio_start_io_acct(bio);</span><br><span class="line">	<span class="comment">// 这是一个宏，作用就是循环遍历bio中的bio_vec</span></span><br><span class="line">	bio_for_each_segment(bvec, bio, iter) &#123;</span><br><span class="line">		<span class="class"><span class="keyword">struct</span> <span class="title">bio_vec</span> <span class="title">bv</span> =</span> bvec;</span><br><span class="line">		<span class="comment">// 得到当前的bio_vec中未处理的字节数</span></span><br><span class="line">		<span class="type">unsigned</span> <span class="type">int</span> unwritten = bvec.bv_len;</span><br><span class="line">		<span class="comment">// 循环处理当前bio_vec，这里主要是如果一个大的bio_vec跨越了多个zram页，</span></span><br><span class="line">		<span class="comment">// 我们就把它给分成对多个页的小的bio_vec来处理</span></span><br><span class="line">		<span class="keyword">do</span> &#123;</span><br><span class="line">			<span class="comment">// 得到当前小的bio_vec的长度</span></span><br><span class="line">			bv.bv_len = <span class="type">min_t</span>(<span class="type">unsigned</span> <span class="type">int</span>, PAGE_SIZE - offset,</span><br><span class="line">							unwritten);</span><br><span class="line">			<span class="comment">// 调用zram_bvec_rw对当前bio_vec进行处理</span></span><br><span class="line">			<span class="keyword">if</span> (zram_bvec_rw(zram, &amp;bv, index, offset,</span><br><span class="line">					 bio_op(bio), bio) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">				bio-&gt;bi_status = BLK_STS_IOERR;</span><br><span class="line">				<span class="keyword">break</span>;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="comment">// 已经处理完了一部分bio_vec，需要更改下一个bio_vec的偏移</span></span><br><span class="line">			<span class="comment">// 这里为啥不考虑bv_offset溢出回0嘞，就是模一个page_size？</span></span><br><span class="line">			<span class="comment">// 我的考虑是bv_bio表示一个段，段的大小小于页的大小，所以不用考虑溢出</span></span><br><span class="line">			bv.bv_offset += bv.bv_len;</span><br><span class="line">			<span class="comment">// 未处理字节减少</span></span><br><span class="line">			unwritten -= bv.bv_len;</span><br><span class="line"></span><br><span class="line">			<span class="comment">// 由于已经处理了bv_len的字节，这里要更新index和offset，把它们后移</span></span><br><span class="line">			update_position(&amp;index, &amp;offset, &amp;bv);</span><br><span class="line">		&#125; <span class="keyword">while</span> (unwritten);</span><br><span class="line">	&#125;</span><br><span class="line">	bio_end_io_acct(bio, start_time);</span><br><span class="line">	bio_endio(bio);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="zram-bio-discard"><a href="#zram-bio-discard" class="headerlink" title="zram_bio_discard"></a>zram_bio_discard</h5><p>接下来我们先看zram_bio_discard这个函数，这里比较有意思的一个地方是如果offset不是0，即要清空的内容的起始地址在一个zram页的中间，按理来说应该先把当前这个zram页从mem_pool中取出来，然后把相应部分清零，之后再把这个页压缩并存回mem_pool中。但是这里对于这种情况zram选择直接skip掉这一部分空间的清空，而是从下一页的开头开始清空。清除操作也比较简单，遍历每一个待清除的页，先加锁，然后调用zram_free_page，最后解锁即可。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">zram_bio_discard</span><span class="params">(<span class="keyword">struct</span> zram *zram, u32 index,</span></span><br><span class="line"><span class="params">			     <span class="type">int</span> offset, <span class="keyword">struct</span> bio *bio)</span></span><br><span class="line">&#123;</span><br><span class="line">	<span class="type">size_t</span> n = bio-&gt;bi_iter.bi_size;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/*</span></span><br><span class="line"><span class="comment">	 * zram manages data in physical block size units. Because logical block</span></span><br><span class="line"><span class="comment">	 * size isn&#x27;t identical with physical block size on some arch, we</span></span><br><span class="line"><span class="comment">	 * could get a discard request pointing to a specific offset within a</span></span><br><span class="line"><span class="comment">	 * certain physical block.  Although we can handle this request by</span></span><br><span class="line"><span class="comment">	 * reading that physiclal block and decompressing and partially zeroing</span></span><br><span class="line"><span class="comment">	 * and re-compressing and then re-storing it, this isn&#x27;t reasonable</span></span><br><span class="line"><span class="comment">	 * because our intent with a discard request is to save memory.  So</span></span><br><span class="line"><span class="comment">	 * skipping this logical block is appropriate here.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">if</span> (offset) &#123;</span><br><span class="line">		<span class="keyword">if</span> (n &lt;= (PAGE_SIZE - offset))</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">		n -= (PAGE_SIZE - offset);</span><br><span class="line">		index++;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">while</span> (n &gt;= PAGE_SIZE) &#123;</span><br><span class="line">		zram_slot_lock(zram, index);</span><br><span class="line">		zram_free_page(zram, index);</span><br><span class="line">		zram_slot_unlock(zram, index);</span><br><span class="line">		atomic64_inc(&amp;zram-&gt;stats.notify_free);</span><br><span class="line">		index++;</span><br><span class="line">		n -= PAGE_SIZE;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="zram-free-page"><a href="#zram-free-page" class="headerlink" title="zram_free_page"></a>zram_free_page</h5><p>zram_free_page函数如下，根据zram结构体中的flag变量来判断当前页的类型，并清除flag中相应的位。这里，如果该页的ZRAM_WB置位，表示当前页被写回到一个磁盘中，然后调用free_block_bdev函数来释放磁盘中的页，但是linux 6.1版本中的free_block_bdev函数是个空函数，也许是不支持zram写回到磁盘？此外，对于ZRAM_SAME置位的页，该页的内容可以从table表中直接获得，不需要再存储到mem_pool中。在清除对应位之后，从table表中获得对应页在mem_pool中的handle，然后调用zs_free函数从mem_pool中释放该页即可。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">zram_free_page</span><span class="params">(<span class="keyword">struct</span> zram *zram, <span class="type">size_t</span> index)</span></span><br><span class="line">&#123;</span><br><span class="line">	<span class="type">unsigned</span> <span class="type">long</span> handle;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> CONFIG_ZRAM_MEMORY_TRACKING</span></span><br><span class="line">	zram-&gt;table[index].ac_time = <span class="number">0</span>;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">	<span class="keyword">if</span> (zram_test_flag(zram, index, ZRAM_IDLE))</span><br><span class="line">		zram_clear_flag(zram, index, ZRAM_IDLE);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (zram_test_flag(zram, index, ZRAM_HUGE)) &#123;</span><br><span class="line">		zram_clear_flag(zram, index, ZRAM_HUGE);</span><br><span class="line">		atomic64_dec(&amp;zram-&gt;stats.huge_pages);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (zram_test_flag(zram, index, ZRAM_WB)) &#123;</span><br><span class="line">		zram_clear_flag(zram, index, ZRAM_WB);</span><br><span class="line">		free_block_bdev(zram, zram_get_element(zram, index));</span><br><span class="line">		<span class="keyword">goto</span> out;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/*</span></span><br><span class="line"><span class="comment">	 * No memory is allocated for same element filled pages.</span></span><br><span class="line"><span class="comment">	 * Simply clear same page flag.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">if</span> (zram_test_flag(zram, index, ZRAM_SAME)) &#123;</span><br><span class="line">		zram_clear_flag(zram, index, ZRAM_SAME);</span><br><span class="line">		atomic64_dec(&amp;zram-&gt;stats.same_pages);</span><br><span class="line">		<span class="keyword">goto</span> out;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 这个handle用于查找mem_pool中的对象</span></span><br><span class="line">	handle = zram_get_handle(zram, index);</span><br><span class="line">	<span class="keyword">if</span> (!handle)</span><br><span class="line">		<span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">	zs_free(zram-&gt;mem_pool, handle);</span><br><span class="line"></span><br><span class="line">	atomic64_sub(zram_get_obj_size(zram, index),</span><br><span class="line">			&amp;zram-&gt;stats.compr_data_size);</span><br><span class="line">out:</span><br><span class="line">	atomic64_dec(&amp;zram-&gt;stats.pages_stored);</span><br><span class="line">	zram_set_handle(zram, index, <span class="number">0</span>);</span><br><span class="line">	zram_set_obj_size(zram, index, <span class="number">0</span>);</span><br><span class="line">	WARN_ON_ONCE(zram-&gt;table[index].flags &amp;</span><br><span class="line">		~(<span class="number">1UL</span> &lt;&lt; ZRAM_LOCK | <span class="number">1UL</span> &lt;&lt; ZRAM_UNDER_WB));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="zram-bvec-rw"><a href="#zram-bvec-rw" class="headerlink" title="zram_bvec_rw"></a>zram_bvec_rw</h5><p>看完discard操作后，我们继续阅读zram I/O读写操作的代码，前边提到__zram_make_request函数对每一个小的bio_vec调用zram_bvec_rw函数执行I/O读写操作。zram_bvec_rw代码如下，可以看到，这个函数只是根据bio的操作类型来调用zram_bvec_read和zram_bvec_write函数。在I/O读操作后，调用了flush_dcache_page函数将读取出的页从dcache写回到内存中。在执行完读写操作后，调用zram_accessed函数将zram中被访问到的页的ZRAM_IDLE位清零。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">int</span> <span class="title function_">zram_bvec_rw</span><span class="params">(<span class="keyword">struct</span> zram *zram, <span class="keyword">struct</span> bio_vec *bvec, u32 index,</span></span><br><span class="line"><span class="params">			<span class="type">int</span> offset, <span class="keyword">enum</span> req_op op, <span class="keyword">struct</span> bio *bio)</span></span><br><span class="line">&#123;</span><br><span class="line">	<span class="type">int</span> ret;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!op_is_write(op)) &#123;</span><br><span class="line">		atomic64_inc(&amp;zram-&gt;stats.num_reads);</span><br><span class="line">		ret = zram_bvec_read(zram, bvec, index, offset, bio);</span><br><span class="line">		flush_dcache_page(bvec-&gt;bv_page);</span><br><span class="line">	&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">		atomic64_inc(&amp;zram-&gt;stats.num_writes);</span><br><span class="line">		ret = zram_bvec_write(zram, bvec, index, offset, bio);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	zram_slot_lock(zram, index);</span><br><span class="line">	zram_accessed(zram, index);</span><br><span class="line">	zram_slot_unlock(zram, index);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (unlikely(ret &lt; <span class="number">0</span>)) &#123;</span><br><span class="line">		<span class="keyword">if</span> (!op_is_write(op))</span><br><span class="line">			atomic64_inc(&amp;zram-&gt;stats.failed_reads);</span><br><span class="line">		<span class="keyword">else</span></span><br><span class="line">			atomic64_inc(&amp;zram-&gt;stats.failed_writes);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="zram-bvec-read"><a href="#zram-bvec-read" class="headerlink" title="zram_bvec_read"></a>zram_bvec_read</h5><p>zram_bvec_read函数逻辑也较为简单，首先判断当前的bio_vec是否是对一整个页的读取，如果不是，则新申请一个临时页，先调用__zram_bvec_read函数将zram中一整个页读取到该临时页，再将临时页中bio_vec需要的一部分复制到bio_vec指示的相应位置处。</p>
<p>如果是对一整个页的读取，则直接调用__zram_bvec_read函数将zram中的页读取到bio_vec对应的内存页中即可。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">int</span> <span class="title function_">zram_bvec_read</span><span class="params">(<span class="keyword">struct</span> zram *zram, <span class="keyword">struct</span> bio_vec *bvec,</span></span><br><span class="line"><span class="params">				u32 index, <span class="type">int</span> offset, <span class="keyword">struct</span> bio *bio)</span></span><br><span class="line">&#123;</span><br><span class="line">	<span class="type">int</span> ret;</span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">page</span> *<span class="title">page</span>;</span></span><br><span class="line"></span><br><span class="line">	page = bvec-&gt;bv_page;</span><br><span class="line">	<span class="keyword">if</span> (is_partial_io(bvec)) &#123;</span><br><span class="line">		<span class="comment">/* Use a temporary buffer to decompress the page */</span></span><br><span class="line">		page = alloc_page(GFP_NOIO|__GFP_HIGHMEM);</span><br><span class="line">		<span class="keyword">if</span> (!page)</span><br><span class="line">			<span class="keyword">return</span> -ENOMEM;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 从zram中读取一页到page中</span></span><br><span class="line">	ret = __zram_bvec_read(zram, page, index, bio, is_partial_io(bvec));</span><br><span class="line">	<span class="keyword">if</span> (unlikely(ret))</span><br><span class="line">		<span class="keyword">goto</span> out;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (is_partial_io(bvec)) &#123;</span><br><span class="line">		<span class="comment">// 注意这个kmap_atomic函数，作用为将一个物理页映射到内核高地址空间，</span></span><br><span class="line">		<span class="comment">// 然后内核就可以访问该物理页了</span></span><br><span class="line">		<span class="type">void</span> *src = kmap_atomic(page);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 将临时页的内容移动到目标内存页中的目标位置</span></span><br><span class="line">		memcpy_to_bvec(bvec, src + offset);</span><br><span class="line">		kunmap_atomic(src);</span><br><span class="line">	&#125;</span><br><span class="line">out:</span><br><span class="line">	<span class="keyword">if</span> (is_partial_io(bvec))</span><br><span class="line">		__free_page(page);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="zram-bvec-read-1"><a href="#zram-bvec-read-1" class="headerlink" title="__zram_bvec_read"></a>__zram_bvec_read</h5><p>I/O读操作的最后一个函数(读操作我想写的最后一个函数，代码太多了，先偷个懒)，__zram_bvec_read首先要判断要zram中存储的页是否被写回到了磁盘中，如果被写回到了磁盘中，则zram调用read_from_bdev函数直接从磁盘将页读取到内存中的目标地址处。这里read_from_bdev函数的一个参数是zram_get_element(zram, index)，这个参数就是table[index].element，代表了磁盘页的句柄。</p>
<p>如果没有被写回到磁盘，zram则判断该页的ZRAM_SAME位是否被置位，如果置位，表示这一页被相同的element填充，则zram可以很简单的从table[index].element获得页的填充内容element，使用这个element填充满一页即可。</p>
<p>最后一种情况，要读取的页存储在mem_pool中，则首先获得该页存储在mem_pool中的对象的大小，如果对象大小小于一页，表示该页被压缩过了，如果对象大小等于一页，则表示该页没有被压缩。首先调用zs_map_object函数把handle映射为一个src指针，这个指针就指向mem_pool中的一块区域，可以通过读这个指针来实现读mem_pool中的内容。然后根据上述对象大小的判断，如果大小为一页，直接将src指针处的内容copy到目标内存页即可；如果大小小于一页，则要先调用zcomp_decompress把src处的内容解压缩到目标内存页中。最后调用zs_unmap_object将handle和src指针的映射撤销掉即可。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">int</span> __zram_bvec_read(<span class="keyword">struct</span> zram *zram, <span class="keyword">struct</span> page *page, u32 index,</span><br><span class="line">				<span class="keyword">struct</span> bio *bio, <span class="type">bool</span> partial_io)</span><br><span class="line">&#123;</span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">zcomp_strm</span> *<span class="title">zstrm</span>;</span></span><br><span class="line">	<span class="type">unsigned</span> <span class="type">long</span> handle;</span><br><span class="line">	<span class="type">unsigned</span> <span class="type">int</span> size;</span><br><span class="line">	<span class="type">void</span> *src, *dst;</span><br><span class="line">	<span class="type">int</span> ret;</span><br><span class="line"></span><br><span class="line">	zram_slot_lock(zram, index);</span><br><span class="line">	<span class="keyword">if</span> (zram_test_flag(zram, index, ZRAM_WB)) &#123;</span><br><span class="line">		<span class="class"><span class="keyword">struct</span> <span class="title">bio_vec</span> <span class="title">bvec</span>;</span></span><br><span class="line"></span><br><span class="line">		zram_slot_unlock(zram, index);</span><br><span class="line">		<span class="comment">/* A null bio means rw_page was used, we must fallback to bio */</span></span><br><span class="line">		<span class="keyword">if</span> (!bio)</span><br><span class="line">			<span class="keyword">return</span> -EOPNOTSUPP;</span><br><span class="line"></span><br><span class="line">		bvec.bv_page = page;</span><br><span class="line">		bvec.bv_len = PAGE_SIZE;</span><br><span class="line">		bvec.bv_offset = <span class="number">0</span>;</span><br><span class="line">		<span class="keyword">return</span> read_from_bdev(zram, &amp;bvec,</span><br><span class="line">				zram_get_element(zram, index),</span><br><span class="line">				bio, partial_io);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	handle = zram_get_handle(zram, index);</span><br><span class="line">	<span class="keyword">if</span> (!handle || zram_test_flag(zram, index, ZRAM_SAME)) &#123;</span><br><span class="line">		<span class="type">unsigned</span> <span class="type">long</span> value;</span><br><span class="line">		<span class="type">void</span> *mem;</span><br><span class="line"></span><br><span class="line">		value = handle ? zram_get_element(zram, index) : <span class="number">0</span>;</span><br><span class="line">		mem = kmap_atomic(page);</span><br><span class="line">		zram_fill_page(mem, PAGE_SIZE, value);</span><br><span class="line">		kunmap_atomic(mem);</span><br><span class="line">		zram_slot_unlock(zram, index);</span><br><span class="line">		<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	size = zram_get_obj_size(zram, index);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (size != PAGE_SIZE)</span><br><span class="line">		zstrm = zcomp_stream_get(zram-&gt;comp);</span><br><span class="line"></span><br><span class="line">	src = zs_map_object(zram-&gt;mem_pool, handle, ZS_MM_RO);</span><br><span class="line">	<span class="keyword">if</span> (size == PAGE_SIZE) &#123;</span><br><span class="line">		dst = kmap_atomic(page);</span><br><span class="line">		<span class="built_in">memcpy</span>(dst, src, PAGE_SIZE);</span><br><span class="line">		kunmap_atomic(dst);</span><br><span class="line">		ret = <span class="number">0</span>;</span><br><span class="line">	&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">		dst = kmap_atomic(page);</span><br><span class="line">		ret = zcomp_decompress(zstrm, src, size, dst);</span><br><span class="line">		kunmap_atomic(dst);</span><br><span class="line">		zcomp_stream_put(zram-&gt;comp);</span><br><span class="line">	&#125;</span><br><span class="line">	zs_unmap_object(zram-&gt;mem_pool, handle);</span><br><span class="line">	zram_slot_unlock(zram, index);</span><br><span class="line"></span><br><span class="line">	<span class="comment">/* Should NEVER happen. Return bio error if it does. */</span></span><br><span class="line">	<span class="keyword">if</span> (WARN_ON(ret))</span><br><span class="line">		pr_err(<span class="string">&quot;Decompression failed! err=%d, page=%u\n&quot;</span>, ret, index);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>OS</tag>
      </tags>
  </entry>
  <entry>
    <title>一个有趣的bug</title>
    <url>/2023/08/04/%E4%B8%80%E4%B8%AA%E6%9C%89%E8%B6%A3%E7%9A%84bug/</url>
    <content><![CDATA[<p><center>bug集合</center><br><span id="more"></span></p>
<p>本文记录编程中遇到的比较好玩的bug。</p>
<h1 id="unsigned-char"><a href="#unsigned-char" class="headerlink" title="unsigned char?"></a>unsigned char?</h1><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">unsigned</span> <span class="type">char</span> imm = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span> (imm = <span class="number">0</span>; imm &lt;= <span class="number">255</span>; imm++)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">do_something</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>（这代码可以当大一C++考试题感觉）这段代码看起来没啥问题，但是实际运行会发现死循环了。原因是imm是<br>unsigned char类型，范围为0~255，所以imm&lt;=255会一直成立。</p>
<p>> to be continue</p>
]]></content>
      <tags>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>大端模式和小端模式的差异和优缺点</title>
    <url>/2022/12/15/%E5%A4%A7%E7%AB%AF%E6%A8%A1%E5%BC%8F%E5%92%8C%E5%B0%8F%E7%AB%AF%E6%A8%A1%E5%BC%8F%E7%9A%84%E5%B7%AE%E5%BC%82%E5%92%8C%E4%BC%98%E7%BC%BA%E7%82%B9/</url>
    <content><![CDATA[<center>大端和小端的差异和优缺点</center>

<span id="more"></span>
<h2 id="大端模式和小端模式的差异"><a href="#大端模式和小端模式的差异" class="headerlink" title="大端模式和小端模式的差异"></a>大端模式和小端模式的差异</h2><p>这个很好理解，大端就是最低有效位位于低地址处；小端就是最低有效位位于高地址处。就不仔细解释了。</p>
<h2 id="两者的优缺点"><a href="#两者的优缺点" class="headerlink" title="两者的优缺点"></a>两者的优缺点</h2><p>操作系统最后一节课，老师讲了讲linux内核中的网络部分。他提了一嘴大端和小端各自的优势，之前还从未思考过。老师说：“小端方便强制类型转换，大端方便判断正负”。想想还挺有道理。</p>
<p>以存储一个有符号整数为例，对于大端模式来说，我们只需要访问变量地址的第一个字节便可以根据符号位来得到变量的正负；而对于小端模式来说，我们必须知道变量的具体长度，将变量的地址加上它的长度减一所得到的地址处的字节取出，才能得到变量的正负。</p>
<p>强制类型转换方面，以c++从int强制类型转换为short为例，对于小端模式来说，我们只需要从变量地址开始取出sizeof(short)个字节便可以完成强制类型转换；而对于大端模式来说，我们还需要知道强制类型转换前变量的类型及int类型，从变量地址加sizeof(int)-sizeof(short)处开始，取sizeof(short)个字节才可以完成强制类型转换。</p>
]]></content>
      <tags>
        <tag>体系结构</tag>
      </tags>
  </entry>
  <entry>
    <title>毕昇杯比赛心得</title>
    <url>/2023/08/04/%E6%AF%95%E6%98%87%E6%9D%AF%E6%AF%94%E8%B5%9B%E5%BF%83%E5%BE%97/</url>
    <content><![CDATA[<p><center>毕昇杯心得体会</center><br><span id="more"></span></p>
<p>这里记录一下2023年华为毕昇杯比赛心得经验。<a href="https://github.com/yuwensq/NKUF4_Compiler">仓库链接</a></p>
<h3 id="经验"><a href="#经验" class="headerlink" title="经验"></a>经验</h3><ol>
<li>做的优化及时合并，虽然自己写的优化跑通了，但是加上其他队友的优化之后往往会浮现出新的bug，要尽早解决。</li>
<li>编写好用的调试工具，段错误的时候可能打印都打印不出来信息（需要刷新缓冲区），这里建议写一个比较好用的log工具，我们直接把PA实验的Log工具搬过来了。</li>
<li>编写数据结构合法性检查工具，对于ssa形式的ir中间代码，维护每个operand的def和use的正确性非常重要，这里建议编写检查工具，每执行一个优化pass都进行检查，尽早发现问题，否则可能会导致其他优化不充分或者优化错误。</li>
<li>尽早调bug，虽然有的bug可能显得不是十分严重，但是也许这些bug是由比较严重的错误导致的，或者会影响其他优化，这里发现bug一定要尽早解决，拖到最后会让人手忙脚乱。</li>
<li>进行良好的内存管理，如果使用C++进行项目编写，要注意释放内存，否则有时候可能会出现莫名奇妙的错误，尤其是使用STL的时候。可以学学智能指针。</li>
<li>Patience is the key of life. 要对自己和自己的代码有耐心，往全面了想，只要我们做的比别人更加全面，我们就胜利了。不要着急写出来优化，先想好再编写往往会更快，bug也更少。如果发现没得优化了，就停下看看其他优化是否还有不足，灵感往往是周期性来的，只要编译出来的程序每天都更快一点点，最后就可以快很多。</li>
<li>多和队友沟通交流，<del>多push队友</del>，遇到自己解决不了的及时和队友商量。不只是队友，也可以多和其他参赛队沟通，大家互补起来可以起到共赢，比如我们决赛的时候就得到了兄弟队伍的帮助，性能分直接++。（当然这个也要分时候）</li>
<li>代码的可扩展性和可复用性很重要。前者可以使我们以最快的速度添加新的改进（比如我在决赛最后只花了将近30分钟添加了几条新的指令，把一个样例优化到了最好）；后者可以降低我们的代码量和debug量，这里建议把一些常用的比如lva分析、define reach分析、循环相关的分析、纯函数分析等单独编写为工具类。</li>
</ol>
<h3 id="具体细节"><a href="#具体细节" class="headerlink" title="具体细节"></a>具体细节</h3><ol>
<li>寄存器分配是最重要的环节之一，别用课上给的线性扫描，又慢生成的指令溢出还多（但是简单好实现）。推荐对线性扫描进行改进，比如使用greedy，或者实现图着色寄存器分配（我们的选择）。此外，尽可能多的使用寄存器，比如我们除了R0-R10，还使用了ip寄存器和lr寄存器，可用的寄存器越多，溢出越少。</li>
<li>纯函数可以看作表达式，因此做gvn或者cse的时候可以考虑把重复的纯函数也删除了。</li>
<li>做后端优化的要对汇编代码有较清晰的了解，选择合适的汇编指令，尽量减少访存操作。比如把立即数加载到寄存器中的时候，别一个劲只用ldr r0, =1这样的伪指令，可以根据立即数的范围来选择使用mov或者mvn指令，有的浮点常数也可以直接vmov.f32到s寄存器中。还有就是了解arm的寻址方式，ldr和str的地址操作数可以有好多种不同的形式。等等等等。</li>
<li>对于某些递归导致栈溢出的程序，做尾调用优化可能比尝试减少栈空间消耗来的更快。</li>
<li>注意栈对齐，尤其是代码中有浮点数的情况，栈不对齐可能会导致结果为0（这个特征比较明显，就是最后打印出来的浮点数很多是很小的数）。</li>
<li>学学文字池是干啥的，这个用的好了也可以减少一些多余的指令。</li>
<li>想得一等奖，得做并行优化。</li>
</ol>
]]></content>
      <tags>
        <tag>编译</tag>
      </tags>
  </entry>
  <entry>
    <title>硬件预取</title>
    <url>/2022/12/15/%E7%A1%AC%E4%BB%B6%E9%A2%84%E5%8F%96/</url>
    <content><![CDATA[<p><center>硬件预取</center><br><span id="more"></span></p>
<h1 id="硬件预取"><a href="#硬件预取" class="headerlink" title="硬件预取"></a>硬件预取</h1><p>体系结构期末大作业要我们用ChampSim模拟器来实现L2Cache的预取和LLCache的替换算法。之前对于硬件的预取没有太多的了解。这段时间学习一下，记录一下学习收获啥的，有一部分摘自网络。</p>
<p>下面给出具体参考的网站以及参考文献：</p>
<p><a href="https://zhuanlan.zhihu.com/p/491673969">https://zhuanlan.zhihu.com/p/491673969</a></p>
<p>Nesbit, K. J., &amp; Smith, J. E. (2004, February). Data cache prefetching using a global history buffer.<br>In 10th International Symposium on High Performance Computer Architecture (HPCA’04) (pp. 96-96).<br>IEEE.</p>
<h2 id="为啥要预取"><a href="#为啥要预取" class="headerlink" title="为啥要预取"></a>为啥要预取</h2><p>处理器和内存之间的性能差异越来越大导致的。两者发展的侧重点不同，CPU主要是要快，往更快执行任务发展；而内存的发展有些偏向于增加内存的密度，让内存变得更大。1985年至2010年间，CPU的性能提升的数千倍，可是内存相关的性能只提升了不到10倍。如果等CPU需要执行相关指令或者需要修改数据的时候再从内存中去读取，那么大部分时间都会花费在等待数据上，这是不可容忍的。这时预取的重要性就体现了，将将要访问的内容提前从内存搬移到Cache中，CPU就可以即时拿到所需的内容，避免了等待。当然，如果预取做得不好，是有可能导致性能下降的，由于Cache的大小是很宝贵的，如果预取判断出错，预取的是无用的数据，然后反而把Cache中后续有可能还会用到的数据给Evict了，那么会增加系统的功耗，减低性能。</p>
<h2 id="硬件预取策略关注点"><a href="#硬件预取策略关注点" class="headerlink" title="硬件预取策略关注点"></a>硬件预取策略关注点</h2><h3 id="考虑点"><a href="#考虑点" class="headerlink" title="考虑点"></a>考虑点</h3><p>想要消除内存性能与CPU性能的巨大的gap，相关的预取策略必须考虑以下三点：</p>
<ul>
<li>预取哪块数据：必须能准确地判断所需预取的数据，这个很好理解，无效数据对我们毫无用处，只会白白耗费电源，还有可能将缓存中有用的数据给踢出，从而导致性能下降。</li>
<li>何时开始预取：如果预取不及时，甚至晚于需要相关数据的节点，那么这个策略毫无帮助，浪费硬件资源，预取过早亦同理。</li>
<li>数据存放在哪：如果相关预取数据存放不合理，将会将后续需要用到的数据踢出CPU，造成性能下降。</li>
</ul>
<h3 id="两个关键指标"><a href="#两个关键指标" class="headerlink" title="两个关键指标"></a>两个关键指标</h3><p>在评价一个预取策略的好坏时，我们一般会从覆盖率和准确性两个维度来评判，好的预取策略必须同时在这两个指标上面有亮眼的成绩。</p>
<ul>
<li>覆盖率很好理解，覆盖率就是引入预取策略消除的Cache Miss次数与引入预取策略前的总的Cache Miss次数的比值。比如没有预取机制的时候会发生100次Cache Miss，然后引入预取将Cache Miss减少到了30次，意味着该预取策略成功避免了70次Cache Miss，即覆盖率为70/100=70%。<img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212142131261.jpg" alt=""></li>
<li>预取的正确率还需要考虑在这个过程中预取机制发出了几次预取操作，接着上面的例子，如果该预取机制总共发出了140次的预取操作，其中70次是有效的预取操作，那么正确率为70/140=50%。<img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212142131110.jpg" alt=""></li>
</ul>
<p>显然，覆盖率和准确性也是相互制约的。覆盖率的提高倾向于有更多的预取次数，而准确性的保证的限制了预取次数不能太高。我们应该在追求覆盖率的同时，尽量保证准确性。</p>
<h3 id="及时性"><a href="#及时性" class="headerlink" title="及时性"></a>及时性</h3><p>预取的及时性也是很需要考虑的一个指标。假设我们正确的判断出了要预取的数据，但是如果预取的时机不合适，也有可能适得其反。如果过早预取，则预取到的数据有可能在需要之前就被踢出缓存，还有可能占用缓存空间，造成Cache Miss的增加；如果过晚预取，则可能会造成较大的延时，CPU需要等待预取后才能继续执行。</p>
<h2 id="硬件预取分类"><a href="#硬件预取分类" class="headerlink" title="硬件预取分类"></a>硬件预取分类</h2><p>显然，程序运行时既需要指令又需要数据，因此硬件预取分为指令预取和数据预取两部分。</p>
<h3 id="指令预取"><a href="#指令预取" class="headerlink" title="指令预取"></a>指令预取</h3><p>程序执行中，指令往往顺序执行，虽然有跳转等等一些指令，但是指令的访问模式相对来说还是比较好预测的。</p>
<h3 id="数据预取"><a href="#数据预取" class="headerlink" title="数据预取"></a>数据预取</h3><p>相对来说，数据预取更加难以预测，因为程序对于数据的访问模式完全可以是随机的。因此，数据预取比较难做，也更加的有意思。</p>
<h2 id="指令预取机制"><a href="#指令预取机制" class="headerlink" title="指令预取机制"></a>指令预取机制</h2><h3 id="next-line-prefetcher"><a href="#next-line-prefetcher" class="headerlink" title="next-line prefetcher"></a>next-line prefetcher</h3><p>最简单的指令预取机制，维护了一个叫stream buffer的预取buffer，每次CPU请求一个缓存行时，会从下一级Cache中将下一个缓存行读取到stream buffer中。只在指令顺序执行的时候表现比较好，当遇到分支或者函数调用时，预测效果可能会较差。</p>
<p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212142157923.jpg" alt=""></p>
<h3 id="FDIP-Fetch-directed-instruction-prefetching-prefetcher"><a href="#FDIP-Fetch-directed-instruction-prefetching-prefetcher" class="headerlink" title="FDIP(Fetch-directed instruction prefetching) prefetcher"></a>FDIP(Fetch-directed instruction prefetching) prefetcher</h3><p>这种预取机制针对分支指令进行了处理，大概思路就是预取指令分支预测的结果。该机制在分支预测模块和取指模块之间插入了一个FTQ的模块，然后会经过一个filter把一部分地址过滤掉（比如已经存在ICache中的地址），然后进入到PIQ中，这时就会将PIQ中的地址发往下一级Cache，然后将取回的Cache Line放入一个全相联的Cache中。思考这种机制，它在分支预测之后预取，如果处理不好，感觉预取的速度跟不上取指的速度，就会带来一定的延迟。</p>
<p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212142236486.jpg" alt=""></p>
<h3 id="discontinuity"><a href="#discontinuity" class="headerlink" title="discontinuity"></a>discontinuity</h3><p>不连续预取机制，我理解的是，这种机制就是把每次分支指令跳转的目标给记录一下，记录到一张表中。当下次再次访问到该分支指令时，在取指阶段查表，然后预取相应的缓存行就行，到分支目标指令取值时，指令已经被预取到缓存中。</p>
<p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212142248807.jpg" alt=""></p>
<p>还有好多不同的指令预取机制，一口气也看不完，之后慢慢再学吧。</p>
<h2 id="数据预取机制"><a href="#数据预取机制" class="headerlink" title="数据预取机制"></a>数据预取机制</h2><h3 id="传统基于表的预取"><a href="#传统基于表的预取" class="headerlink" title="传统基于表的预取"></a>传统基于表的预取</h3><p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212201728018.jpg" alt=""></p>
<p>如上图所示，传统的基于表的预取机制通常使用一个表来记录历史信息，并根据表中记录的信息来指导预取的进行。表由键来索引，键常常是PC值或者访问数据的地址。这种预取机制分为两种方式，步幅预取(stride prefetching)和关联预取(correlation prefetching)。(不知道翻译的对不对，很好理解，步幅预取是根据访问历史记录的地址间的步长信息来进行预取，而关联预取则是根据不同地址之间出现的相关性来进行预取)</p>
<h4 id="Stride-Prefetching"><a href="#Stride-Prefetching" class="headerlink" title="Stride Prefetching"></a>Stride Prefetching</h4><p>传统的步幅预取使用一个表来存储关于单条load指令的与步幅相关的历史信息。因此，它使用load指令的PC值来索引历史表。每个表项记录对应的load指令的最近访存时发生缓存缺失的两个地址之间的<strong>步幅</strong>以及最近的一个发生缓存缺失的<strong>访存地址</strong>。当发生缓存缺失时，预取机制会使用当前的load指令的PC值来索引历史表，并将当前load指令的访存地址addr与表项中记录的最近一次的缓存缺失地址相减。得到的结果s如果与表项中记录的步幅一致，则这个时候预取器会感觉未来很有可能在$addr+d*s$的地址处也发生缓存缺失，此时，它便会向更低一级的cache发出预取请求，请求的地址就是$addr+s$、$addr+2s$……、$addr+ds$，其中，d就叫做预取度。</p>
<h4 id="Correlation-Prefetching"><a href="#Correlation-Prefetching" class="headerlink" title="Correlation Prefetching"></a>Correlation Prefetching</h4><p>比起步幅预取来说，关联预取可以处理更加复杂的地址访问模式。马尔可夫预取(Markov Prefetch)是一个使用历史表的关联预取算法。其中，预取键是导致缓存缺失的访问地址。历史表中的每个表项存储了历史中紧跟在对应预取键后的导致缓存缺失的访问地址的链表。当再次发生缓存缺失时，预取算法根据访存地址来索引历史表，然后从表项的链表中找出若干个置信度比较高的地址给预取了。(这里置信度高指的是未来更加有可能导致缓存缺失，比如我们可以预取发生缓存缺失时间上离预取键最近的那个访存地址)</p>
<p>马尔可夫预取还有一个推广，叫做距离预取(Distance Prefetch)。从上边的描述我们不难发现，马尔可夫预取与导致缓存缺失的地址的具体值有较大的联系(后者是前者历史表的索引)。距离预取则是弱化了这种联系，其使用地址增量(address delta)————两个连续导致缓存缺失的访存地址之间的距离作为历史表的预取键。每个历史表条目都包含一个增量列表，这些增量在过去出现在预取键对应的增量之后。当发生缓存缺失时，预取算法将当前地址加上查表得到的增量后的地址预取到缓存中。</p>
<h3 id="GHB预取机制"><a href="#GHB预取机制" class="headerlink" title="GHB预取机制"></a>GHB预取机制</h3><p>传统的表预取有一些缺点，比如容易有过时的数据；预取键容易造成冲突；每个表项能存储的历史信息较少等不足。然后就有人发明出了基于GHB的预取机制，将预取键的匹配和历史信息的存储给解耦了。</p>
<p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212202150882.jpg" alt=""></p>
<p>GHB预取机制的结构如上图所示，主要有两级组成：</p>
<ul>
<li>Index table. 索引表，这个表主要用于根据预取键来索引GHB缓冲区中的具体条目。这里预取键可以是PC，也可以是访存地址。</li>
<li>Global history buffer. 全局历史缓冲区，是一个循环队列。一共能存n项，其中每项存储一个地址以及一个指针。通过指针按时间顺序将一个预取键对应的地址给链起来。</li>
</ul>
<p>使用GHB也可以实现传统的步幅预取、马尔可夫预取、距离预取机制。</p>
<h4 id="GHB步幅预取"><a href="#GHB步幅预取" class="headerlink" title="GHB步幅预取"></a>GHB步幅预取</h4><p>类似于传统的基于表的步幅预取，基于GHB的步幅预取的预取键也是load指令的PC值。预取器通过预取键来索引GHB中的条目，这里我们使每个预取键对应的GHB中的条目为预取键对应的load指令的访存地址。上边说过，这些地址形成了一个链，我们只需要取出链上的最后三个地址，计算出来对应的两个步幅，如果两个步幅相等，就可以像步幅预取那样预取新的缓存行了。</p>
<h4 id="GHB关联预取"><a href="#GHB关联预取" class="headerlink" title="GHB关联预取"></a>GHB关联预取</h4><p>GHB关联预取也是类似的，它的预取键是实际的访存地址。预取键对应的GHB中的条目中存储了与预取键相同的地址。这样，当发生缓存缺失时，预取器根据缺失地址索引索引表，得到指向GHB条目的指针，然后访问得到的GHB条目链，链上每个元素在GHB中的直接后继元素即是预取候选。</p>
]]></content>
      <tags>
        <tag>体系结构</tag>
      </tags>
  </entry>
  <entry>
    <title>计算机夏令营复习知识点汇总</title>
    <url>/2023/06/26/%E8%AE%A1%E7%AE%97%E6%9C%BA%E5%A4%8F%E4%BB%A4%E8%90%A5%E5%A4%8D%E4%B9%A0%E7%9F%A5%E8%AF%86%E7%82%B9%E6%B1%87%E6%80%BB/</url>
    <content><![CDATA[<center>夏令营加油！(ง •_•)ง</center>

<span id="more"></span>
<h2 id="数学"><a href="#数学" class="headerlink" title="数学"></a>数学</h2><h3 id="线性代数"><a href="#线性代数" class="headerlink" title="线性代数"></a>线性代数</h3><p><strong>1.正定矩阵和半正定矩阵</strong></p>
<blockquote>
<p>【定义1】给定一个大小为$n\times n$的实对称矩阵$A$，若对于任意长度为$n$的非零向量$\boldsymbol{x}$，有$\boldsymbol{x}^TA\boldsymbol{x}&gt;0$恒成立，则矩阵$A$是一个正定矩阵。</p>
<p>【定义2】给定一个大小为$n\times n$的实对称矩阵$A$，若对于任意长度为$n$的向量$\boldsymbol{x}$，有$\boldsymbol{x}^TA\boldsymbol{x}\geq0$恒成立，则矩阵$A$是一个半正定矩阵。</p>
</blockquote>
<p>这个其实就是说一个向量经过正定矩阵这一线性变换之后的到的新的向量与原向量的夹角不会超过$90^{o}$。</p>
<p>判断一个矩阵A是否为正定矩阵有两种方法：</p>
<ol>
<li>求出A的所有特征值。若A的特征值均为正数，则A是正定的；若A的特征值均为负数，则A为负定的。</li>
<li>计算A的各阶顺序主子式。若A的各阶顺序主子式均大于零，则A是正定的；若A的各阶主子式中，奇数阶主子式为负，偶数阶为正，则A为负定的。</li>
</ol>
<p>矩阵正定性的性质：</p>
<ol>
<li>正定矩阵的特征值都是正数。</li>
<li>正定矩阵的所有子行列式都是正数。</li>
<li>若A为n阶正定矩阵，则A为n阶可逆矩阵。</li>
</ol>
<p><strong>2.正交矩阵的定义和性质</strong></p>
<p>定义：A是一个方阵，且$A^TA=E$，则称A为正交矩阵。</p>
<p>性质：</p>
<ol>
<li>$det(A^TA)=det(E)=1$，得到$det(A)=\pm 1$</li>
<li>A和B都是正交阵，则AB也是正交阵</li>
<li>一组标准正交基(两两正交且模长为1的向量组)下的正交变换可以用正交矩阵表示。正交变换不改变向量的模长和向量间的夹角。</li>
</ol>
<p>正交变换$T$满足：$|T\alpha|=|\alpha|$。</p>
<h3 id="概率论"><a href="#概率论" class="headerlink" title="概率论"></a>概率论</h3><p><strong>1.独立和互斥</strong></p>
<p>独立说的是$P(A|B)=P(A)$，即$P(AB)=P(A)P(B)$，就是说A发生的概率不受B的影响。</p>
<p>互斥说的是$P(AB)=0$，即A和B不能同时发生。</p>
<p>显然，当$P(A)&gt;0, P(B)&gt;0$时，独立不互斥，互斥不独立。</p>
<p><strong>2.大数定理</strong></p>
<blockquote>
<p>切比雪夫不等式：$P\left \{ |X - EX| \ge \varepsilon  \right \} \le \frac{DX}{\varepsilon ^{2} }$ </p>
<p>辛钦大数定理：设$X_1,X_2…X_n$<br>是独立同分布的随机变量序列，且具有数学期望$EX_k=μ，k=1,2,3…$。则对于$∀ε&gt;0$，有$\lim_{n \to +\infty} P\left \{ | \frac{1}{n}\sum_{k=1}^{n}X_{k} - \mu |  &lt; \varepsilon \right \} = 1$，均值依概率收敛于期望。（对方差没有要求）</p>
<p>切比雪夫大数定律：设$X_1,X_2…X_n$是不相关的随机变量序列，且期望$E_i$和方差$D_i$都存在，方差有界$D_i\le M$，则对于$∀ε&gt;0$，有$\lim_{n \to +\infty} P\left \{ | \frac{1}{n}\sum_{i=1}^{n}X_{i} -  \frac{1}{n}\sum_{i=1}^{n}EX_i |  &lt; \varepsilon \right \} = 1$，均值依概率收敛于期望的均值。</p>
<p>切比雪夫大数定律：设$X_1,X_2…X_n$是相互独立的随机变量序列，且具有相同的期望$\mu$和方差$\sigma ^ 2$，则对于$∀ε&gt;0$，有$\lim_{n \to +\infty} P\left \{ | \frac{1}{n}\sum_{i=1}^{n}X_{i} -  \mu |  &lt; \varepsilon \right \} = 1$，均值依概率收敛于期望。</p>
</blockquote>
<p>其实就是n越来越大，样本均值趋近于期望，频率趋近于概率。</p>
<p><strong>3.贝叶斯公式</strong></p>
<p>$P(A|B)= \frac{P(A)P(B|A)}{P(B)}$</p>
<p>$P(A_i|B)= \frac{P(A_i)P(B|A_i)}{\sum_{k=1}^{n}P(A_k)P(B|A_k)}$</p>
<p>第二个公式下边用了一个全概率公式$（A_1,A_2,…,A_n）$为完备事件集。</p>
<p>举个栗子，就是说如果我们知道患各种病的情况下发烧的概率，我们就可以推断出发烧的情况下患各种病的概率。</p>
<p><strong>4.中心极限定理</strong></p>
<blockquote>
<p>$X_1, X_2, …, X_n…$独立同分布，$EX_i=\mu, DX_i=\sigma^2, 0&lt;\sigma^2&lt;+\infty$，则$\lim_{n \to \infty} P(\frac{\Sigma_{i=1}^nX_i-n\mu}{\sqrt{n}\sigma  }\le x)=\Phi_0(x)$。</p>
</blockquote>
<p>大量独立同分布的随机变量的和服从正态分布。</p>
<h3 id="离散数学"><a href="#离散数学" class="headerlink" title="离散数学"></a>离散数学</h3><p><strong>1.群、环、域</strong></p>
<p><a href="http://sparkandshine.net/algebraic-structure-primer-group-ring-field-vector-space/">参考网站，下方图片来源</a></p>
<blockquote>
<p>代数主要研究的是运算规则。一门代数， 其实都是从某种具体的运算体系中抽象出一些基本规则，建立一个公理体系，然后在这基础上进行研究。一个集合再加上一套运算规则，就构成一个代数结构。</p>
</blockquote>
<p>他三都是抽象代数，就是说把数字抽象成一个集合，集合里的元素不管是啥。然后把具体的加、减、乘、除之类的二元运算抽象成运算，不管运算具体干啥。<br>表示就表示为代数结构(R, *)，R是对象集合，*是集合上的二元运算。如果代数结构满足了某些性质，就给他们起相应的名字。</p>
<p>群：<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202306302310153.jpg" alt=""></p>
<p>环和域：<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202306302311512.jpg" alt=""></p>
<p><strong>2.命题逻辑与谓词逻辑</strong></p>
<p>命题逻辑具有局限性，比如苏格拉底三段论就没法表示。</p>
<p>在命题逻辑中，命题是具有真假意义的陈述句。从语法上分析，一个陈述句由主语和谓语两部分组成。在谓词逻辑中，为揭示命题内部结构及其不同命题的内部结构关系，对命题进行分析，并且把主语称为个体或客体，把谓语称为谓词。</p>
<p>在命题逻辑中，一个可以判断真假的陈述句就是研究的最小单元；谓词逻辑是对命题逻辑更进一步的细分，将一个陈述句分为客体和谓词。</p>
<p><strong>3.幂集</strong></p>
<p>集合的集合：$P(A)=\left \{x|x\subseteq A \right \}$<br>估计叫做幂集是因为里面的元素个数正好是A集合元素个数的2的幂次。</p>
<p><strong>4.关系</strong></p>
<p>定义 如果一个集合满足以下条件之一：</p>
<ol>
<li>集合非空, 且它的元素都是有序对</li>
<li>集合是空集<br>则称该集合为一个二元关系, 简称为关系，记作R.<br>如$\lt x,y\gt$∈R, 可记作xRy；</li>
</ol>
<p>设A,B为集合,A×B的任何子集所定义的二元<br>关系叫做从A到B的二元关系, 当A=B时则叫做 A上<br>的二元关系.</p>
<p><strong>5.集合的划分</strong></p>
<p>集合的划分指的是把集合划分成若干个小的集合，这些小集合并起来等于原来的大集合，且小集合两两之间没有交集。这些小集合就叫做原来集合的一个划分。</p>
<p>等价关系的商集就是一个划分。</p>
<p><strong>6.欧拉图和哈密尔顿图</strong></p>
<p>这里要区分欧拉路径和欧拉回路。欧拉回路是个圈，欧拉路径是个环或者链。</p>
<p>连通的无向图$G$有欧拉路径的充要条件是：$G$中奇顶点（连接的边数量为奇数的顶点）的数目等于0或者2。</p>
<p>连通的无向图$G$是欧拉环（存在欧拉回路）的充要条件是：<br>$G$中每个顶点的度都是偶数。</p>
<p>哈密顿路径和哈密顿回路也是类似的。回路是圈，路径是环或链。哈密顿回路去掉一条边就是哈密顿路径，但是哈密顿路径加上一条边不一定能变成哈密顿回路。</p>
<p>找哈密顿路径是个NP问题。</p>
<p>具有欧拉回路的图叫欧拉图；具有哈密顿回路的图叫哈密顿图。只有欧拉路径但没有欧拉回路的图叫半欧拉图；只有哈密顿路径但没有哈密顿回路的图叫半哈密顿图。</p>
<h3 id="高数"><a href="#高数" class="headerlink" title="高数"></a>高数</h3><p><strong>1.连续和一致连续</strong></p>
<p>连续：$\lim_{x \to x_0}f(x)=f(x_0)$。即$\forall \varepsilon &gt; 0, \exists \delta &gt; 0, \forall x\in D_f, 0 &lt; |x-x_0| &lt; \delta, 使得|f(x)-f(x_0)|&lt;\varepsilon$。</p>
<p>一致连续：$\forall \varepsilon &gt; 0, \exists \delta &gt; 0, \forall x_1, x_2\in D_f, |x_1-x_2| &lt; \delta, 都有|f(x_1)-f(x_2)|&lt;\varepsilon$。</p>
<p>一致连续的函数不能很陡峭。</p>
<p><strong>2.最小二乘法</strong></p>
<p>就是用几个线性无关的函数的线性组合来对样本进行拟合。目标就是让预测值和实际值的差的平方和最小。求每个函数前边的系数。具体解法就是对每个参数求偏导，让偏导数等于零。</p>
<p>$h_\theta(x)=\theta_0+\theta_1x+\theta_2x^2+…+\theta_nx^n+…$</p>
<p>目标是让$\Sigma_{i=1}^{n}(y_i-h_\theta(x_i))^2$最小。</p>
<p><strong>3.可微和可导的关系</strong></p>
<p>对于一元函数来说，可微$\Leftrightarrow $可导$\Rightarrow$连续$\Rightarrow$可积。</p>
<p>对于多元函数来说，函数在一点可微$\Leftrightarrow$函数的所有偏导数在该点存在并连续$\Rightarrow$连续$\Rightarrow$可积。</p>
<p>导数和微分本质是两种东西，前者是函数在某个方向上的变化率，后者是映射的局部线性近似。<a href="https://www.zhihu.com/question/67235804">ref</a></p>
<h2 id="专业课"><a href="#专业课" class="headerlink" title="专业课"></a>专业课</h2><h3 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h3><p><strong>1.几个排序算法介绍，时空复杂度，稳定性咋样？</strong></p>
<h3 id="操作系统"><a href="#操作系统" class="headerlink" title="操作系统"></a>操作系统</h3><h3 id="计组"><a href="#计组" class="headerlink" title="计组"></a>计组</h3><p><strong>1.USB接鼠标/键盘有什么区别？</strong></p>
<p>北航面试的问题，这是啥意思鸭。我个人理解的是对于键盘来说，按键的到来随时间是离散的；而对于鼠标来说，其位置移动随时间是连续的。因此如果接入键盘，按下一个按键就发起一个中断即可，如果接入鼠标，则需要将鼠标的位置移动先缓存到缓冲区中，缓冲区满了再统一发起中断？</p>
<p><strong>2.计算机是如何启动的？</strong></p>
<ol>
<li>BIOS: 计算机通电后，首先读取并执行BIOS（基本输入输出系统）程序。BIOS程序首先进行硬件自检，检查硬件是否能满足运行的基本条件。BIOS感觉和AM点类似？</li>
<li>启动程序: BIOS寻找MBR主引导记录，将找到的引导程序加载至内存，并将控制权交给引导程序。引导程序把操作系统从硬盘读入到内存。</li>
<li>控制权交给操作系统，OS创建一个init进程，其pid为1，是所有进程的祖先。然后init进程加载系统的各个模块。</li>
</ol>
<p><strong>3.体系结构和组成原理的区别</strong></p>
<p><a href="https://www.zhihu.com/question/326558346/answer/749946338">参考</a></p>
<p>计算机体系结构是程序员眼中的计算机。程序员眼中的计算机有指令集、数据类型、寻址技术等。只要两个计算机指令集、数据类型、寻址类型等相等，就说这两个计算机体系结构相同。计算机组成是体系结构的具体实现形式。一个计算机是否具有乘法指令的功能，这是结构问题，而用什么方式实现乘法，就是组成问题。实现乘法可以用一个专门的乘法电路，也可以用连续相加的加法电路来实现，这两者的区别就是组成问题。</p>
<p>举个例子：大概就是x86架构是体系结构，如何实现x86架构是组成原理。</p>
<h3 id="计网"><a href="#计网" class="headerlink" title="计网"></a>计网</h3><p><strong>1.各个层的作用</strong></p>
<ul>
<li>应用层：满足用户应用需求。</li>
<li>传输层：维护端到端连接的可靠性，复用和分用。</li>
<li>网络层：路由与转发。</li>
<li>链路层：维护点到点连接的可靠性，编码解码，逻辑链路层+介质访问控制层。</li>
<li>物理层：信息传输。</li>
</ul>
<p><strong>2.输入网址并按下enter后发生的事情。</strong></p>
<ul>
<li>俯视角度：<ol>
<li>DNS找到目标主机ip：网址是url，是人看的懂得形式，但是计算机在网络中是通过ip地址来标识主机的。所以首先需要通过DNS来把域名解析为ip地址。具体过程为，首先查找本机的DNS缓存如果找到了直接解析，如果没有找到就向本地域名服务器发起DNS请求。本地域名服务器接收到DNS请求后，首先查找自己的缓存有没有对应的记录，如果找到了直接返回给客户机即可。如果没有找到，则首先向根域名服务器发起DNS请求，根域名服务器返回对应的顶级域名服务器的ip地址；然后本地域名服务器再向顶级域名服务器发起DNS请求，顶级域名服务器返回对应域名的权威服务器的ip地址；然后本地域名服务器向权威域名服务器发起请求，权威域名服务器返回所请求的域名对应的ip地址。最后，本地域名服务器将得到的ip地址返回给客户机，并在自己机器里缓存对应的域名资源记录。请求本地域名服务器的过程是递归式的，本地域名服务器向其他域名服务器请求解析的过程是迭代式的。</li>
<li>TCP三次握手建立连接：Web浏览器使用http应用层协议，http协议基于TCP传输层协议。因此，浏览器得到服务器的ip地址后首先要和服务器通过三次握手建立TCP连接。这个过程很简单，主要就是用于同步TCP的序列号之类的。就是SYN、SYN+ACK、ACK这三次握手，具体不写了。</li>
<li>客户端服务器通过http协议传输数据：建立TCP连接之后，客户端向服务器发送http请求报文，然后服务器收到请求报文后对其进行解析和处理，并返回响应报文。客户端接收到响应报文后，根据报文的内容将页面渲染出来，如果页面中引用了其他的对象，就需要再次发起http请求。报文格式也比较固定，不写了。</li>
<li>TCP四次挥手断开连接：客户端和服务器完成通信后，需要通过四次挥手断开TCP连接。然后这里的一个问题就是一方发送FIN后表示他不会发送数据了，但是另一方此时还是可以发送数据的。</li>
</ol>
</li>
<li>平视角度：<ol>
<li>应用层调用socket接口，借助传输层传输数据。</li>
<li>传输层得到应用层要传输的数据和目标ip目标端口之后，封装一个传输层数据单元（忘了叫啥名字了，数据段？），头部里面存储的有源端口、目标端口、协议相关的一些东西。然后传输层将数据单元传给ip层。这里端口起到分用和复用的作用，就是说一个ip上有多个端口，就类似于一栋楼里面有多个住户一样。</li>
<li>ip层得到传出层传下来的服务数据单元和目标ip之后，封装一个ip层数据单元，头部里面有源ip、目标ip、ttl、分片的字节偏移啥的。然后ip层会查自己的路由表，找到要从哪个端口把数据发出去。这个过程就是转发。查表之后把数据报传给链路层。生成路由表的过程是路由，需要路由算法的支持，路由算法主要有两种，一个是链路状态算法（dijkstra的思想），一个是距离矢量算法（dp的思想）。</li>
<li>链路层对ip层的数据进行进一步的封装和发送。这里涉及到需要知道目标端口的问题，因为链路层只知道目标ip，不知道目标端口，它就需要首先通过目标ip（指的是下一跳的ip）找到目标端口，具体使用ARP协议。大致就是先广播一下，询问目标ip的主机的端口MAC地址（标识端口）是啥，然后目标ip的主机收到报文后就会返回给询问MAC地址的主机一个报文，告诉他自己的MAC地址是啥。这样就可以找到目标ip的端口了，最后再把这个ip到MAC的映射缓存一下，之后用的时候直接查表即可。找到目标MAC后，链路层对数据报进行封装，封装成帧，然后把帧通过物理层发送出去就行了。</li>
<li>物理层不太会，大概就是传输信号？</li>
</ol>
</li>
</ul>
<p><strong>3.为什么IP是面向无连接的协议。</strong></p>
<ol>
<li>面向连接的协议的连接处理过程较为复杂，且容易降低处理速度。</li>
<li>不利于提供不同类型的服务，如一些即时视频等应用，使用面向连接的协议会影响服务的质量。网络分层可以很好的提供个性化的定制服务。</li>
</ol>
<p>IP协议就是<strong>尽力而为交付</strong>。</p>
<p><strong>4.路由算法</strong></p>
<ol>
<li>链路状态算法：首先得到整个网络的拓扑结构，然后在这个图上执行dikstra算法，计算出当前点到其他节点的最短路。代表协议是OSPF协议。</li>
<li>距离矢量算法：维护一个当前节点到其他各个节点的距离向量，以及当前节点的每个邻居到其他各个节点的距离向量。当自己的距离向量发生改变时，将这个距离向量通告给邻居节点；当接收到来自邻居节点的新的距离向量时，更新自己的距离向量。<br>毒性逆转就是说，如果一个节点通过另一个邻居节点到目标节点路径最短，这个节点通告给该邻居节点的距离向量中，该节点到目标节点的最短距离为无穷。主要是为了解决坏消息传递的慢的问题。代表协议是RIP协议。</li>
</ol>
<h3 id="数据库"><a href="#数据库" class="headerlink" title="数据库"></a>数据库</h3><p><strong>1.数据库的第一第二第三范式</strong></p>
<ol>
<li>1NF:<strong>数据库表的每一列都是不可分割的原子数据项</strong>，而不能是集合，数组，记录等非原子数据项。</li>
<li>2NF:<strong>非主属性必须完全依赖于主键</strong>，要求数据库表中的每个实例或记录必须可以被唯一地区分。</li>
<li>3NF:在第二范式的基础上，数据表中如果不存在非关键字段对任一候选关键字段的传递函数依赖，则符合第三范式。简而言之，第三范式就是<strong>属性不依赖于其它非主属性</strong>。</li>
<li>BCNF:任何主属性不能对主键子集依赖。<br>一般来说，数据库只需满足第三范式（3NF）就行了。</li>
</ol>
<h3 id="编译原理"><a href="#编译原理" class="headerlink" title="编译原理"></a>编译原理</h3><p><strong>1.从代码到可执行程序的步骤</strong></p>
<ol>
<li>预处理：主要是处理注释和宏定义。</li>
<li>编译：把预处理后的代码编译为目标平台的汇编代码。<ol>
<li>词法分析：将字符流识别为单词流。</li>
<li>语法分析：识别语句的语法结构，构建语法树。</li>
<li>语义分析：类型检查，类型转换之类的。</li>
<li>中间代码生成：将语法树翻译为中间代码。</li>
<li>中间代码优化：优化中间代码。</li>
<li>目标代码生成：将中间代码翻译为目标代码。</li>
<li>目标代码优化：优化目标代码。</li>
</ol>
</li>
<li>汇编：对于编译生成的汇编代码，汇编器将其翻译为二进制可重定位文件。</li>
<li>链接：文件中可能引用外部符号，链接主要就是处理这些符号引用，把多个可重定位文件合成一个可执行文件，并计算符号的地址之类的。</li>
</ol>
<h2 id="英文"><a href="#英文" class="headerlink" title="英文"></a>英文</h2><h3 id="自我介绍"><a href="#自我介绍" class="headerlink" title="自我介绍"></a>自我介绍</h3><p>Good morning/afternoon/evening professors. I am greatly honored to be here for this interview. </p>
<p>// 大致介绍背景<br>My name is XuWenBin, from XinXiang, HeNan Province, and I am studying computer science at NanKai University.</p>
<p>// 介绍高中情况，省一<br>Since high school, I have had a strong interest in computer science and started to learn programming and algorithm knowledge. And I won the provincial first prize in NOIP.</p>
<p>// 介绍大学成绩和奖项<br>In terms of my schoolwork in college, I love learning very much and have a solid professional knowledge base. I worked hard and got good grades in most of my courses, ranking 10th out of 163.And I was awarded an Academic Excellence Scholarship for outstanding grades. As for language, besides passing CET-4 and CET-6 with decent scores, I also learned Japanese and passed the CJT-4.</p>
<p>// 介绍项目<br>What’s more, I have strong practical ability and took part in many projects. For example, I wrote a compiler for a subset of the C language with my teammates from scratch this summer and won second prize of Bi Sheng Cup. And I implemented a reliable transmission protocol that supports piplining and congestion control at the application layer based on the UDP protocol.</p>
<p>// 介绍研究兴趣和未来规划<br>In the past three years, I have developed a strong interest in the field of computer systems. In the future, I’m willing to do relevant scientific research work in university.</p>
<p>This concludes my introduction. Thanks for your attention.</p>
<h3 id="介绍家乡"><a href="#介绍家乡" class="headerlink" title="介绍家乡"></a>介绍家乡</h3><p>I come from Henan province. There are many people in Henan province. And Henan people are very able to bear hardships. What’s more, we don’t really steal manhole covers. As for food, I think HuiMian is the most famous and delicious food of Henan. There are also many places and interests in Henan, such as Longmen Grottoes, white horse temple, Qingming Shanghe Garden and so on.</p>
]]></content>
      <tags>
        <tag>夏令营</tag>
      </tags>
  </entry>
</search>
