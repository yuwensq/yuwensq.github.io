<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Hello World</title>
    <url>/2022/12/13/Hello%20World/</url>
    <content><![CDATA[<p>Hello World!</p>
<span id="more"></span>
<p>Welcome to Bin’s blog.</p>
]]></content>
  </entry>
  <entry>
    <title>VSCODE比较代码差异</title>
    <url>/2022/12/15/VSCODE%E6%AF%94%E8%BE%83%E4%BB%A3%E7%A0%81%E5%B7%AE%E5%BC%82/</url>
    <content><![CDATA[<h1 id="VSCODE比较代码差异"><a href="#VSCODE比较代码差异" class="headerlink" title="VSCODE比较代码差异"></a>VSCODE比较代码差异</h1><span id="more"></span>
<p>这学期的实验老是要把代码从上一个实验移到下一个实验，由于使用win+vmware，我直接在windows的vscode里进行代码编写，利用vscode连接linux虚拟机。当要合并代码时就很烦。之前使用linux的meld，但是要进入到vmware里。最后我发现，使用vscode也可以进行代码的对比和合并，虽然功能大概没有meld强大（<strong>而且目前好像只能比较两个文件</strong>），但是目前够用了。下面简单说一下大致用法。</p>
<ul>
<li>选中两个文件<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212081131962.jpg" alt=""></li>
<li>右键，选择“将已选项进行比较”<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212081132639.jpg" alt=""></li>
</ul>
<p>然后我们就可以对比文件差异了，如下图所示，我们也可以点击箭头进行代码移动。</p>
<p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212081133074.jpg" alt=""></p>
]]></content>
      <tags>
        <tag>小科技</tag>
      </tags>
  </entry>
  <entry>
    <title>大端模式和小端模式的差异和优缺点</title>
    <url>/2022/12/15/%E5%A4%A7%E7%AB%AF%E6%A8%A1%E5%BC%8F%E5%92%8C%E5%B0%8F%E7%AB%AF%E6%A8%A1%E5%BC%8F%E7%9A%84%E5%B7%AE%E5%BC%82%E5%92%8C%E4%BC%98%E7%BC%BA%E7%82%B9/</url>
    <content><![CDATA[<h1 id="大端和小端的差异和优缺点"><a href="#大端和小端的差异和优缺点" class="headerlink" title="大端和小端的差异和优缺点"></a>大端和小端的差异和优缺点</h1><span id="more"></span>
<h2 id="大端模式和小端模式的差异"><a href="#大端模式和小端模式的差异" class="headerlink" title="大端模式和小端模式的差异"></a>大端模式和小端模式的差异</h2><p>这个很好理解，大端就是最低有效位位于低地址处；小端就是最低有效位位于高地址处。就不仔细解释了。</p>
<h2 id="两者的优缺点"><a href="#两者的优缺点" class="headerlink" title="两者的优缺点"></a>两者的优缺点</h2><p>操作系统最后一节课，老师讲了讲linux内核中的网络部分。他提了一嘴大端和小端各自的优势，之前还从未思考过。老师说：“小端方便强制类型转换，大端方便判断正负”。想想还挺有道理。</p>
<p>以存储一个有符号整数为例，对于大端模式来说，我们只需要访问变量地址的第一个字节便可以根据符号位来得到变量的正负；而对于小端模式来说，我们必须知道变量的具体长度，将变量的地址加上它的长度减一所得到的地址处的字节取出，才能得到变量的正负。</p>
<p>强制类型转换方面，以c++从int强制类型转换为short为例，对于小端模式来说，我们只需要从变量地址开始取出sizeof(short)个字节便可以完成强制类型转换；而对于大端模式来说，我们还需要知道强制类型转换前变量的类型及int类型，从变量地址加sizeof(int)-sizeof(short)处开始，取sizeof(short)个字节才可以完成强制类型转换。</p>
]]></content>
      <tags>
        <tag>细碎知识</tag>
      </tags>
  </entry>
  <entry>
    <title>计网大作业一</title>
    <url>/2022/12/13/%E8%AE%A1%E7%BD%91%E5%A4%A7%E4%BD%9C%E4%B8%9A%E4%B8%80/</url>
    <content><![CDATA[<h1 id="计算机网络大作业第一部分"><a href="#计算机网络大作业第一部分" class="headerlink" title="计算机网络大作业第一部分"></a>计算机网络大作业第一部分</h1><blockquote>
<p>徐文斌 计算机科学与技术 2010234</p>
</blockquote>
<h2 id="实验要求"><a href="#实验要求" class="headerlink" title="实验要求"></a>实验要求</h2><p>利用数据报套接字在用户空间实现面向连接的可靠数据传输，功能包括：建立连接、差错检测、确认重传等。流量控制采用停等机制，完成给定测试文件的传输。</p>
<span id="more"></span>
<h2 id="实验设计"><a href="#实验设计" class="headerlink" title="实验设计"></a>实验设计</h2><h3 id="协议设计"><a href="#协议设计" class="headerlink" title="协议设计"></a>协议设计</h3><h5 id="1-报文格式"><a href="#1-报文格式" class="headerlink" title="1. 报文格式"></a>1. 报文格式</h5><p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202211181112058.jpg" alt=""></p>
<p>报文由10字节的定长报文首部和变长的数据部分组成。</p>
<p>首部各字段作用如下：</p>
<ul>
<li>0-15位为传输的数据报的序列号，本次实验只使用了0和1。</li>
<li>16-31位为ack确认值，用于服务器对接收到的客户端的数据进行确认。</li>
<li>32-47位为标志位，目前只使用低5位，从低至高分别为SYN、ACK、FIN、LAS、RST，其中LAS位用于标识当前数据包是承载着所发送文件的最后一个数据包。</li>
<li>48-63位为校验和，用于实现差错检测。</li>
<li>64-79位为数据部分的长度。</li>
</ul>
<h5 id="2-握手与挥手"><a href="#2-握手与挥手" class="headerlink" title="2. 握手与挥手"></a>2. 握手与挥手</h5><ul>
<li>三次握手进行连接<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202211181120061.jpg" alt=""><br>这里我使用类似于TCP的三次握手进行连接建立。首先，客户端向服务端发送请求数据包，将SYN置位。服务端接收到数据包后，向客户端发送确认数据包，数据包中SYN和ACK均置位。客户端接收到数据包后，向服务端发送数据包，将ACK置位，之后进入连接建立状态，开始向服务器发送数据。服务端接收到ACK数据包后，进入连接建立状态，开始进行数据的接收。</li>
<li>握手过程中丢包的处理<ol>
<li>如果客户端发送的第一个SYN数据包产生了丢失，客户端迟迟无法接收到来自服务端的SYN、ACK数据包，则客户端会对SYN数据包进行超时重传，当超过重传次数后若还未收到响应，则建立连接失败。</li>
<li>如果服务端接收到了客户端的SYN数据包，但其发回客户端的SYN、ACK数据包产生了丢失。此时，客户端仍无法接收到SYN、ACK数据包，客户端会继续重传SYN报文；而服务端则迟迟无法接收到第三次握手来自客户端的ACK数据包，此时服务器也会对SYN、ACK数据包进行超时重传，同样，当超过重传次数后若还未收到响应，则建立连接失败。</li>
<li>如果客户端发送给服务器的ACK数据包产生了丢失。此时，客户端已经接收到了来自服务器的SYN、ACK报文，并向服务器发送ACK报文后，客户端进入了建连状态，可以开始向服务器发送具体的数据。而如果服务器在等待第三次握手的ACK报文时接收到了来自客户端发送的承载着具体数据的报文，服务器会认为产生了异常的连接，其会向客户端发送一个RST数据包，表示连接异常，建立连接失败。</li>
</ol>
</li>
<li>两次挥手断开连接<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202211181126063.jpg" alt=""><br>挥手方面，由于在实验中，发送端和接收端是固定的，服务器不会主动向客户端发送数据，我们只需要进行两次挥手即可。首先，客户端向服务器发送数据包，将FIN置位，并将seq填充一个随机值y，由于本次序列号只用了0和1，这里是为了避免对FIN报文进行确认的ACK数据包和对普通数据报文进行确认的ACK数据包混淆。服务端接收到报文后，向客户端发送响应报文，将ACK置位，并将ack值设置为y+1，然后断开连接。客户端接收到数据包后，连接成功断开。</li>
<li>挥手过程中丢包的处理<ol>
<li>如果客户端向服务器发送的FIN报文产生了丢失，客户端将迟迟无法接收到来自服务器的ACK报文，其会对FIN报文进行超时重传，重传超过一定次数之后会默认此时连接产生了异常，会直接断开连接。对于服务器来说，服务器接收到LAS标志位为1的报文而进入挥手状态后，会开启一个计时器，设置一个超时时间，如果超过该时间仍未接收到来自客户端的FIN报文，服务器也会默认连接产生了异常，会直接断开连接。</li>
<li>如果服务器向客户端发送的ACK确认报文产生了丢失，此时服务器已经接收到了来自客户端的FIN报文，向客户端发送ACK报文后服务器便会断开连接。此时，客户端由于没有接收到ACK报文，会继续重传FIN报文，直到超过最大重传次数。</li>
</ol>
</li>
</ul>
<h5 id="3-数据传输"><a href="#3-数据传输" class="headerlink" title="3.数据传输"></a>3.数据传输</h5><p>对于数据的发送和接收采用rdt3.0中的状态机。</p>
<p>发送端状态机：<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202211181132453.png" alt=""></p>
<p>接收端状态机：<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202211181133632.png" alt=""></p>
<p>数据在传输时，将一个文件分为多个数据报进行分段传输。使用停等机制，在传输时，发送端需要接收到对上一个发送出的数据包的确认报文才能发送下一个数据包；接收端接收到了一个数据包后，首先要进行校验，如果检查无误，则向发送端返回对该报文的确认报文。在一定时间内，如果发送端没有收到对最近发送的数据报文的确认报文，将会重新传输该包。如果接收端收到了重复的包裹，则会将其丢弃，但仍需要向发送端发送对该报文的确认报文。</p>
<p>在最后，发送方需要向接收端发送一个LAS=1的报文，以通知接收端文件传输结束，进入挥手阶段。</p>
<h2 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h2><h3 id="数据头和标志位定义"><a href="#数据头和标志位定义" class="headerlink" title="数据头和标志位定义"></a>数据头和标志位定义</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> MSS 14600</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYN 0x1</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> ACK 0x2</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> FIN 0x4</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> LAS 0x8</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> RST 0x10</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> pack(push)</span></span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> pack(1)</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Header</span> &#123;</span><br><span class="line">	u_short seq; <span class="comment">//序列号</span></span><br><span class="line">	u_short ack; <span class="comment">//ack值</span></span><br><span class="line">	u_short flag; <span class="comment">//标志位</span></span><br><span class="line">	u_short checksum; <span class="comment">//校验和</span></span><br><span class="line">	u_short length; <span class="comment">//数据部分长度</span></span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">set_args</span><span class="params">(u_short seq, u_short ack, u_short flag, u_short checksum, u_short length)</span> </span>&#123; <span class="comment">//设置首部各个字段的值</span></span><br><span class="line">		<span class="keyword">this</span>-&gt;seq = seq;</span><br><span class="line">		<span class="keyword">this</span>-&gt;ack = ack;</span><br><span class="line">		<span class="keyword">this</span>-&gt;flag = flag;</span><br><span class="line">		<span class="keyword">this</span>-&gt;checksum = checksum;</span><br><span class="line">		<span class="keyword">this</span>-&gt;length = length;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> pack(pop)</span></span><br></pre></td></tr></table></figure>
<h3 id="计算校验和"><a href="#计算校验和" class="headerlink" title="计算校验和"></a>计算校验和</h3><ul>
<li>发送方生成校验和<ol>
<li>将要发送的数据包分成若干个16位的位串，每个位串看成一个二进制数。</li>
<li>将首部中的校验和字段置为0，该字段也参与校验和运算。</li>
<li>对这些16位的二进制数进行反码求和，累加的结果再取反即生成了校验和，将校验和放入检验和字段中。</li>
</ol>
</li>
<li>接收方校验检验和<ol>
<li>接收方将接收的数据(包括检验和字段)按同样的方法进行反码求和，累加的结果再取反码。</li>
<li>校验，如果上一步运算出的结果为0，表示传输正确；否则，说明传输中出现了差错。</li>
</ol>
</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">u_short <span class="title">checksum</span><span class="params">(<span class="type">char</span> *msg, <span class="type">int</span> length)</span> </span>&#123;</span><br><span class="line">	<span class="type">int</span> size = length % <span class="number">2</span> ? length + <span class="number">1</span> : length;</span><br><span class="line">    <span class="type">int</span> count = size / <span class="number">2</span>;</span><br><span class="line">	<span class="type">char</span>* buf = <span class="keyword">new</span> <span class="type">char</span>[size];</span><br><span class="line">    <span class="built_in">memset</span>(buf, <span class="number">0</span>, size);</span><br><span class="line">    <span class="built_in">memcpy</span>(buf, msg, length);</span><br><span class="line">    u_long sum = <span class="number">0</span>;</span><br><span class="line">	u_short *buf_it = (u_short*)buf;</span><br><span class="line">    <span class="keyword">while</span> (count--) &#123;</span><br><span class="line">        sum += *buf_it++;</span><br><span class="line">        <span class="keyword">if</span> (sum &amp; <span class="number">0xffff0000</span>) &#123;</span><br><span class="line">            sum &amp;= <span class="number">0xffff</span>;</span><br><span class="line">            sum++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">	<span class="keyword">delete</span> []buf;</span><br><span class="line">    <span class="keyword">return</span> ~(sum &amp; <span class="number">0xffff</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="三次握手"><a href="#三次握手" class="headerlink" title="三次握手"></a>三次握手</h3><ul>
<li><p>客户端</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">shake_hand</span><span class="params">(SOCKET *server, SOCKADDR_IN *server_addr)</span> </span>&#123;</span><br><span class="line">	<span class="type">int</span> retry_times = TCP_SYN_RETRIES;</span><br><span class="line">	<span class="type">static</span> <span class="type">int</span> esti_rtt = TIMEOUT;</span><br><span class="line">	Header header;</span><br><span class="line">	<span class="type">char</span> *send_buffer = <span class="keyword">new</span> <span class="type">char</span>[<span class="built_in">sizeof</span>(header)];</span><br><span class="line">	<span class="type">char</span> *recv_buffer = <span class="keyword">new</span> <span class="type">char</span>[<span class="built_in">sizeof</span>(header)];</span><br><span class="line">	<span class="comment">// 发送第一个SYN数据报</span></span><br><span class="line">	header.<span class="built_in">set_args</span>(<span class="number">0</span>, <span class="number">0</span>, SYN, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">	<span class="built_in">memcpy</span>(send_buffer, (<span class="type">char</span>*)&amp;header, <span class="built_in">sizeof</span>(header));</span><br><span class="line">	<span class="type">int</span> chksum = <span class="built_in">checksum</span>(send_buffer, <span class="built_in">sizeof</span>(header));</span><br><span class="line">	((Header*)send_buffer)-&gt;checksum = chksum;</span><br><span class="line">	<span class="type">int</span> res = <span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(header), <span class="number">0</span>, (sockaddr*)server_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">	<span class="keyword">if</span> (res == SOCKET_ERROR) &#123;</span><br><span class="line">		<span class="keyword">delete</span> []send_buffer;</span><br><span class="line">		<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;向服务器发送SYN报文，请求建连&quot;</span> &lt;&lt; endl;</span><br><span class="line">	<span class="type">clock_t</span> start = <span class="built_in">clock</span>();</span><br><span class="line">	<span class="comment">// 非阻塞模式</span></span><br><span class="line">	u_long mode = <span class="number">1</span>;</span><br><span class="line">	<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">	SOCKADDR_IN serv_addr;</span><br><span class="line">	<span class="type">int</span> serv_addr_len = <span class="built_in">sizeof</span>(SOCKADDR_IN);</span><br><span class="line">	<span class="comment">// 等待第二次握手信息</span></span><br><span class="line">	<span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">		<span class="keyword">while</span> (<span class="built_in">recvfrom</span>(*server, recv_buffer, <span class="built_in">sizeof</span>(header), <span class="number">0</span>, (sockaddr*)&amp;serv_addr, &amp;serv_addr_len) &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">			<span class="keyword">if</span> (<span class="built_in">clock</span>() - start &gt; <span class="number">1.2</span> * esti_rtt) &#123;</span><br><span class="line">				<span class="keyword">if</span> (retry_times &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">					cout &lt;&lt; <span class="string">&quot;超过最大握手重传次数&quot;</span>;</span><br><span class="line">					mode = <span class="number">0</span>;</span><br><span class="line">					<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">					<span class="keyword">delete</span> []send_buffer;</span><br><span class="line">					<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">					<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(header), <span class="number">0</span>, (sockaddr*)server_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">				start = <span class="built_in">clock</span>();</span><br><span class="line">				cout &lt;&lt; <span class="string">&quot;超时，重发第一次握手信息&quot;</span> &lt;&lt; endl;</span><br><span class="line">				esti_rtt += CLOCKS_PER_SEC;</span><br><span class="line">				retry_times--;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="built_in">memcpy</span>(&amp;header, recv_buffer, <span class="built_in">sizeof</span>(header));</span><br><span class="line">		chksum = <span class="built_in">checksum</span>(recv_buffer, <span class="built_in">sizeof</span>(header));</span><br><span class="line">		<span class="keyword">if</span> ((header.flag &amp; ACK) &amp;&amp; (header.flag &amp; SYN) &amp;&amp; chksum == <span class="number">0</span>) &#123;</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;接收到服务器第二次握手信息SYN = 1,ACK = 1&quot;</span> &lt;&lt; endl;</span><br><span class="line">			<span class="comment">// 发送第三次握手信息</span></span><br><span class="line">			header.<span class="built_in">set_args</span>(<span class="number">0</span>, <span class="number">0</span>, ACK, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">			<span class="built_in">memcpy</span>(send_buffer, (<span class="type">char</span>*)&amp;header, <span class="built_in">sizeof</span>(header));</span><br><span class="line">			chksum = <span class="built_in">checksum</span>(send_buffer, <span class="built_in">sizeof</span>(header));</span><br><span class="line">			((Header*)send_buffer)-&gt;checksum = chksum;</span><br><span class="line">			<span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(header), <span class="number">0</span>, (sockaddr*)server_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;向服务端发送ACK报文&quot;</span> &lt;&lt; endl;</span><br><span class="line">			<span class="comment">// 结束</span></span><br><span class="line">			mode = <span class="number">0</span>;</span><br><span class="line">			<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">			<span class="keyword">delete</span> []send_buffer;</span><br><span class="line">			<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">			<span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 阻塞模式</span></span><br><span class="line">	mode = <span class="number">0</span>;</span><br><span class="line">	<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">	<span class="keyword">delete</span> []send_buffer;</span><br><span class="line">	<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>服务端</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">shake_hand</span><span class="params">(SOCKET *server, SOCKADDR_IN *server_addr, <span class="type">char</span> *data_buffer, <span class="type">int</span> *len)</span> </span>&#123;</span><br><span class="line">	<span class="type">int</span> retry_times = TCP_SYNACK_RETRIES;</span><br><span class="line">	<span class="type">int</span> esti_rtt = TIMEOUT;</span><br><span class="line">	Header header;</span><br><span class="line">	<span class="type">char</span> *send_buffer = <span class="keyword">new</span> <span class="type">char</span>[<span class="built_in">sizeof</span>(header)];</span><br><span class="line">	<span class="type">char</span> *recv_buffer = <span class="keyword">new</span> <span class="type">char</span>[<span class="built_in">sizeof</span>(header) + MSS];</span><br><span class="line">	SOCKADDR_IN client_addr;</span><br><span class="line">	<span class="type">int</span> client_addr_length = <span class="built_in">sizeof</span>(SOCKADDR_IN);</span><br><span class="line">	<span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">		<span class="type">int</span> res = <span class="built_in">recvfrom</span>(*server, recv_buffer, <span class="built_in">sizeof</span>(header), <span class="number">0</span>, (sockaddr*)&amp;client_addr, &amp;client_addr_length);</span><br><span class="line">		<span class="keyword">if</span> (res == SOCKET_ERROR) &#123;</span><br><span class="line">			cout &lt;&lt; <span class="built_in">strerror</span>(errno);</span><br><span class="line">			<span class="built_in">Sleep</span>(<span class="number">2000</span>);</span><br><span class="line">			<span class="keyword">continue</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// 收到来自客户端的建连请求</span></span><br><span class="line">		<span class="built_in">memcpy</span>(&amp;header, recv_buffer, <span class="built_in">sizeof</span>(header));</span><br><span class="line">		<span class="type">int</span> chksum = <span class="built_in">checksum</span>(recv_buffer, <span class="built_in">sizeof</span>(header));</span><br><span class="line">		<span class="keyword">if</span> (chksum == <span class="number">0</span> &amp;&amp; (header.flag &amp; SYN)) &#123;</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;接收到来自客户端的SYN建连请求报文&quot;</span> &lt;&lt; endl;</span><br><span class="line">			header.<span class="built_in">set_args</span>(<span class="number">0</span>, <span class="number">0</span>, ACK + SYN, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">			<span class="built_in">memcpy</span>(send_buffer, (<span class="type">char</span>*)&amp;header, <span class="built_in">sizeof</span>(header));</span><br><span class="line">			chksum = <span class="built_in">checksum</span>(send_buffer, <span class="built_in">sizeof</span>(header));</span><br><span class="line">			((Header*)send_buffer)-&gt;checksum = chksum;</span><br><span class="line">			<span class="comment">// 发送第二次握手信息给客户端</span></span><br><span class="line">			<span class="type">int</span> n = <span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(header), <span class="number">0</span>, (sockaddr*)&amp;client_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">			<span class="keyword">if</span> (n == SOCKET_ERROR) &#123;</span><br><span class="line">				<span class="keyword">delete</span> []send_buffer;</span><br><span class="line">				<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">				<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">			&#125;</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;发回给客户端SYN、ACK报文&quot;</span> &lt;&lt; endl;</span><br><span class="line">			<span class="comment">// 非阻塞模式</span></span><br><span class="line">			u_long mode = <span class="number">1</span>;</span><br><span class="line">			<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">			<span class="type">clock_t</span> start = <span class="built_in">clock</span>();</span><br><span class="line">			<span class="comment">// 等待接收第三次握手信息</span></span><br><span class="line">			<span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">				<span class="type">int</span> result;</span><br><span class="line">				<span class="keyword">while</span> ((result = <span class="built_in">recvfrom</span>(*server, recv_buffer, <span class="built_in">sizeof</span>(header) + MSS, <span class="number">0</span>, (sockaddr*)&amp;client_addr, &amp;client_addr_length)) &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">					<span class="keyword">if</span> (<span class="built_in">clock</span>() - start &gt; <span class="number">1.2</span> * esti_rtt) &#123;</span><br><span class="line">						<span class="keyword">if</span> (retry_times &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">							cout &lt;&lt; <span class="string">&quot;超过SYN、ACK报文重传上限，握手失败&quot;</span> &lt;&lt; endl;</span><br><span class="line">							<span class="comment">// 超过大重传次数</span></span><br><span class="line">							<span class="keyword">delete</span> []send_buffer;</span><br><span class="line">							<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">							mode = <span class="number">0</span>;</span><br><span class="line">							<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">							<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">						&#125;</span><br><span class="line">						<span class="type">int</span> n = <span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(header), <span class="number">0</span>, (sockaddr*)&amp;client_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">						cout &lt;&lt; <span class="string">&quot;等待ACK超时，重发SYN、ACK报文&quot;</span> &lt;&lt; endl;</span><br><span class="line">						retry_times--;</span><br><span class="line">						esti_rtt += CLOCKS_PER_SEC;</span><br><span class="line">						start = <span class="built_in">clock</span>();</span><br><span class="line">					&#125;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="built_in">memcpy</span>(&amp;header, recv_buffer, <span class="built_in">sizeof</span>(header));</span><br><span class="line">				chksum = <span class="built_in">checksum</span>(recv_buffer, result);</span><br><span class="line">				<span class="keyword">if</span> (chksum == <span class="number">0</span> &amp;&amp; (header.flag &amp; ACK)) &#123;</span><br><span class="line">					<span class="comment">// 成功收到第三次握手信息</span></span><br><span class="line">					<span class="keyword">delete</span> []send_buffer;</span><br><span class="line">					<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">					mode = <span class="number">0</span>;</span><br><span class="line">					<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">					cout &lt;&lt; <span class="string">&quot;接收到来自客户端的ACK包，握手成功&quot;</span> &lt;&lt; endl;</span><br><span class="line">					<span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">else</span> <span class="keyword">if</span> (chksum == <span class="number">0</span> &amp;&amp; (header.flag &amp; SYN)) &#123;</span><br><span class="line">					start = <span class="built_in">clock</span>();</span><br><span class="line">					<span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(header), <span class="number">0</span>, (sockaddr*)&amp;client_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="comment">// 如果接收到了数据报文，发回给客户端一个rst报文</span></span><br><span class="line">				<span class="keyword">else</span> <span class="keyword">if</span> (chksum == <span class="number">0</span> &amp;&amp; header.flag == <span class="number">0</span>)&#123;</span><br><span class="line">					Header reply_header;</span><br><span class="line">					reply_header.<span class="built_in">set_args</span>(<span class="number">0</span>, <span class="number">0</span>, RST, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">					<span class="built_in">memcpy</span>(send_buffer, (<span class="type">char</span>*)&amp;reply_header, <span class="built_in">sizeof</span>(reply_header));</span><br><span class="line">					chksum = <span class="built_in">checksum</span>(send_buffer, <span class="built_in">sizeof</span>(reply_header));</span><br><span class="line">					((Header*)send_buffer)-&gt;checksum = chksum;</span><br><span class="line">					<span class="type">int</span> n = <span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(reply_header), <span class="number">0</span>, (sockaddr*)&amp;client_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">					cout &lt;&lt; <span class="string">&quot;接收到来自客户端的数据，发送RST报文，握手失败&quot;</span> &lt;&lt; endl;</span><br><span class="line">					<span class="keyword">delete</span> []send_buffer;</span><br><span class="line">					<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">					mode = <span class="number">0</span>;</span><br><span class="line">					<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">					<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">				&#125; </span><br><span class="line">				<span class="comment">// 真的嘎了</span></span><br><span class="line">				<span class="keyword">else</span> &#123;</span><br><span class="line">					<span class="keyword">delete</span> []send_buffer;</span><br><span class="line">					<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">					mode = <span class="number">0</span>;</span><br><span class="line">					<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">					<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h3 id="传输数据"><a href="#传输数据" class="headerlink" title="传输数据"></a>传输数据</h3><ul>
<li><p>发送单个数据包</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">rdt_send</span><span class="params">(SOCKET *server, SOCKADDR_IN *server_addr, <span class="type">char</span> *msg, <span class="type">int</span> len, <span class="type">bool</span> last = <span class="literal">false</span>)</span> </span>&#123;</span><br><span class="line">	<span class="built_in">assert</span>(len &lt;= MSS);</span><br><span class="line">	<span class="type">bool</span> result = <span class="literal">true</span>;</span><br><span class="line">	<span class="comment">// 设置一下header</span></span><br><span class="line">	<span class="type">static</span> <span class="type">int</span> esti_rtt = TIMEOUT;</span><br><span class="line">	<span class="type">int</span> flag = <span class="number">0</span>;</span><br><span class="line">	flag = last ? LAS : <span class="number">0</span>;</span><br><span class="line">	Header header;</span><br><span class="line">	header.<span class="built_in">set_args</span>(seq_num, <span class="number">0</span>, flag, <span class="number">0</span>, len);</span><br><span class="line">	<span class="comment">// 发送缓冲区</span></span><br><span class="line">	<span class="type">char</span> *send_buffer = <span class="keyword">new</span> <span class="type">char</span>[<span class="built_in">sizeof</span>(header) + len];</span><br><span class="line">	<span class="built_in">memcpy</span>(send_buffer, (<span class="type">char</span> *)&amp;header, <span class="built_in">sizeof</span>(header));</span><br><span class="line">	<span class="built_in">memcpy</span>(send_buffer + <span class="built_in">sizeof</span>(header), msg, len);</span><br><span class="line">	<span class="comment">// 计算校验和</span></span><br><span class="line">	u_short chksum = <span class="built_in">checksum</span>(send_buffer, len + <span class="built_in">sizeof</span>(header));</span><br><span class="line">	((Header*)send_buffer)-&gt;checksum = chksum;</span><br><span class="line">	<span class="comment">// 发送数据</span></span><br><span class="line">	<span class="type">int</span> n = <span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(header) + len, <span class="number">0</span>, (sockaddr*)server_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;发送数据包:&quot;</span>;</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;seq: &quot;</span> &lt;&lt; header.seq &lt;&lt; <span class="string">&quot; ack: &quot;</span> &lt;&lt; header.ack &lt;&lt; <span class="string">&quot; flag: &quot;</span> &lt;&lt; header.flag &lt;&lt; <span class="string">&quot; checksum: &quot;</span> &lt;&lt; chksum &lt;&lt; <span class="string">&quot; length: &quot;</span> &lt;&lt; header.length &lt;&lt; endl;</span><br><span class="line">	<span class="comment">// 等待来ack</span></span><br><span class="line">	<span class="type">char</span> *recv_buffer = <span class="keyword">new</span> <span class="type">char</span>[<span class="built_in">sizeof</span>(header)];</span><br><span class="line">	<span class="type">clock_t</span> start = <span class="built_in">clock</span>(); <span class="comment">// 开启时钟</span></span><br><span class="line">	<span class="type">clock_t</span> begin = start;</span><br><span class="line">	<span class="type">bool</span> rep = <span class="literal">false</span>;</span><br><span class="line">	<span class="comment">// 开启非阻塞模式</span></span><br><span class="line">	u_long mode = <span class="number">1</span>;</span><br><span class="line">	<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">	<span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">		SOCKADDR_IN serv_addr;</span><br><span class="line">		<span class="type">int</span> serv_addr_len = <span class="built_in">sizeof</span>(SOCKADDR_IN);</span><br><span class="line">		<span class="keyword">while</span> (<span class="built_in">recvfrom</span>(*server, recv_buffer, <span class="built_in">sizeof</span>(header), <span class="number">0</span>, (sockaddr*)&amp;serv_addr, &amp;serv_addr_len) &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">			<span class="comment">// 如果超时了，要重传</span></span><br><span class="line">			<span class="keyword">if</span> (<span class="built_in">clock</span>() - start &gt; <span class="number">1.2</span> * esti_rtt) &#123;</span><br><span class="line">				<span class="type">int</span> n = <span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(header) + len, <span class="number">0</span>, (sockaddr*)server_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">				cout &lt;&lt; <span class="string">&quot;超时重传数据包&quot;</span> &lt;&lt; endl;</span><br><span class="line">				start = <span class="built_in">clock</span>();</span><br><span class="line">				rep = <span class="literal">true</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// 接收到来自服务器的报文</span></span><br><span class="line">		<span class="built_in">memcpy</span>(&amp;header, recv_buffer, <span class="built_in">sizeof</span>(header));</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;接收到来自服务器的报文,头部为: &quot;</span>;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;seq: &quot;</span> &lt;&lt; header.seq &lt;&lt; <span class="string">&quot; ack: &quot;</span> &lt;&lt; header.ack &lt;&lt; <span class="string">&quot; flag: &quot;</span> &lt;&lt; header.flag &lt;&lt; <span class="string">&quot; checksum: &quot;</span> &lt;&lt; header.checksum &lt;&lt; <span class="string">&quot; length: &quot;</span> &lt;&lt; header.length &lt;&lt; endl;</span><br><span class="line">		chksum = <span class="built_in">checksum</span>(recv_buffer, <span class="built_in">sizeof</span>(header));</span><br><span class="line">		<span class="keyword">if</span> (chksum == <span class="number">0</span> &amp;&amp; (header.flag &amp; ACK) &amp;&amp; header.ack == seq_num) &#123;</span><br><span class="line">			<span class="keyword">if</span> (rep == <span class="literal">false</span>) </span><br><span class="line">				esti_rtt = <span class="number">0.8</span> * esti_rtt + <span class="number">0.2</span> * (<span class="built_in">clock</span>() - begin);</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;服务器接收成功&quot;</span> &lt;&lt; endl;</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> <span class="keyword">if</span> (chksum == <span class="number">0</span> &amp;&amp; (header.flag &amp; RST)) &#123;</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;连接异常&quot;</span> &lt;&lt; endl;</span><br><span class="line">			result = <span class="literal">false</span>;</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">			<span class="keyword">continue</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 改回阻塞模式</span></span><br><span class="line">	mode = <span class="number">0</span>;</span><br><span class="line">	<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">	<span class="comment">// 序列号反转</span></span><br><span class="line">	seq_num ^= <span class="number">1</span>;</span><br><span class="line">	<span class="keyword">delete</span> []send_buffer;</span><br><span class="line">	<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">	<span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>发送文件以及结束标志</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">send</span><span class="params">(SOCKET&amp; socketClient, SOCKADDR_IN&amp; servAddr, <span class="type">int</span>&amp; servAddrlen, <span class="type">char</span>* message, <span class="type">int</span> len)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> packagenum = len / MAXSIZE + (len % MAXSIZE != <span class="number">0</span>);</span><br><span class="line">    <span class="type">int</span> seqnum = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; packagenum; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">send_package</span>(socketClient, servAddr, servAddrlen, message + i * MAXSIZE, i == packagenum - <span class="number">1</span>? len - (packagenum - <span class="number">1</span>) * MAXSIZE : MAXSIZE, seqnum);</span><br><span class="line">        seqnum++;</span><br><span class="line">        <span class="keyword">if</span> (seqnum &gt; <span class="number">255</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            seqnum = seqnum - <span class="number">256</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//发送结束信息</span></span><br><span class="line">    HEADER header;</span><br><span class="line">    <span class="type">char</span>* Buffer = <span class="keyword">new</span> <span class="type">char</span>[<span class="built_in">sizeof</span>(header)];</span><br><span class="line">    header.flag = OVER;</span><br><span class="line">    header.sum = <span class="number">0</span>;</span><br><span class="line">    u_short temp = <span class="built_in">cksum</span>((u_short*)&amp;header, <span class="built_in">sizeof</span>(header));</span><br><span class="line">    header.sum = temp;</span><br><span class="line">    <span class="built_in">memcpy</span>(Buffer, &amp;header, <span class="built_in">sizeof</span>(header));</span><br><span class="line">    <span class="built_in">sendto</span>(socketClient, Buffer, <span class="built_in">sizeof</span>(header), <span class="number">0</span>, (sockaddr*)&amp;servAddr, servAddrlen);</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;Send End!&quot;</span> &lt;&lt; endl;</span><br><span class="line">    <span class="type">clock_t</span> start = <span class="built_in">clock</span>();</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span> == <span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        u_long mode = <span class="number">1</span>;</span><br><span class="line">        <span class="built_in">ioctlsocket</span>(socketClient, FIONBIO, &amp;mode);</span><br><span class="line">        <span class="keyword">while</span> (<span class="built_in">recvfrom</span>(socketClient, Buffer, MAXSIZE, <span class="number">0</span>, (sockaddr*)&amp;servAddr, &amp;servAddrlen) &lt;= <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">clock</span>() - start &gt; MAX_TIME)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="type">char</span>* Buffer = <span class="keyword">new</span> <span class="type">char</span>[<span class="built_in">sizeof</span>(header)];</span><br><span class="line">                header.flag = OVER;</span><br><span class="line">                header.sum = <span class="number">0</span>;</span><br><span class="line">                u_short temp = <span class="built_in">cksum</span>((u_short*)&amp;header, <span class="built_in">sizeof</span>(header));</span><br><span class="line">                header.sum = temp;</span><br><span class="line">                <span class="built_in">memcpy</span>(Buffer, &amp;header, <span class="built_in">sizeof</span>(header));</span><br><span class="line">                <span class="built_in">sendto</span>(socketClient, Buffer, <span class="built_in">sizeof</span>(header), <span class="number">0</span>, (sockaddr*)&amp;servAddr, servAddrlen);</span><br><span class="line">                cout &lt;&lt; <span class="string">&quot;Time Out! ReSend End!&quot;</span> &lt;&lt; endl;</span><br><span class="line">                start = <span class="built_in">clock</span>();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">memcpy</span>(&amp;header, Buffer, <span class="built_in">sizeof</span>(header));<span class="comment">//缓冲区接收到信息，读取</span></span><br><span class="line">        u_short check = <span class="built_in">cksum</span>((u_short*)&amp;header, <span class="built_in">sizeof</span>(header));</span><br><span class="line">        <span class="keyword">if</span> (header.flag == OVER)</span><br><span class="line">        &#123;</span><br><span class="line">            cout &lt;&lt; <span class="string">&quot;对方已成功接收文件!&quot;</span> &lt;&lt; endl;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    u_long mode = <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">ioctlsocket</span>(socketClient, FIONBIO, &amp;mode);<span class="comment">//改回阻塞模式</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h3 id="接收数据"><a href="#接收数据" class="headerlink" title="接收数据"></a>接收数据</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">recv_file</span><span class="params">(SOCKET *server, SOCKADDR_IN *server_addr, <span class="type">char</span> *data_buffer, <span class="type">int</span> *len)</span> </span>&#123;</span><br><span class="line">	<span class="type">bool</span> file_finish = <span class="literal">false</span>;</span><br><span class="line">	SOCKADDR_IN client_addr;</span><br><span class="line">	<span class="type">int</span> client_addr_length = <span class="built_in">sizeof</span>(SOCKADDR_IN);</span><br><span class="line">	Header header;</span><br><span class="line">	<span class="comment">// 接收缓冲和发送缓冲</span></span><br><span class="line">	<span class="type">char</span> *recv_buffer = <span class="keyword">new</span> <span class="type">char</span>[MSS + <span class="built_in">sizeof</span>(header)];</span><br><span class="line">	<span class="type">char</span> *send_buffer = <span class="keyword">new</span> <span class="type">char</span>[<span class="built_in">sizeof</span>(header)];</span><br><span class="line">	<span class="built_in">memset</span>(recv_buffer, <span class="number">0</span>, MSS + <span class="built_in">sizeof</span>(header));</span><br><span class="line">	<span class="built_in">memset</span>(send_buffer, <span class="number">0</span>, <span class="built_in">sizeof</span>(header));</span><br><span class="line">	<span class="comment">// 一直接收数据</span></span><br><span class="line">	u_long mode = <span class="number">1</span>;</span><br><span class="line">	<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">	<span class="type">clock_t</span> start;</span><br><span class="line">	<span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">		<span class="type">int</span> result;</span><br><span class="line">		<span class="keyword">while</span> ((result = <span class="built_in">recvfrom</span>(*server, recv_buffer, MSS + <span class="built_in">sizeof</span>(header), <span class="number">0</span>, (sockaddr*)&amp;client_addr, &amp;client_addr_length)) &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">			<span class="comment">// 长时间没有接收到信息，且文件传完了，认为对方嘎了</span></span><br><span class="line">			<span class="keyword">if</span> (file_finish == <span class="literal">true</span> &amp;&amp; <span class="built_in">clock</span>() - start &gt; WAITTIME) &#123;</span><br><span class="line">				cout &lt;&lt; <span class="string">&quot;长时间没有接收到报文，断开连接&quot;</span>;</span><br><span class="line">				<span class="keyword">delete</span> []send_buffer;</span><br><span class="line">				<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">				mode = <span class="number">0</span>;</span><br><span class="line">				<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">				<span class="keyword">return</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// 得到数据头</span></span><br><span class="line">		<span class="built_in">memcpy</span>(&amp;header, recv_buffer, <span class="built_in">sizeof</span>(header));</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;接收到长度为&quot;</span> &lt;&lt; result &lt;&lt; <span class="string">&quot;字节的数据报,头部为: &quot;</span>;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;seq: &quot;</span> &lt;&lt; header.seq &lt;&lt; <span class="string">&quot; ack: &quot;</span> &lt;&lt; header.ack &lt;&lt; <span class="string">&quot; flag: &quot;</span> &lt;&lt; header.flag &lt;&lt; <span class="string">&quot; checksum: &quot;</span> &lt;&lt; header.checksum &lt;&lt; <span class="string">&quot; length: &quot;</span> &lt;&lt; header.length &lt;&lt; endl;</span><br><span class="line">		<span class="comment">// 先计算一下校验和</span></span><br><span class="line">		u_short chksum = <span class="built_in">checksum</span>(recv_buffer, result);</span><br><span class="line">		<span class="comment">// 坏包，重发ack</span></span><br><span class="line">		<span class="keyword">if</span> (chksum != <span class="number">0</span>) &#123;</span><br><span class="line">			Header reply_header;</span><br><span class="line">			reply_header.<span class="built_in">set_args</span>(<span class="number">0</span>, (seq_num ^ <span class="number">1</span>), ACK, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">			<span class="built_in">memcpy</span>(send_buffer, (<span class="type">char</span>*)&amp;reply_header, <span class="built_in">sizeof</span>(reply_header));</span><br><span class="line">			chksum = <span class="built_in">checksum</span>(send_buffer, <span class="built_in">sizeof</span>(reply_header));</span><br><span class="line">			((Header*)send_buffer)-&gt;checksum = chksum;</span><br><span class="line">			<span class="type">int</span> n = <span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(reply_header), <span class="number">0</span>, (sockaddr*)&amp;client_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;数据报校验和出错&quot;</span> &lt;&lt; endl;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// 挥手报文</span></span><br><span class="line">		<span class="keyword">else</span> <span class="keyword">if</span> (header.flag == FIN) &#123;</span><br><span class="line">			Header reply_header;</span><br><span class="line">			reply_header.<span class="built_in">set_args</span>(<span class="number">0</span>, (header.seq + <span class="number">1</span>) % SEQRANGE, ACK, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">			<span class="built_in">memcpy</span>(send_buffer, (<span class="type">char</span>*)&amp;reply_header, <span class="built_in">sizeof</span>(reply_header));</span><br><span class="line">			chksum = <span class="built_in">checksum</span>(send_buffer, <span class="built_in">sizeof</span>(reply_header));</span><br><span class="line">			((Header*)send_buffer)-&gt;checksum = chksum;</span><br><span class="line">			<span class="type">int</span> n = <span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(reply_header), <span class="number">0</span>, (sockaddr*)&amp;client_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;接收到FIN报文，发回ACK报文，传输结束&quot;</span> &lt;&lt; endl;</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// 正确接收到一个报文</span></span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">			<span class="comment">// 发回一个ack</span></span><br><span class="line">			Header reply_header;</span><br><span class="line">			reply_header.<span class="built_in">set_args</span>(<span class="number">0</span>, header.seq, ACK, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">			<span class="built_in">memcpy</span>(send_buffer, (<span class="type">char</span>*)&amp;reply_header, <span class="built_in">sizeof</span>(reply_header));</span><br><span class="line">			chksum = <span class="built_in">checksum</span>(send_buffer, <span class="built_in">sizeof</span>(reply_header));</span><br><span class="line">			((Header*)send_buffer)-&gt;checksum = chksum;</span><br><span class="line">			<span class="type">int</span> n = <span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(reply_header), <span class="number">0</span>, (sockaddr*)&amp;client_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;发送ACK报文:&quot;</span> &lt;&lt; <span class="string">&quot;seq: &quot;</span> &lt;&lt; reply_header.seq &lt;&lt; <span class="string">&quot; ack: &quot;</span> &lt;&lt; reply_header.ack &lt;&lt; <span class="string">&quot; flag: &quot;</span> &lt;&lt; reply_header.flag &lt;&lt; <span class="string">&quot; checksum: &quot;</span> &lt;&lt; chksum &lt;&lt; <span class="string">&quot; length: &quot;</span> &lt;&lt; reply_header.length &lt;&lt; endl;</span><br><span class="line">			<span class="comment">// 确实是当前期望接收的报文</span></span><br><span class="line">			<span class="keyword">if</span> (seq_num == header.seq)  &#123;</span><br><span class="line">				seq_num ^= <span class="number">1</span>;</span><br><span class="line">				<span class="built_in">memcpy</span>(data_buffer + *len, recv_buffer + <span class="built_in">sizeof</span>(header), header.length);</span><br><span class="line">				*len += header.length;</span><br><span class="line">				cout &lt;&lt; <span class="string">&quot;数据报成功获取&quot;</span> &lt;&lt; endl;</span><br><span class="line">				<span class="comment">// 最后一个报文到了</span></span><br><span class="line">				<span class="keyword">if</span> (header.flag &amp; LAS) &#123;</span><br><span class="line">					file_finish = <span class="literal">true</span>;</span><br><span class="line">					start = <span class="built_in">clock</span>();</span><br><span class="line">					cout &lt;&lt; <span class="string">&quot;文件接收完毕，等待客户端挥手&quot;</span> &lt;&lt; endl;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				cout &lt;&lt; <span class="string">&quot;接收到重复数据报，忽略&quot;</span> &lt;&lt; endl;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">delete</span> []send_buffer;</span><br><span class="line">	<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">	mode = <span class="number">0</span>;</span><br><span class="line">	<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="二次挥手"><a href="#二次挥手" class="headerlink" title="二次挥手"></a>二次挥手</h3><ul>
<li><p>客户端</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">wave_hand</span><span class="params">(SOCKET *server, SOCKADDR_IN *server_addr)</span> </span>&#123;</span><br><span class="line">	<span class="type">int</span> retry_times = TCP_WAV_RETRIES;</span><br><span class="line">	<span class="type">static</span> <span class="type">int</span> esti_rtt = TIMEOUT;</span><br><span class="line">	<span class="type">int</span> seq = <span class="built_in">rand</span>() % SEQRANGE;</span><br><span class="line">	Header header;</span><br><span class="line">	<span class="type">char</span> *send_buffer = <span class="keyword">new</span> <span class="type">char</span>[<span class="built_in">sizeof</span>(header)];</span><br><span class="line">	<span class="type">char</span> *recv_buffer = <span class="keyword">new</span> <span class="type">char</span>[<span class="built_in">sizeof</span>(header)];</span><br><span class="line">	header.<span class="built_in">set_args</span>(seq, <span class="number">0</span>, FIN, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">	<span class="built_in">memcpy</span>(send_buffer, (<span class="type">char</span>*)&amp;header, <span class="built_in">sizeof</span>(header));</span><br><span class="line">	<span class="type">int</span> chksum = <span class="built_in">checksum</span>(send_buffer, <span class="built_in">sizeof</span>(header));</span><br><span class="line">	((Header*)send_buffer)-&gt;checksum = chksum;</span><br><span class="line">	<span class="comment">// 发送fin报文</span></span><br><span class="line">	<span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(header), <span class="number">0</span>, (sockaddr*)server_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;开始挥手，向服务器发送FIN报文，seq为&quot;</span> &lt;&lt; seq &lt;&lt; endl;</span><br><span class="line">	<span class="comment">// 等待并超时重发</span></span><br><span class="line">	<span class="comment">// 开启非阻塞模式</span></span><br><span class="line">	SOCKADDR_IN serv_addr;</span><br><span class="line">	<span class="type">int</span> serv_addr_length = <span class="built_in">sizeof</span>(SOCKADDR_IN);</span><br><span class="line">	u_long mode = <span class="number">1</span>;</span><br><span class="line">	<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">	<span class="type">clock_t</span> start = <span class="built_in">clock</span>();</span><br><span class="line">	<span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">		<span class="keyword">while</span> (<span class="built_in">recvfrom</span>(*server, recv_buffer, <span class="built_in">sizeof</span>(header), <span class="number">0</span>, (sockaddr*)&amp;serv_addr, &amp;serv_addr_length) &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">			<span class="comment">// 超时了</span></span><br><span class="line">			<span class="keyword">if</span> (<span class="built_in">clock</span>() - start &gt; <span class="number">1.2</span> * esti_rtt) &#123;</span><br><span class="line">				<span class="comment">// 重传次数用完了，直接返回</span></span><br><span class="line">				<span class="keyword">if</span> (retry_times &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">					mode = <span class="number">0</span>;</span><br><span class="line">					<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">					<span class="keyword">delete</span> []send_buffer;</span><br><span class="line">					<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">					cout &lt;&lt; <span class="string">&quot;挥手超过重传次数，直接结束&quot;</span> &lt;&lt; endl;</span><br><span class="line">					<span class="keyword">return</span>;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(header), <span class="number">0</span>, (sockaddr*)server_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">				retry_times--;</span><br><span class="line">				esti_rtt += CLOCKS_PER_SEC;</span><br><span class="line">				start = <span class="built_in">clock</span>();</span><br><span class="line">				cout &lt;&lt; <span class="string">&quot;超时，重发FIN报文&quot;</span> &lt;&lt; endl;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="built_in">memcpy</span>(&amp;header, recv_buffer, <span class="built_in">sizeof</span>(header));</span><br><span class="line">		chksum = <span class="built_in">checksum</span>(recv_buffer, <span class="built_in">sizeof</span>(header));</span><br><span class="line">		<span class="keyword">if</span> (chksum == <span class="number">0</span> &amp;&amp; header.flag == ACK &amp;&amp; header.ack == (seq + <span class="number">1</span>) % SEQRANGE) &#123;</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;接收到来自服务器的ACK报文，挥手成功&quot;</span> &lt;&lt; endl;</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		&#125; </span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">			<span class="keyword">continue</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	mode = <span class="number">0</span>;</span><br><span class="line">	<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">	<span class="keyword">delete</span> []send_buffer;</span><br><span class="line">	<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">	<span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
</li>
<li><p>服务端</p>
<p>由于挥手过程中服务端的工作较为简单，这里将服务端的挥手过程与其数据接收函数合并到了一起。</p>
</li>
</ul>
<h2 id="实验结果展示"><a href="#实验结果展示" class="headerlink" title="实验结果展示"></a>实验结果展示</h2><h3 id="三次握手建立连接"><a href="#三次握手建立连接" class="headerlink" title="三次握手建立连接"></a>三次握手建立连接</h3><p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202211181718906.jpg" alt=""></p>
<h3 id="传输数据-1"><a href="#传输数据-1" class="headerlink" title="传输数据"></a>传输数据</h3><p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202211181721542.jpg" alt=""></p>
<h3 id="两次挥手断开连接"><a href="#两次挥手断开连接" class="headerlink" title="两次挥手断开连接"></a>两次挥手断开连接</h3><p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202211181724708.jpg" alt=""></p>
<h3 id="传输结果展示"><a href="#传输结果展示" class="headerlink" title="传输结果展示"></a>传输结果展示</h3><p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202211181726664.jpg" alt=""></p>
<p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202211181726732.jpg" alt=""></p>
<p>与原图大小、信息完全相同，传输成功！</p>
]]></content>
      <tags>
        <tag>计网</tag>
      </tags>
  </entry>
  <entry>
    <title>计网大作业三</title>
    <url>/2022/12/14/%E8%AE%A1%E7%BD%91%E5%A4%A7%E4%BD%9C%E4%B8%9A%E4%B8%89/</url>
    <content><![CDATA[<h1 id="计算机网络大作业第三部分"><a href="#计算机网络大作业第三部分" class="headerlink" title="计算机网络大作业第三部分"></a>计算机网络大作业第三部分</h1><blockquote>
<p>徐文斌 计算机科学与技术 2010234</p>
</blockquote>
<h2 id="实验要求"><a href="#实验要求" class="headerlink" title="实验要求"></a>实验要求</h2><p>在实验3-2的基础上，选择实现一种拥塞控制算法，也可以是改进的算法，完成给定测试文件的传输。</p>
<span id="more"></span>
<h2 id="实验设计"><a href="#实验设计" class="headerlink" title="实验设计"></a>实验设计</h2><h3 id="实验原理"><a href="#实验原理" class="headerlink" title="实验原理"></a>实验原理</h3><h4 id="1-网络拥塞"><a href="#1-网络拥塞" class="headerlink" title="1.网络拥塞"></a>1.网络拥塞</h4><p>主机发送的数据过多或过快，造成网络中的路由器(或其他设备)无法及时处理，从而引入时延或丢弃。</p>
<h4 id="2-拥塞控制"><a href="#2-拥塞控制" class="headerlink" title="2.拥塞控制"></a>2.拥塞控制</h4><p>在某段时间，若对网络中某一资源的需求超过了该资源所能提供的可用部分，网络性能就要变坏，这种情况就叫做网络拥塞。在计算机网络中数位链路容量（即带宽）、交换结点中的缓存和处理机等，都是网络的资源。若出现拥塞而不进行控制，整个网络的吞吐量将随输入负荷的增大而下降。</p>
<p>目标：既不造成网络严重拥塞，又能更快地传输数据 。</p>
<p>带宽探测：接收到ACK，提高传输速率；发生丢失事件，降低传输速率。</p>
<p>ACK返回：说明网络并未拥塞，可以继续提高发送速率 。</p>
<p>丢失事件：假设所有丢失是由于拥塞造成的，降低发送速率。</p>
<h4 id="3-拥塞窗口"><a href="#3-拥塞窗口" class="headerlink" title="3.拥塞窗口"></a>3.拥塞窗口</h4><p>发送方维持一个拥塞窗口cwnd的状态变量。拥塞窗口的大小取决于网络的拥塞程度，并且动态地在变化。</p>
<p>发送方控制拥塞窗口的原则是：只要网络没有出现拥塞，拥塞窗口就再增大一些，以便把更多的分组发送出去。但只要网络出现拥塞，拥塞窗口就减小一些，以减少注入到网络中的分组数。</p>
<h3 id="协议设计"><a href="#协议设计" class="headerlink" title="协议设计"></a>协议设计</h3><h4 id="1-报文格式"><a href="#1-报文格式" class="headerlink" title="1. 报文格式"></a>1. 报文格式</h4><p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202211181112058.jpg" alt=""></p>
<p>报文由10字节的定长报文首部和变长的数据部分组成。</p>
<p>首部各字段作用如下：</p>
<ul>
<li><p>0-15位为传输的数据报的序列号，序列号空间为0-65535。</p>
</li>
<li><p>16-31位为ack确认值，用于服务器对接收到的客户端的数据进行确认。</p>
</li>
<li><p>32-47位为标志位，目前只使用低5位，从低至高分别为SYN、ACK、FIN、LAS、RST，其中LAS位用于标识当前数据包是承载着所发送文件的最后一个数据包。</p>
</li>
<li><p>48-63位为校验和，用于实现差错检测。</p>
</li>
<li><p>64-79位为数据部分的长度。</p>
</li>
</ul>
<h4 id="2-滑动窗口"><a href="#2-滑动窗口" class="headerlink" title="2.滑动窗口"></a>2.滑动窗口</h4><p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212021513087.jpg" alt=""></p>
<p>如上图所示，窗口分为左边界send_base、发送边界nextseqnum和右边界，窗口大小固定。窗口左边界左侧为已经发送并得到确认的数据，左边界到发送边界的数据为已发送但未得到确认的数据，发送边界到右边界为等待发送的数据，右边界右侧为不可发送的数据。发送数据时，判断nextseqnum是否超过有边界，如果没有超过则发送序列号为nextseqnum的数据包。</p>
<h4 id="3-握手与挥手"><a href="#3-握手与挥手" class="headerlink" title="3.握手与挥手"></a>3.握手与挥手</h4><ul>
<li><p>三次握手进行连接<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202211181120061.jpg" alt=""><br>这里我使用类似于TCP的三次握手进行连接建立。首先，客户端向服务端发送请求数据包，将SYN置位。服务端接收到数据包后，向客户端发送确认数据包，数据包中SYN和ACK均置位。客户端接收到数据包后，向服务端发送数据包，将ACK置位，之后进入连接建立状态，开始向服务器发送数据。服务端接收到ACK数据包后，进入连接建立状态，开始进行数据的接收。</p>
</li>
<li><p>握手过程中丢包的处理</p>
<ol>
<li>如果客户端发送的第一个SYN数据包产生了丢失，客户端迟迟无法接收到来自服务端的SYN、ACK数据包，则客户端会对SYN数据包进行超时重传，当超过重传次数后若还未收到响应，则建立连接失败。</li>
<li>如果服务端接收到了客户端的SYN数据包，但其发回客户端的SYN、ACK数据包产生了丢失。此时，客户端仍无法接收到SYN、ACK数据包，客户端会继续重传SYN报文；而服务端则迟迟无法接收到第三次握手来自客户端的ACK数据包，此时服务器也会对SYN、ACK数据包进行超时重传，同样，当超过重传次数后若还未收到响应，则建立连接失败。</li>
<li>如果客户端发送给服务器的ACK数据包产生了丢失。此时，客户端已经接收到了来自服务器的SYN、ACK报文，并向服务器发送ACK报文后，客户端进入了建连状态，可以开始向服务器发送具体的数据。而如果服务器在等待第三次握手的ACK报文时接收到了来自客户端发送的承载着具体数据的报文，服务器会认为产生了异常的连接，其会向客户端发送一个RST数据包，表示连接异常，建立连接失败。</li>
</ol>
</li>
<li><p>两次挥手断开连接<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202211181126063.jpg" alt=""><br>挥手方面，由于在实验中，发送端和接收端是固定的，服务器不会主动向客户端发送数据，我们只需要进行两次挥手即可。首先，客户端向服务器发送数据包，将FIN置位，并将seq填充一个随机值y，由于本次序列号只用了0和1，这里是为了避免对FIN报文进行确认的ACK数据包和对普通数据报文进行确认的ACK数据包混淆。服务端接收到报文后，向客户端发送响应报文，将ACK置位，并将ack值设置为y+1，然后断开连接。客户端接收到数据包后，连接成功断开。</p>
</li>
<li><p>挥手过程中丢包的处理</p>
<ol>
<li>如果客户端向服务器发送的FIN报文产生了丢失，客户端将迟迟无法接收到来自服务器的ACK报文，其会对FIN报文进行超时重传，重传超过一定次数之后会默认此时连接产生了异常，会直接断开连接。对于服务器来说，服务器接收到LAS标志位为1的报文而进入挥手状态后，会开启一个计时器，设置一个超时时间，如果超过该时间仍未接收到来自客户端的FIN报文，服务器也会默认连接产生了异常，会直接断开连接。</li>
<li>如果服务器向客户端发送的ACK确认报文产生了丢失，此时服务器已经接收到了来自客户端的FIN报文，向客户端发送ACK报文后服务器便会断开连接。此时，客户端由于没有接收到ACK报文，会继续重传FIN报文，直到超过最大重传次数。</li>
</ol>
</li>
</ul>
<h4 id="4-数据传输"><a href="#4-数据传输" class="headerlink" title="4.数据传输"></a>4.数据传输</h4><p>对于数据的发送和接收采用课本中GBN的状态机。</p>
<p>发送端状态机：<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212031104524.png" alt=""></p>
<p>接收端状态机：<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212031104600.png" alt=""></p>
<p>数据在传输时，将一个文件封装到多个报文中进行分段传输。在传输时，发送端无需等待接收到上一个发送包的序号的ACK报文便能发送下一个数据包，可以继续发送直到已发送但未确认的数据包的数目达到窗口大小。接收端接收到了一个数据包，先进行差错校验，如果检查无误，而且该报文的序列号为接收端的期望的序列号，则会接受该报文，并向发送端发回一个对该报文序列号的ACK报文；如果该报文的序列号不是接收端期望的序列号，接收端会将该报文丢弃，并向发送端发回一个对最后一次正确收到的序列号的ACK报文。<br>发送端如果长时间没有接收到任何对已发送的数据包的ACK报文，则其会进行超时重传，将当前窗口中所有的已发送但未确认的报文重发一次。</p>
<p>在最后，发送方需要向接收端发送一个LAS=1的报文，以通知接收端文件传输结束，进入挥手阶段。</p>
<h4 id="5-拥塞控制reno算法"><a href="#5-拥塞控制reno算法" class="headerlink" title="5.拥塞控制reno算法"></a>5.拥塞控制reno算法</h4><p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212031704409.jpg" alt=""></p>
<p>三个阶段：</p>
<ul>
<li>慢启动阶段：初始拥塞窗口cwnd=1，每收到一个新的ACK，cwnd增1。当连接初始建立或超时时，进入慢启动阶段。</li>
<li>拥塞避免阶段：设置阈值ssthresh，当拥塞窗口cwnd&gt;=ssthresh时，从慢启动阶段进入拥塞避免阶段。拥塞避免阶段中，每轮rtt，cwnd增1。</li>
<li>快速恢复阶段：当接收到三次冗余ACK时，进入快速恢复阶段。快速恢复阶段中，每收到一个冗余ACK，cwnd增1。如果出现超时，进入慢启动阶段；如果接收到新的ACK，进入拥塞避免阶段。</li>
</ul>
<p>主要事件：</p>
<ul>
<li>超时：当超时发生时，ssthresh设置为cwnd/2，cwnd设置为1，并进入慢启动阶段。</li>
<li>接收到新的ACK：如果当前处于快速恢复阶段，接收到新的ACK后会进入拥塞避免阶段，cwnd=ssthresh。如果当前处于其余两个阶段，则会对cwnd进行相应的增加。</li>
<li>三次冗余ACK：接收到三次冗余ACK，ssthresh设置为cwnd/2，cwnd设置为ssthresh+3，并进入快速恢复阶段。</li>
</ul>
<h2 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h2><p>这里只展示本次实验相对于第二次实验代码的大的修改部分。由于拥塞控制主要由发送端进行，本次实验中并未对接收端代码进行改动。</p>
<h3 id="拥塞控制类"><a href="#拥塞控制类" class="headerlink" title="拥塞控制类"></a>拥塞控制类</h3><p>为了实现拥塞控制reno算法，我实现了一个congestionController拥塞控制类，具体代码如下。<br><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">congestionController</span> &#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	u_int cwnd;	<span class="comment">// 拥塞窗口</span></span><br><span class="line">	u_int ssthresh;	<span class="comment">// 阈值</span></span><br><span class="line">	u_int dupACKcount;	<span class="comment">// 冗余ACK数目</span></span><br><span class="line">	u_int step;	<span class="comment">// 进入拥塞避免阶段后，cwnd增长所需的步长，接收到step个NEW ACK后，cwnd增1</span></span><br><span class="line">	<span class="keyword">enum</span> &#123;</span><br><span class="line">		SLOWSTART,</span><br><span class="line">		CONGESTIONAVOID,</span><br><span class="line">		QUICKRECOVER</span><br><span class="line">	&#125; state;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">congestionController</span>() &#123;	<span class="comment">// 初始时，处于慢启动阶段，cwnd=1，ssthresh这里我设置为8</span></span><br><span class="line">		cwnd = <span class="number">1</span>;</span><br><span class="line">		ssthresh = INITIALSSTHRESH;	<span class="comment">// INITIALSSTHRESH是个宏，8</span></span><br><span class="line">		dupACKcount = <span class="number">0</span>;</span><br><span class="line">		state = SLOWSTART;</span><br><span class="line">	&#125;;</span><br><span class="line">	<span class="function">u_int <span class="title">getCWND</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> cwnd;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function">u_int <span class="title">getDupACK</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> dupACKcount;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">newACK</span><span class="params">()</span> </span>&#123;	<span class="comment">// 接收到一个新的ACK</span></span><br><span class="line">		dupACKcount = <span class="number">0</span>;</span><br><span class="line">		<span class="keyword">switch</span>(state) &#123;</span><br><span class="line">		<span class="keyword">case</span> SLOWSTART: &#123;</span><br><span class="line">			cwnd += <span class="number">1</span>;</span><br><span class="line">			<span class="keyword">if</span> (cwnd &gt;= ssthresh) &#123;</span><br><span class="line">				step = cwnd;</span><br><span class="line">				state = CONGESTIONAVOID;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;<span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> CONGESTIONAVOID: &#123;</span><br><span class="line">			step -= <span class="number">1</span>;</span><br><span class="line">			<span class="keyword">if</span> (step &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">				cwnd += <span class="number">1</span>;</span><br><span class="line">				step = cwnd;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;<span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> QUICKRECOVER: &#123;</span><br><span class="line">			cwnd = ssthresh;</span><br><span class="line">			state = CONGESTIONAVOID;</span><br><span class="line">			step = cwnd;</span><br><span class="line">		&#125;;<span class="keyword">break</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">timeOut</span><span class="params">()</span> </span>&#123;	<span class="comment">// 超时发生</span></span><br><span class="line">		dupACKcount = <span class="number">0</span>;</span><br><span class="line">		ssthresh = cwnd / <span class="number">2</span>;</span><br><span class="line">		cwnd = <span class="number">1</span>;</span><br><span class="line">		state = SLOWSTART;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">dupACK</span><span class="params">()</span> </span>&#123;	<span class="comment">// 接收到一个冗余ACK</span></span><br><span class="line">		dupACKcount++;</span><br><span class="line">		<span class="keyword">if</span> (dupACKcount &lt; <span class="number">3</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">switch</span>(state) &#123;</span><br><span class="line">		<span class="keyword">case</span> SLOWSTART: </span><br><span class="line">		<span class="keyword">case</span> CONGESTIONAVOID: &#123;</span><br><span class="line">			ssthresh = cwnd / <span class="number">2</span>;</span><br><span class="line">			cwnd = ssthresh + <span class="number">3</span>;</span><br><span class="line">			state = QUICKRECOVER;</span><br><span class="line">		&#125;<span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> QUICKRECOVER: &#123;</span><br><span class="line">			cwnd += <span class="number">1</span>;		   </span><br><span class="line">		&#125;<span class="keyword">break</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<h3 id="发送端数据发送函数"><a href="#发送端数据发送函数" class="headerlink" title="发送端数据发送函数"></a>发送端数据发送函数</h3><p>发送数据时，我们既需要使已发送的窗口大小小于发送窗口大小，也需要使其小于拥塞窗口大小。<br><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">rdt_send</span><span class="params">(SOCKET *server, SOCKADDR_IN *server_addr, <span class="type">char</span> *msg, <span class="type">int</span> len, <span class="type">bool</span> last = <span class="literal">false</span>)</span> </span>&#123;</span><br><span class="line">	<span class="built_in">assert</span>(len &lt;= MSS);</span><br><span class="line">	<span class="comment">// 窗口满了就阻塞</span></span><br><span class="line">	<span class="keyword">while</span> ((u_short)(nextseqnum - base) &gt;= WND || (u_short)(nextseqnum - base) &gt;= my_cong_controller.<span class="built_in">getCWND</span>()) &#123;	<span class="comment">// 还需要判断是否小于拥塞窗口大小</span></span><br><span class="line">		<span class="keyword">continue</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	Packet *packet = <span class="keyword">new</span> Packet;</span><br><span class="line">	packet-&gt;hdr.<span class="built_in">set_args</span>(nextseqnum, <span class="number">0</span>, last ? LAS : <span class="number">0</span>, <span class="number">0</span>, len); </span><br><span class="line">	<span class="built_in">memcpy</span>(packet-&gt;data, msg, len);</span><br><span class="line">	u_short chksum = <span class="built_in">checksum</span>((<span class="type">char</span>*)packet, <span class="built_in">sizeof</span>(Header) + len);</span><br><span class="line">	packet-&gt;hdr.checksum = chksum;</span><br><span class="line">	buffer_lock.<span class="built_in">lock</span>();</span><br><span class="line">	gbn_buffer.<span class="built_in">push_back</span>(packet);</span><br><span class="line">	buffer_lock.<span class="built_in">unlock</span>();</span><br><span class="line">	<span class="built_in">sendto</span>(*server, (<span class="type">char</span>*)packet, len + <span class="built_in">sizeof</span>(Header), <span class="number">0</span>, (sockaddr*)server_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">	print_lock.<span class="built_in">lock</span>();</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;向服务器发送数据包，首部为: seq:&quot;</span> &lt;&lt; packet-&gt;hdr.seq &lt;&lt; <span class="string">&quot;, ack:&quot;</span> &lt;&lt; packet-&gt;hdr.ack &lt;&lt; <span class="string">&quot;, flag:&quot;</span> &lt;&lt; packet-&gt;hdr.flag &lt;&lt; <span class="string">&quot;, checksum:&quot;</span> &lt;&lt; packet-&gt;hdr.checksum &lt;&lt; <span class="string">&quot;, len:&quot;</span> &lt;&lt; packet-&gt;hdr.length &lt;&lt; <span class="string">&quot;, 剩余发送窗口大小:&quot;</span> &lt;&lt; WND - (nextseqnum - base) - <span class="number">1</span> &lt;&lt;endl;</span><br><span class="line">	print_lock.<span class="built_in">unlock</span>();</span><br><span class="line">	<span class="keyword">if</span> (base == nextseqnum) &#123;</span><br><span class="line">		my_timer.<span class="built_in">start_timer</span>();</span><br><span class="line">	&#125;</span><br><span class="line">	nextseqnum += <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h3 id="发送端接收线程函数"><a href="#发送端接收线程函数" class="headerlink" title="发送端接收线程函数"></a>发送端接收线程函数</h3><p>修改接收线程函数，在相应事件发生时调用拥塞控制类的函数，实现拥塞窗口动态变化。<br><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">receive_thread</span><span class="params">(SOCKET *server, SOCKADDR_IN *server_addr)</span> </span>&#123;</span><br><span class="line">	<span class="comment">// 开启非阻塞模式</span></span><br><span class="line">	u_long mode = <span class="number">1</span>;</span><br><span class="line">	<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">	<span class="type">char</span> *recv_buffer = <span class="keyword">new</span> <span class="type">char</span>[<span class="built_in">sizeof</span>(Header)];</span><br><span class="line">	Header *header;</span><br><span class="line">	SOCKADDR_IN ser_addr;</span><br><span class="line">	<span class="type">int</span> ser_addr_len = <span class="built_in">sizeof</span>(SOCKADDR_IN);</span><br><span class="line">	<span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">		<span class="keyword">if</span> (send_over) &#123;</span><br><span class="line">			mode = <span class="number">0</span>;</span><br><span class="line">			<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">			<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">while</span> (<span class="built_in">recvfrom</span>(*server, recv_buffer, <span class="built_in">sizeof</span>(Header), <span class="number">0</span>, (sockaddr*)&amp;ser_addr, &amp;ser_addr_len) &lt;= <span class="number">-1</span>) &#123;</span><br><span class="line">			<span class="keyword">if</span> (send_over) &#123;</span><br><span class="line">				mode = <span class="number">0</span>;</span><br><span class="line">				<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">				<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">				<span class="keyword">return</span>;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">if</span> (my_timer.<span class="built_in">time_out</span>()) &#123;</span><br><span class="line">				my_cong_controller.<span class="built_in">timeOut</span>();	<span class="comment">// 发生了超时事件</span></span><br><span class="line">				<span class="keyword">for</span> (<span class="keyword">auto</span> packet : gbn_buffer) &#123;</span><br><span class="line">					<span class="built_in">sendto</span>(*server, (<span class="type">char</span>*)packet, <span class="built_in">sizeof</span>(Header) + packet-&gt;hdr.length, <span class="number">0</span>, (sockaddr*)server_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">					print_lock.<span class="built_in">lock</span>();</span><br><span class="line">					cout &lt;&lt; <span class="string">&quot;超时重传数据包，首部为: seq:&quot;</span> &lt;&lt; packet-&gt;hdr.seq &lt;&lt; <span class="string">&quot;, ack:&quot;</span> &lt;&lt; packet-&gt;hdr.ack &lt;&lt; <span class="string">&quot;, flag:&quot;</span> &lt;&lt; packet-&gt;hdr.flag &lt;&lt; <span class="string">&quot;, checksum:&quot;</span> &lt;&lt; packet-&gt;hdr.checksum &lt;&lt; <span class="string">&quot;, len:&quot;</span> &lt;&lt; packet-&gt;hdr.length &lt;&lt; endl;</span><br><span class="line">					print_lock.<span class="built_in">unlock</span>();</span><br><span class="line">				&#125;</span><br><span class="line">				my_timer.<span class="built_in">start_timer</span>();</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		header = (Header*)recv_buffer;</span><br><span class="line">		<span class="type">int</span> chksum = <span class="built_in">checksum</span>(recv_buffer, <span class="built_in">sizeof</span>(Header));</span><br><span class="line">		<span class="keyword">if</span> (chksum != <span class="number">0</span>) &#123;</span><br><span class="line">			<span class="keyword">continue</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (header-&gt;flag == RST) &#123;</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;连接异常，退出程序&quot;</span> &lt;&lt; endl;</span><br><span class="line">			<span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> <span class="keyword">if</span> (header-&gt;flag == ACK) &#123;</span><br><span class="line">			<span class="keyword">if</span> (header-&gt;ack &gt;= base) &#123;</span><br><span class="line">				my_cong_controller.<span class="built_in">newACK</span>();	<span class="comment">// 接收到新的ACK</span></span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">else</span> &#123;</span><br><span class="line">				my_cong_controller.<span class="built_in">dupACK</span>();	<span class="comment">// 接收到重复ACk</span></span><br><span class="line">				<span class="keyword">if</span> (my_cong_controller.<span class="built_in">getDupACK</span>() == <span class="number">3</span>) &#123; <span class="comment">// 如果收到了3个重复ACK，这里会快速重传base处的数据包</span></span><br><span class="line">					Packet *packet = gbn_buffer[<span class="number">0</span>];</span><br><span class="line">					<span class="built_in">sendto</span>(*server, (<span class="type">char</span>*)packet, <span class="built_in">sizeof</span>(Header) + packet-&gt;hdr.length, <span class="number">0</span>, (sockaddr*)server_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">					print_lock.<span class="built_in">lock</span>();</span><br><span class="line">					cout &lt;&lt; <span class="string">&quot;接收到三个重复ACK，快速重传数据包，首部为: seq:&quot;</span> &lt;&lt; packet-&gt;hdr.seq &lt;&lt; <span class="string">&quot;, ack:&quot;</span> &lt;&lt; packet-&gt;hdr.ack &lt;&lt; <span class="string">&quot;, flag:&quot;</span> &lt;&lt; packet-&gt;hdr.flag &lt;&lt; <span class="string">&quot;, checksum:&quot;</span> &lt;&lt; packet-&gt;hdr.checksum &lt;&lt; <span class="string">&quot;, len:&quot;</span> &lt;&lt; packet-&gt;hdr.length &lt;&lt; endl;</span><br><span class="line">					print_lock.<span class="built_in">unlock</span>();</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="type">int</span> recv_num = header-&gt;ack + <span class="number">1</span> - base;</span><br><span class="line">			<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; recv_num; i++) &#123;</span><br><span class="line">				buffer_lock.<span class="built_in">lock</span>();</span><br><span class="line">				<span class="keyword">if</span> (gbn_buffer.<span class="built_in">size</span>() &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">					<span class="keyword">break</span>;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">delete</span> gbn_buffer[<span class="number">0</span>];</span><br><span class="line">				gbn_buffer.<span class="built_in">erase</span>(gbn_buffer.<span class="built_in">begin</span>());</span><br><span class="line">				buffer_lock.<span class="built_in">unlock</span>();</span><br><span class="line">			&#125;</span><br><span class="line">			base = header-&gt;ack + <span class="number">1</span>;</span><br><span class="line">			print_lock.<span class="built_in">lock</span>();</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;接收到来自服务器的数据包，首部为: seq:&quot;</span> &lt;&lt; header-&gt;seq &lt;&lt; <span class="string">&quot;, ack:&quot;</span> &lt;&lt; header-&gt;ack &lt;&lt; <span class="string">&quot;, flag:&quot;</span> &lt;&lt; header-&gt;flag &lt;&lt; <span class="string">&quot;, checksum:&quot;</span> &lt;&lt; header-&gt;checksum &lt;&lt; <span class="string">&quot;, len:&quot;</span> &lt;&lt; header-&gt;length &lt;&lt; <span class="string">&quot;, 拥塞窗口大小:&quot;</span> &lt;&lt; my_cong_controller.<span class="built_in">getCWND</span>() &lt;&lt; endl;</span><br><span class="line">			print_lock.<span class="built_in">unlock</span>();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (base != nextseqnum) &#123;</span><br><span class="line">			my_timer.<span class="built_in">start_timer</span>();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">			my_timer.<span class="built_in">stop_timer</span>();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="实验结果展示"><a href="#实验结果展示" class="headerlink" title="实验结果展示"></a>实验结果展示</h2><p>握手与挥手和实验前两部分一致，在此略过。</p>
<h3 id="数据传输"><a href="#数据传输" class="headerlink" title="数据传输"></a>数据传输</h3><h3 id="传输结果"><a href="#传输结果" class="headerlink" title="传输结果"></a>传输结果</h3><p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212021611641.jpg" alt=""></p>
<p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212021612400.jpg" alt=""></p>
<p>如上图所示，传输结果与原图大小、信息完全相同，传输成功。</p>
]]></content>
      <tags>
        <tag>计网</tag>
      </tags>
  </entry>
  <entry>
    <title>硬件预取</title>
    <url>/2022/12/15/%E7%A1%AC%E4%BB%B6%E9%A2%84%E5%8F%96/</url>
    <content><![CDATA[<p><center>硬件预取</center><br><span id="more"></span></p>
<h1 id="硬件预取"><a href="#硬件预取" class="headerlink" title="硬件预取"></a>硬件预取</h1><p>体系结构期末大作业要我们用ChampSim模拟器来实现L2Cache的预取和LLCache的替换算法。之前对于硬件的预取没有太多的了解。这段时间学习一下，记录一下学习收获啥的，有一部分摘自网络。</p>
<p>下面给出具体参考的网站以及参考文献：</p>
<p><a href="https://zhuanlan.zhihu.com/p/491673969">https://zhuanlan.zhihu.com/p/491673969</a></p>
<p>Nesbit, K. J., &amp; Smith, J. E. (2004, February). Data cache prefetching using a global history buffer.<br>In 10th International Symposium on High Performance Computer Architecture (HPCA’04) (pp. 96-96).<br>IEEE.</p>
<h2 id="为啥要预取"><a href="#为啥要预取" class="headerlink" title="为啥要预取"></a>为啥要预取</h2><p>处理器和内存之间的性能差异越来越大导致的。两者发展的侧重点不同，CPU主要是要快，往更快执行任务发展；而内存的发展有些偏向于增加内存的密度，让内存变得更大。1985年至2010年间，CPU的性能提升的数千倍，可是内存相关的性能只提升了不到10倍。如果等CPU需要执行相关指令或者需要修改数据的时候再从内存中去读取，那么大部分时间都会花费在等待数据上，这是不可容忍的。这时预取的重要性就体现了，将将要访问的内容提前从内存搬移到Cache中，CPU就可以即时拿到所需的内容，避免了等待。当然，如果预取做得不好，是有可能导致性能下降的，由于Cache的大小是很宝贵的，如果预取判断出错，预取的是无用的数据，然后反而把Cache中后续有可能还会用到的数据给Evict了，那么会增加系统的功耗，减低性能。</p>
<h2 id="硬件预取策略关注点"><a href="#硬件预取策略关注点" class="headerlink" title="硬件预取策略关注点"></a>硬件预取策略关注点</h2><h3 id="考虑点"><a href="#考虑点" class="headerlink" title="考虑点"></a>考虑点</h3><p>想要消除内存性能与CPU性能的巨大的gap，相关的预取策略必须考虑以下三点：</p>
<ul>
<li>预取哪块数据：必须能准确地判断所需预取的数据，这个很好理解，无效数据对我们毫无用处，只会白白耗费电源，还有可能将缓存中有用的数据给踢出，从而导致性能下降。</li>
<li>何时开始预取：如果预取不及时，甚至晚于需要相关数据的节点，那么这个策略毫无帮助，浪费硬件资源，预取过早亦同理。</li>
<li>数据存放在哪：如果相关预取数据存放不合理，将会将后续需要用到的数据踢出CPU，造成性能下降。</li>
</ul>
<h3 id="两个关键指标"><a href="#两个关键指标" class="headerlink" title="两个关键指标"></a>两个关键指标</h3><p>在评价一个预取策略的好坏时，我们一般会从覆盖率和准确性两个维度来评判，好的预取策略必须同时在这两个指标上面有亮眼的成绩。</p>
<ul>
<li>覆盖率很好理解，覆盖率就是引入预取策略消除的Cache Miss次数与引入预取策略前的总的Cache Miss次数的比值。比如没有预取机制的时候会发生100次Cache Miss，然后引入预取将Cache Miss减少到了30次，意味着该预取策略成功避免了70次Cache Miss，即覆盖率为70/100=70%。<img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212142131261.jpg" alt=""></li>
<li>预取的正确率还需要考虑在这个过程中预取机制发出了几次预取操作，接着上面的例子，如果该预取机制总共发出了140次的预取操作，其中70次是有效的预取操作，那么正确率为70/140=50%。<img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212142131110.jpg" alt=""></li>
</ul>
<p>显然，覆盖率和准确性也是相互制约的。覆盖率的提高倾向于有更多的预取次数，而准确性的保证的限制了预取次数不能太高。我们应该在追求覆盖率的同时，尽量保证准确性。</p>
<h3 id="及时性"><a href="#及时性" class="headerlink" title="及时性"></a>及时性</h3><p>预取的及时性也是很需要考虑的一个指标。假设我们正确的判断出了要预取的数据，但是如果预取的时机不合适，也有可能适得其反。如果过早预取，则预取到的数据有可能在需要之前就被踢出缓存，还有可能占用缓存空间，造成Cache Miss的增加；如果过晚预取，则可能会造成较大的延时，CPU需要等待预取后才能继续执行。</p>
<h2 id="硬件预取分类"><a href="#硬件预取分类" class="headerlink" title="硬件预取分类"></a>硬件预取分类</h2><p>显然，程序运行时既需要指令又需要数据，因此硬件预取分为指令预取和数据预取两部分。</p>
<h3 id="指令预取"><a href="#指令预取" class="headerlink" title="指令预取"></a>指令预取</h3><p>程序执行中，指令往往顺序执行，虽然有跳转等等一些指令，但是指令的访问模式相对来说还是比较好预测的。</p>
<h3 id="数据预取"><a href="#数据预取" class="headerlink" title="数据预取"></a>数据预取</h3><p>相对来说，数据预取更加难以预测，因为程序对于数据的访问模式完全可以是随机的。因此，数据预取比较难做，也更加的有意思。</p>
<h2 id="指令预取机制"><a href="#指令预取机制" class="headerlink" title="指令预取机制"></a>指令预取机制</h2><h3 id="next-line-prefetcher"><a href="#next-line-prefetcher" class="headerlink" title="next-line prefetcher"></a>next-line prefetcher</h3><p>最简单的指令预取机制，维护了一个叫stream buffer的预取buffer，每次CPU请求一个缓存行时，会从下一级Cache中将下一个缓存行读取到stream buffer中。只在指令顺序执行的时候表现比较好，当遇到分支或者函数调用时，预测效果可能会较差。</p>
<p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212142157923.jpg" alt=""></p>
<h3 id="FDIP-Fetch-directed-instruction-prefetching-prefetcher"><a href="#FDIP-Fetch-directed-instruction-prefetching-prefetcher" class="headerlink" title="FDIP(Fetch-directed instruction prefetching) prefetcher"></a>FDIP(Fetch-directed instruction prefetching) prefetcher</h3><p>这种预取机制针对分支指令进行了处理，大概思路就是预取指令分支预测的结果。该机制在分支预测模块和取指模块之间插入了一个FTQ的模块，然后会经过一个filter把一部分地址过滤掉（比如已经存在ICache中的地址），然后进入到PIQ中，这时就会将PIQ中的地址发往下一级Cache，然后将取回的Cache Line放入一个全相联的Cache中。思考这种机制，它在分支预测之后预取，如果处理不好，感觉预取的速度跟不上取指的速度，就会带来一定的延迟。</p>
<p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212142236486.jpg" alt=""></p>
<h3 id="discontinuity"><a href="#discontinuity" class="headerlink" title="discontinuity"></a>discontinuity</h3><p>不连续预取机制，我理解的是，这种机制就是把每次分支指令跳转的目标给记录一下，记录到一张表中。当下次再次访问到该分支指令时，在取指阶段查表，然后预取相应的缓存行就行，到分支目标指令取值时，指令已经被预取到缓存中。</p>
<p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212142248807.jpg" alt=""></p>
<p>还有好多不同的指令预取机制，一口气也看不完，之后慢慢再学吧。</p>
<h2 id="数据预取机制"><a href="#数据预取机制" class="headerlink" title="数据预取机制"></a>数据预取机制</h2><h3 id="传统基于表的预取"><a href="#传统基于表的预取" class="headerlink" title="传统基于表的预取"></a>传统基于表的预取</h3><p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212201728018.jpg" alt=""></p>
<p>如上图所示，传统的基于表的预取机制通常使用一个表来记录历史信息，并根据表中记录的信息来指导预取的进行。表由键来索引，键常常是PC值或者访问数据的地址。这种预取机制分为两种方式，步幅预取(stride prefetching)和关联预取(correlation prefetching)。(不知道翻译的对不对，很好理解，步幅预取是根据访问历史记录的地址间的步长信息来进行预取，而关联预取则是根据不同地址之间出现的相关性来进行预取)</p>
<h4 id="Stride-Prefetching"><a href="#Stride-Prefetching" class="headerlink" title="Stride Prefetching"></a>Stride Prefetching</h4><p>传统的步幅预取使用一个表来存储关于单条load指令的与步幅相关的历史信息。因此，它使用load指令的PC值来索引历史表。每个表项记录对应的load指令的最近访存时发生缓存缺失的两个地址之间的<strong>步幅</strong>以及最近的一个发生缓存缺失的<strong>访存地址</strong>。当发生缓存缺失时，预取机制会使用当前的load指令的PC值来索引历史表，并将当前load指令的访存地址addr与表项中记录的最近一次的缓存缺失地址相减。得到的结果s如果与表项中记录的步幅一致，则这个时候预取器会感觉未来很有可能在$addr+d*s$的地址处也发生缓存缺失，此时，它便会向更低一级的cache发出预取请求，请求的地址就是$addr+s$、$addr+2s$……、$addr+ds$，其中，d就叫做预取度。</p>
<h4 id="Correlation-Prefetching"><a href="#Correlation-Prefetching" class="headerlink" title="Correlation Prefetching"></a>Correlation Prefetching</h4><p>比起步幅预取来说，关联预取可以处理更加复杂的地址访问模式。马尔可夫预取(Markov Prefetch)是一个使用历史表的关联预取算法。其中，预取键是导致缓存缺失的访问地址。历史表中的每个表项存储了历史中紧跟在对应预取键后的导致缓存缺失的访问地址的链表。当再次发生缓存缺失时，预取算法根据访存地址来索引历史表，然后从表项的链表中找出若干个置信度比较高的地址给预取了。(这里置信度高指的是未来更加有可能导致缓存缺失，比如我们可以预取发生缓存缺失时间上离预取键最近的那个访存地址)</p>
<p>马尔可夫预取还有一个推广，叫做距离预取(Distance Prefetch)。从上边的描述我们不难发现，马尔可夫预取与导致缓存缺失的地址的具体值有较大的联系(后者是前者历史表的索引)。距离预取则是弱化了这种联系，其使用地址增量(address delta)————两个连续导致缓存缺失的访存地址之间的距离作为历史表的预取键。每个历史表条目都包含一个增量列表，这些增量在过去出现在预取键对应的增量之后。当发生缓存缺失时，预取算法将当前地址加上查表得到的增量后的地址预取到缓存中。</p>
<h3 id="GHB预取机制"><a href="#GHB预取机制" class="headerlink" title="GHB预取机制"></a>GHB预取机制</h3><p>传统的表预取有一些缺点，比如容易有过时的数据；预取键容易造成冲突；每个表项能存储的历史信息较少等不足。然后就有人发明出了基于GHB的预取机制，将预取键的匹配和历史信息的存储给解耦了。</p>
<p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212202150882.jpg" alt=""></p>
<p>GHB预取机制的结构如上图所示，主要有两级组成：</p>
<ul>
<li>Index table. 索引表，这个表主要用于根据预取键来索引GHB缓冲区中的具体条目。这里预取键可以是PC，也可以是访存地址。</li>
<li>Global history buffer. 全局历史缓冲区，是一个循环队列。一共能存n项，其中每项存储一个地址以及一个指针。通过指针按时间顺序将一个预取键对应的地址给链起来。</li>
</ul>
<p>使用GHB也可以实现传统的步幅预取、马尔可夫预取、距离预取机制。</p>
<h4 id="GHB步幅预取"><a href="#GHB步幅预取" class="headerlink" title="GHB步幅预取"></a>GHB步幅预取</h4><p>类似于传统的基于表的步幅预取，基于GHB的步幅预取的预取键也是load指令的PC值。预取器通过预取键来索引GHB中的条目，这里我们使每个预取键对应的GHB中的条目为预取键对应的load指令的访存地址。上边说过，这些地址形成了一个链，我们只需要取出链上的最后三个地址，计算出来对应的两个步幅，如果两个步幅相等，就可以像步幅预取那样预取新的缓存行了。</p>
<h4 id="GHB关联预取"><a href="#GHB关联预取" class="headerlink" title="GHB关联预取"></a>GHB关联预取</h4><p>GHB关联预取也是类似的，它的预取键是实际的访存地址。预取键对应的GHB中的条目中存储了与预取键相同的地址。这样，当发生缓存缺失时，预取器根据缺失地址索引索引表，得到指向GHB条目的指针，然后访问得到的GHB条目链，链上每个元素在GHB中的直接后继元素即是预取候选。</p>
]]></content>
      <tags>
        <tag>体系结构</tag>
      </tags>
  </entry>
  <entry>
    <title>计网大作业二</title>
    <url>/2022/12/14/%E8%AE%A1%E7%BD%91%E5%A4%A7%E4%BD%9C%E4%B8%9A%E4%BA%8C/</url>
    <content><![CDATA[<h1 id="计算机网络大作业第二部分"><a href="#计算机网络大作业第二部分" class="headerlink" title="计算机网络大作业第二部分"></a>计算机网络大作业第二部分</h1><blockquote>
<p>徐文斌 计算机科学与技术 2010234</p>
</blockquote>
<h2 id="实验要求"><a href="#实验要求" class="headerlink" title="实验要求"></a>实验要求</h2><p>在实验3-1的基础上，将停等机制改成基于滑动窗口的流量控制机制，采用固定窗口大小，支持累积确认，完成给定测试文件的传输。</p>
<span id="more"></span>
<h2 id="实验设计"><a href="#实验设计" class="headerlink" title="实验设计"></a>实验设计</h2><h3 id="实验原理"><a href="#实验原理" class="headerlink" title="实验原理"></a>实验原理</h3><h4 id="滑动窗口"><a href="#滑动窗口" class="headerlink" title="滑动窗口"></a>滑动窗口</h4><p>滑动窗口(Sliding window)是一种流量控制技术。如果网络通信中，通信双方不会考虑网络的拥挤情况直接发送数据，由于大家不知道网络拥塞状况，同时发送数据，则会导致中间节点阻塞掉包，谁也发不了数据，所以就有了滑动窗口机制来解决此问题。 TCP中采用滑动窗口来进行传输控制，滑动窗口的大小意味着接收方还有多大的缓冲区可以用于接收数据。发送方可以通过滑动窗口的大小来确定应该发送多少字节的数据。当滑动窗口为0时，发送方一般不能再发送数据报，但有两种情况除外，一种情况是可以发送紧急数据，例如，允许用户终止在远端机上的运行进程。另一种情况是发送方可以发送一个1字节的数据报来通知接收方重新声明它希望接收的下一字节及发送方的滑动窗口大小。</p>
<h4 id="累计确认"><a href="#累计确认" class="headerlink" title="累计确认"></a>累计确认</h4><p>停等协议虽然编写简单，效果稳定，但是其效率比较低，实际中很难利用其进行大量的数据包发送，所以一般情况下，采用流水线的方式进行数据包的发送和接收。Go-Back-N协议采用累积确认的方式，GBN的关键是发送方能够在收到确认之前发送多个分组，但接收方只能缓存一个分组。发送方为发送出去的分组保留副本，直到来自接收方确认达到。</p>
<h3 id="协议设计"><a href="#协议设计" class="headerlink" title="协议设计"></a>协议设计</h3><h4 id="1-报文格式"><a href="#1-报文格式" class="headerlink" title="1. 报文格式"></a>1. 报文格式</h4><p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202211181112058.jpg" alt=""></p>
<p>报文由10字节的定长报文首部和变长的数据部分组成。</p>
<p>首部各字段作用如下：</p>
<ul>
<li><p>0-15位为传输的数据报的序列号，序列号空间为0-65535。</p>
</li>
<li><p>16-31位为ack确认值，用于服务器对接收到的客户端的数据进行确认。</p>
</li>
<li><p>32-47位为标志位，目前只使用低5位，从低至高分别为SYN、ACK、FIN、LAS、RST，其中LAS位用于标识当前数据包是承载着所发送文件的最后一个数据包。</p>
</li>
<li><p>48-63位为校验和，用于实现差错检测。</p>
</li>
<li><p>64-79位为数据部分的长度。</p>
</li>
</ul>
<h4 id="2-滑动窗口"><a href="#2-滑动窗口" class="headerlink" title="2.滑动窗口"></a>2.滑动窗口</h4><p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212021513087.jpg" alt=""></p>
<p>如上图所示，窗口分为左边界send_base、发送边界nextseqnum和右边界，窗口大小固定。窗口左边界左侧为已经发送并得到确认的数据，左边界到发送边界的数据为已发送但未得到确认的数据，发送边界到右边界为等待发送的数据，右边界右侧为不可发送的数据。发送数据时，判断nextseqnum是否超过有边界，如果没有超过则发送序列号为nextseqnum的数据包。</p>
<h4 id="3-握手与挥手"><a href="#3-握手与挥手" class="headerlink" title="3.握手与挥手"></a>3.握手与挥手</h4><p>握手与挥手和上次实验一致，在此不做过多的说明。</p>
<h4 id="4-数据传输"><a href="#4-数据传输" class="headerlink" title="4.数据传输"></a>4.数据传输</h4><p>对于数据的发送和接收采用课本中GBN的状态机。</p>
<p>发送端状态机：<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212031104524.png" alt=""></p>
<p>接收端状态机：<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212031104600.png" alt=""></p>
<p>数据在传输时，将一个文件封装到多个报文中进行分段传输。在传输时，发送端无需等待接收到上一个发送包的序号的ACK报文便能发送下一个数据包，可以继续发送直到已发送但未确认的数据包的数目达到窗口大小。接收端接收到了一个数据包，先进行差错校验，如果检查无误，而且该报文的序列号为接收端的期望的序列号，则会接受该报文，并向发送端发回一个对该报文序列号的ACK报文；如果该报文的序列号不是接收端期望的序列号，接收端会将该报文丢弃，并向发送端发回一个对最后一次正确收到的序列号的ACK报文。<br>发送端如果长时间没有接收到任何对已发送的数据包的ACK报文，则其会进行超时重传，将当前窗口中所有的已发送但未确认的报文重发一次。</p>
<p>在最后，发送方需要向接收端发送一个LAS=1的报文，以通知接收端文件传输结束，进入挥手阶段。</p>
<h2 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h2><p>这里只展示本次实验相对于第一次实验代码的大的修改部分。</p>
<h3 id="报文首部和标志位定义"><a href="#报文首部和标志位定义" class="headerlink" title="报文首部和标志位定义"></a>报文首部和标志位定义</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> MSS 6666 <span class="comment">// 最大报文段长度</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> WND 10   <span class="comment">// 窗口大小</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SYN 0x1</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> ACK 0x2</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> FIN 0x4</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> LAS 0x8</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> RST 0x10</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TCP_SYN_RETRIES 5</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TCP_WAV_RETRIES 5</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TIMEOUT (CLOCKS_PER_SEC / 2)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> pack(push)</span></span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> pack(1)</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Header</span> &#123; <span class="comment">// header结构体</span></span><br><span class="line">	u_short seq;</span><br><span class="line">	u_short ack;</span><br><span class="line">	u_short flag;</span><br><span class="line">	u_short checksum;</span><br><span class="line">	u_short length;</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">set_args</span><span class="params">(u_short seq, u_short ack, u_short flag, u_short checksum, u_short length)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;seq = seq;</span><br><span class="line">		<span class="keyword">this</span>-&gt;ack = ack;</span><br><span class="line">		<span class="keyword">this</span>-&gt;flag = flag;</span><br><span class="line">		<span class="keyword">this</span>-&gt;checksum = checksum;</span><br><span class="line">		<span class="keyword">this</span>-&gt;length = length;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> pack(pop)</span></span><br></pre></td></tr></table></figure>
<h3 id="报文结构体"><a href="#报文结构体" class="headerlink" title="报文结构体"></a>报文结构体</h3><p>由于实现了滑动串口，程序需要缓存当前已经发送但未被确认的报文。这里我定义报文结构体用来对这些未被确认的报文进行存储。<br><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">pragma</span> pack(push)</span></span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> pack(1)</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Packet</span> &#123; <span class="comment">// packet结构体</span></span><br><span class="line">	Header hdr; <span class="comment">// 报文首部</span></span><br><span class="line">	<span class="type">char</span> data[MSS]; <span class="comment">// 数据段</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> pack(pop)</span></span><br></pre></td></tr></table></figure></p>
<h3 id="计时器类"><a href="#计时器类" class="headerlink" title="计时器类"></a>计时器类</h3><p>相比于rdt3.0，GBN的计时机制更为复杂，且程序利用多线程进行编写，因此这里我编写了一个计时器类timer，用于计时功能。<br><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">timer</span> &#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	mutex mtx; 		<span class="comment">// 锁，防止出现竞争状态</span></span><br><span class="line">	<span class="type">bool</span> valid; 	<span class="comment">// 计时器是否开启</span></span><br><span class="line">	u_int esti_rtt; <span class="comment">// 超时时间</span></span><br><span class="line">	<span class="type">clock_t</span> start; 	<span class="comment">// 计时起点</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">timer</span>() : <span class="built_in">esti_rtt</span>(TIMEOUT) &#123;&#125;;</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">start_timer</span><span class="params">(u_int esti_rtt)</span> </span>&#123;	<span class="comment">// 开启计时器</span></span><br><span class="line">		mtx.<span class="built_in">lock</span>();</span><br><span class="line">		valid = <span class="literal">true</span>;</span><br><span class="line">		<span class="keyword">this</span>-&gt;esti_rtt = esti_rtt;</span><br><span class="line">		start = <span class="built_in">clock</span>();</span><br><span class="line">		mtx.<span class="built_in">unlock</span>();</span><br><span class="line">	&#125;;</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">start_timer</span><span class="params">()</span> </span>&#123;	<span class="comment">// 开启计时器</span></span><br><span class="line">		mtx.<span class="built_in">lock</span>();</span><br><span class="line">		valid = <span class="literal">true</span>;</span><br><span class="line">		start = <span class="built_in">clock</span>();</span><br><span class="line">		mtx.<span class="built_in">unlock</span>();</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">stop_timer</span><span class="params">()</span> </span>&#123;		<span class="comment">// 关闭计时器</span></span><br><span class="line">		mtx.<span class="built_in">lock</span>();</span><br><span class="line">		valid = <span class="literal">false</span>;</span><br><span class="line">		mtx.<span class="built_in">unlock</span>();</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="type">bool</span> <span class="title">time_out</span><span class="params">()</span> </span>&#123;		<span class="comment">// 判断是否超时</span></span><br><span class="line">		<span class="keyword">return</span> valid &amp;&amp; (<span class="built_in">clock</span>() - start &gt;= esti_rtt);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="type">float</span> <span class="title">remain_time</span><span class="params">()</span> </span>&#123;	<span class="comment">// 打印距离超时的剩余时间，用于调试</span></span><br><span class="line">		<span class="keyword">return</span> (esti_rtt - (<span class="built_in">clock</span>() - start)) / <span class="number">1000.0</span> &lt;= <span class="number">0</span> ? <span class="number">0</span> : (esti_rtt - (<span class="built_in">clock</span>() - start)) / <span class="number">1000.0</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<h3 id="传输数据"><a href="#传输数据" class="headerlink" title="传输数据"></a>传输数据</h3><ul>
<li>发送端发送单个数据包</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">rdt_send</span><span class="params">(SOCKET *server, SOCKADDR_IN *server_addr, <span class="type">char</span> *msg, <span class="type">int</span> len, <span class="type">bool</span> last = <span class="literal">false</span>)</span> </span>&#123;</span><br><span class="line">	<span class="built_in">assert</span>(len &lt;= MSS);</span><br><span class="line">	<span class="comment">// 窗口满了就阻塞，窗口有空余时开始发送当前数据包</span></span><br><span class="line">	<span class="keyword">while</span> (nextseqnum &gt;= base + WND) &#123;</span><br><span class="line">		<span class="keyword">continue</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	Packet *packet = <span class="keyword">new</span> Packet;</span><br><span class="line">	packet-&gt;hdr.<span class="built_in">set_args</span>(nextseqnum, <span class="number">0</span>, last ? LAS : <span class="number">0</span>, <span class="number">0</span>, len); </span><br><span class="line">	<span class="built_in">memcpy</span>(packet-&gt;data, msg, len);</span><br><span class="line">	u_short chksum = <span class="built_in">checksum</span>((<span class="type">char</span>*)packet, <span class="built_in">sizeof</span>(Header) + len);</span><br><span class="line">	packet-&gt;hdr.checksum = chksum;</span><br><span class="line">	buffer_lock.<span class="built_in">lock</span>();</span><br><span class="line">	gbn_buffer.<span class="built_in">push_back</span>(packet);	<span class="comment">// 缓存当前发送的数据包，便于后续的超时重传</span></span><br><span class="line">	buffer_lock.<span class="built_in">unlock</span>();</span><br><span class="line">	<span class="built_in">sendto</span>(*server, (<span class="type">char</span>*)packet, len + <span class="built_in">sizeof</span>(Header), <span class="number">0</span>, (sockaddr*)server_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));				<span class="comment">// 发送当前数据包</span></span><br><span class="line">	print_lock.<span class="built_in">lock</span>();</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;向服务器发送数据包，首部为: seq:&quot;</span> &lt;&lt; packet-&gt;hdr.seq &lt;&lt; <span class="string">&quot;, ack:&quot;</span> &lt;&lt; packet-&gt;hdr.ack &lt;&lt; <span class="string">&quot;, flag:&quot;</span> &lt;&lt; packet-&gt;hdr.flag &lt;&lt; <span class="string">&quot;, checksum:&quot;</span> &lt;&lt; packet-&gt;hdr.checksum &lt;&lt; <span class="string">&quot;, len:&quot;</span> &lt;&lt; packet-&gt;hdr.length &lt;&lt; <span class="string">&quot;, 剩余窗口大小:&quot;</span> &lt;&lt; WND - (nextseqnum - base) - <span class="number">1</span> &lt;&lt;endl;</span><br><span class="line">	print_lock.<span class="built_in">unlock</span>();</span><br><span class="line">	<span class="keyword">if</span> (base == nextseqnum) &#123;</span><br><span class="line">		my_timer.<span class="built_in">start_timer</span>();		<span class="comment">// 如果是窗口发送的第一个数据包，则开启计时器</span></span><br><span class="line">	&#125;		</span><br><span class="line">	nextseqnum += <span class="number">1</span>;				<span class="comment">// nextseqnum加一</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>发送端接收报文线程函数</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">receive_thread</span><span class="params">(SOCKET *server, SOCKADDR_IN *server_addr)</span> </span>&#123;</span><br><span class="line">	<span class="comment">// 开启非阻塞模式，既要接收数据，也要判断超时</span></span><br><span class="line">	u_long mode = <span class="number">1</span>;</span><br><span class="line">	<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">	<span class="type">char</span> *recv_buffer = <span class="keyword">new</span> <span class="type">char</span>[<span class="built_in">sizeof</span>(Header)];</span><br><span class="line">	Header *header;</span><br><span class="line">	SOCKADDR_IN ser_addr;</span><br><span class="line">	<span class="type">int</span> ser_addr_len = <span class="built_in">sizeof</span>(SOCKADDR_IN);</span><br><span class="line">	<span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">		<span class="keyword">if</span> (send_over) &#123;	<span class="comment">// 如果文件发送成功，结束线程</span></span><br><span class="line">			mode = <span class="number">0</span>;</span><br><span class="line">			<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">			<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">while</span> (<span class="built_in">recvfrom</span>(*server, recv_buffer, <span class="built_in">sizeof</span>(Header), <span class="number">0</span>, (sockaddr*)&amp;ser_addr, &amp;ser_addr_len) &lt;= <span class="number">-1</span>) &#123;</span><br><span class="line">			<span class="keyword">if</span> (send_over) &#123;<span class="comment">// 如果文件发送成功，结束线程</span></span><br><span class="line">				mode = <span class="number">0</span>;</span><br><span class="line">				<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">				<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">				<span class="keyword">return</span>;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="keyword">if</span> (my_timer.<span class="built_in">time_out</span>()) &#123;</span><br><span class="line">				<span class="keyword">for</span> (<span class="keyword">auto</span> packet : gbn_buffer) &#123;	<span class="comment">// 如果计时器超时，则重传当前窗口中的所有未确认的报文</span></span><br><span class="line">					<span class="built_in">sendto</span>(*server, (<span class="type">char</span>*)packet, <span class="built_in">sizeof</span>(Header) + packet-&gt;hdr.length, <span class="number">0</span>, (sockaddr*)server_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">					print_lock.<span class="built_in">lock</span>();</span><br><span class="line">					cout &lt;&lt; <span class="string">&quot;超时重传数据包，首部为: seq:&quot;</span> &lt;&lt; packet-&gt;hdr.seq &lt;&lt; <span class="string">&quot;, ack:&quot;</span> &lt;&lt; packet-&gt;hdr.ack &lt;&lt; <span class="string">&quot;, flag:&quot;</span> &lt;&lt; packet-&gt;hdr.flag &lt;&lt; <span class="string">&quot;, checksum:&quot;</span> &lt;&lt; packet-&gt;hdr.checksum &lt;&lt; <span class="string">&quot;, len:&quot;</span> &lt;&lt; packet-&gt;hdr.length &lt;&lt; endl;</span><br><span class="line">					print_lock.<span class="built_in">unlock</span>();</span><br><span class="line">				&#125;</span><br><span class="line">				my_timer.<span class="built_in">start_timer</span>();</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		header = (Header*)recv_buffer;</span><br><span class="line">		<span class="type">int</span> chksum = <span class="built_in">checksum</span>(recv_buffer, <span class="built_in">sizeof</span>(Header));</span><br><span class="line">		<span class="keyword">if</span> (chksum != <span class="number">0</span>) &#123;</span><br><span class="line">			<span class="keyword">continue</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (header-&gt;flag == RST) &#123;</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;连接异常，退出程序&quot;</span> &lt;&lt; endl;</span><br><span class="line">			<span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> <span class="keyword">if</span> (header-&gt;flag == ACK) &#123;</span><br><span class="line">			<span class="type">int</span> recv_num = header-&gt;ack + <span class="number">1</span> - base;</span><br><span class="line">			<span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; recv_num; i++) &#123; <span class="comment">// 接收到一个ACK报文，且是一个新的ACK，则将ACK之前的缓冲区中的报文弹出</span></span><br><span class="line">				buffer_lock.<span class="built_in">lock</span>();</span><br><span class="line">				<span class="keyword">if</span> (gbn_buffer.<span class="built_in">size</span>() &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">					<span class="keyword">break</span>;</span><br><span class="line">				&#125;</span><br><span class="line">				<span class="keyword">delete</span> gbn_buffer[<span class="number">0</span>];</span><br><span class="line">				gbn_buffer.<span class="built_in">erase</span>(gbn_buffer.<span class="built_in">begin</span>());</span><br><span class="line">				buffer_lock.<span class="built_in">unlock</span>();</span><br><span class="line">			&#125;</span><br><span class="line">			base = header-&gt;ack + <span class="number">1</span>;</span><br><span class="line">            print_lock.<span class="built_in">lock</span>();</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;接收到来自服务器的数据包，首部为: seq:&quot;</span> &lt;&lt; header-&gt;seq &lt;&lt; <span class="string">&quot;, ack:&quot;</span> &lt;&lt; header-&gt;ack &lt;&lt; <span class="string">&quot;, flag:&quot;</span> &lt;&lt; header-&gt;flag &lt;&lt; <span class="string">&quot;, checksum:&quot;</span> &lt;&lt; header-&gt;checksum &lt;&lt; <span class="string">&quot;, len:&quot;</span> &lt;&lt; header-&gt;length &lt;&lt; <span class="string">&quot;, 剩余窗口大小:&quot;</span> &lt;&lt; WND - (nextseqnum - base) &lt;&lt; endl;</span><br><span class="line">			print_lock.<span class="built_in">unlock</span>();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (base != nextseqnum) &#123;</span><br><span class="line">			my_timer.<span class="built_in">start_timer</span>();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123; <span class="comment">// 窗口空了，关闭计时器</span></span><br><span class="line">			my_timer.<span class="built_in">stop_timer</span>();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="接收端接收数据"><a href="#接收端接收数据" class="headerlink" title="接收端接收数据"></a>接收端接收数据</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">recv_file</span><span class="params">(SOCKET *server, SOCKADDR_IN *server_addr, <span class="type">char</span> *data_buffer, <span class="type">int</span> *len)</span> </span>&#123;</span><br><span class="line">	<span class="type">bool</span> file_finish = <span class="literal">false</span>;</span><br><span class="line">	SOCKADDR_IN client_addr;</span><br><span class="line">	<span class="type">int</span> client_addr_length = <span class="built_in">sizeof</span>(SOCKADDR_IN);</span><br><span class="line">	Header header;</span><br><span class="line">	<span class="comment">// 接收缓冲和发送缓冲</span></span><br><span class="line">	<span class="type">char</span> *recv_buffer = <span class="keyword">new</span> <span class="type">char</span>[MSS + <span class="built_in">sizeof</span>(header)];</span><br><span class="line">	<span class="type">char</span> *send_buffer = <span class="keyword">new</span> <span class="type">char</span>[<span class="built_in">sizeof</span>(header)];</span><br><span class="line">	<span class="built_in">memset</span>(recv_buffer, <span class="number">0</span>, MSS + <span class="built_in">sizeof</span>(header));</span><br><span class="line">	<span class="built_in">memset</span>(send_buffer, <span class="number">0</span>, <span class="built_in">sizeof</span>(header));</span><br><span class="line">	<span class="comment">// 先初始化一下send_buffer</span></span><br><span class="line">	((Header*)send_buffer)-&gt;<span class="built_in">set_args</span>(<span class="number">0</span>, <span class="number">0</span>, ACK, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">	((Header*)send_buffer)-&gt;checksum = <span class="built_in">checksum</span>(send_buffer, <span class="built_in">sizeof</span>(Header));</span><br><span class="line">	<span class="comment">// 一直接收数据</span></span><br><span class="line">	u_long mode = <span class="number">1</span>;</span><br><span class="line">	<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">	<span class="type">clock_t</span> start;</span><br><span class="line">	<span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">		<span class="type">int</span> result;</span><br><span class="line">		<span class="keyword">while</span> ((result = <span class="built_in">recvfrom</span>(*server, recv_buffer, MSS + <span class="built_in">sizeof</span>(header), <span class="number">0</span>, (sockaddr*)&amp;client_addr, &amp;client_addr_length)) &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">			<span class="comment">// 长时间没有接收到信息，且文件传完了，认为对方嘎了</span></span><br><span class="line">			<span class="keyword">if</span> (file_finish == <span class="literal">true</span> &amp;&amp; <span class="built_in">clock</span>() - start &gt; WAITTIME) &#123;</span><br><span class="line">				cout &lt;&lt; <span class="string">&quot;长时间没有接收到报文，断开连接&quot;</span>;</span><br><span class="line">				<span class="keyword">delete</span> []send_buffer;</span><br><span class="line">				<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">				mode = <span class="number">0</span>;</span><br><span class="line">				<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">				<span class="keyword">return</span>;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// 得到数据头</span></span><br><span class="line">		<span class="built_in">memcpy</span>(&amp;header, recv_buffer, <span class="built_in">sizeof</span>(header));</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;接收到长度为&quot;</span> &lt;&lt; result &lt;&lt; <span class="string">&quot;字节的数据报,头部为: &quot;</span>;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;seq: &quot;</span> &lt;&lt; header.seq &lt;&lt; <span class="string">&quot; ack: &quot;</span> &lt;&lt; header.ack &lt;&lt; <span class="string">&quot; flag: &quot;</span> &lt;&lt; header.flag &lt;&lt; <span class="string">&quot; checksum: &quot;</span> &lt;&lt; header.checksum &lt;&lt; <span class="string">&quot; length: &quot;</span> &lt;&lt; header.length &lt;&lt; endl;</span><br><span class="line">		<span class="comment">// 先计算一下校验和</span></span><br><span class="line">		u_short chksum = <span class="built_in">checksum</span>(recv_buffer, result);</span><br><span class="line">		<span class="comment">// 校验和不对</span></span><br><span class="line">		<span class="keyword">if</span> (chksum != <span class="number">0</span>) &#123;</span><br><span class="line">			<span class="type">int</span> n = <span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(Header), <span class="number">0</span>, (sockaddr*)&amp;client_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;数据报校验和出错&quot;</span> &lt;&lt; endl;</span><br><span class="line">			<span class="keyword">continue</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (header.flag == FIN) &#123;</span><br><span class="line">			Header reply_header;</span><br><span class="line">			reply_header.<span class="built_in">set_args</span>(<span class="number">0</span>, (u_short)(header.seq + <span class="number">1</span>), ACK, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">			<span class="built_in">memcpy</span>(send_buffer, (<span class="type">char</span>*)&amp;reply_header, <span class="built_in">sizeof</span>(reply_header));</span><br><span class="line">			chksum = <span class="built_in">checksum</span>(send_buffer, <span class="built_in">sizeof</span>(reply_header));</span><br><span class="line">			((Header*)send_buffer)-&gt;checksum = chksum;</span><br><span class="line">			<span class="type">int</span> n = <span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(reply_header), <span class="number">0</span>, (sockaddr*)&amp;client_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;接收到FIN报文，发回ACK报文，传输结束&quot;</span> &lt;&lt; endl;</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// 接收到一个数据报文</span></span><br><span class="line">		<span class="keyword">else</span> <span class="keyword">if</span> (header.seq == expectedseqnum) &#123;</span><br><span class="line">			<span class="comment">// 发回一个ack</span></span><br><span class="line">			Header reply_header;</span><br><span class="line">			reply_header.<span class="built_in">set_args</span>(<span class="number">0</span>, expectedseqnum, ACK, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">			<span class="built_in">memcpy</span>(send_buffer, (<span class="type">char</span>*)&amp;reply_header, <span class="built_in">sizeof</span>(reply_header));</span><br><span class="line">			chksum = <span class="built_in">checksum</span>(send_buffer, <span class="built_in">sizeof</span>(reply_header));</span><br><span class="line">			((Header*)send_buffer)-&gt;checksum = chksum;</span><br><span class="line">			<span class="type">int</span> n = <span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(reply_header), <span class="number">0</span>, (sockaddr*)&amp;client_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;发送ACK报文:&quot;</span> &lt;&lt; <span class="string">&quot;seq: &quot;</span> &lt;&lt; reply_header.seq &lt;&lt; <span class="string">&quot; ack: &quot;</span> &lt;&lt; reply_header.ack &lt;&lt; <span class="string">&quot; flag: &quot;</span> &lt;&lt; reply_header.flag &lt;&lt; <span class="string">&quot; checksum: &quot;</span> &lt;&lt; chksum &lt;&lt; <span class="string">&quot; length: &quot;</span> &lt;&lt; reply_header.length &lt;&lt; endl;</span><br><span class="line">			expectedseqnum++;</span><br><span class="line">			<span class="built_in">memcpy</span>(data_buffer + *len, recv_buffer + <span class="built_in">sizeof</span>(header), header.length);</span><br><span class="line">			*len += header.length;</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;数据报成功获取&quot;</span> &lt;&lt; endl;</span><br><span class="line">			<span class="comment">// 最后一个报文到了</span></span><br><span class="line">			<span class="keyword">if</span> (header.flag &amp; LAS) &#123;</span><br><span class="line">				file_finish = <span class="literal">true</span>;</span><br><span class="line">				start = <span class="built_in">clock</span>();</span><br><span class="line">				cout &lt;&lt; <span class="string">&quot;文件接收完毕，等待客户端挥手&quot;</span> &lt;&lt; endl;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">			<span class="type">int</span> n = <span class="built_in">sendto</span>(*server, send_buffer, <span class="built_in">sizeof</span>(Header), <span class="number">0</span>, (sockaddr*)&amp;client_addr, <span class="built_in">sizeof</span>(SOCKADDR_IN));</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">delete</span> []send_buffer;</span><br><span class="line">	<span class="keyword">delete</span> []recv_buffer;</span><br><span class="line">	mode = <span class="number">0</span>;</span><br><span class="line">	<span class="built_in">ioctlsocket</span>(*server, FIONBIO, &amp;mode);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="实验结果展示"><a href="#实验结果展示" class="headerlink" title="实验结果展示"></a>实验结果展示</h2><p>握手与挥手和实验第一部分一致，在此略过。</p>
<h3 id="数据传输"><a href="#数据传输" class="headerlink" title="数据传输"></a>数据传输</h3><ul>
<li><p>开启路由程序<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212021603441.jpg" alt=""></p>
</li>
<li><p>数据传输<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212021631765.jpg" alt=""></p>
</li>
<li><p>超时重传<br>如下图所示，序列号为232的报文产生了丢失，发送端发生超时，将会重传当前窗口中所有已发送但未被确认的报文。<br><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212021632873.png" alt=""></p>
</li>
</ul>
<h3 id="传输结果"><a href="#传输结果" class="headerlink" title="传输结果"></a>传输结果</h3><p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212021611641.jpg" alt=""></p>
<p><img src="https://raw.githubusercontent.com/yuwensq/imgBase/master/202212021612400.jpg" alt=""></p>
<p>如上图所示，传输结果与原图大小、信息完全相同，传输成功。</p>
]]></content>
      <tags>
        <tag>计网</tag>
      </tags>
  </entry>
</search>
